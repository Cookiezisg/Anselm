---
id: WRK-090
type: working
status: active
owner: "@weilin"
created: 2026-07-28
reviewed: 2026-07-28
review-due: 2026-10-26
audience: [human, ai]
landed-into:
---

# WRK-090 · 旅程目录(JOURNEYS)——⚠️ 待用户过目(N6:旅程编码产品意图,源头应是用户)

> WRK-087 的旅程账本。**旅程是主循环的基本单位**:一个真实用户带一个真实目的进来,端到端走完;
> 判据是**这件事办成没有 + 全程体验配得上 craft bar**(五级判据全过)。**驻停清扫**:旅程每到一站,
> 打开该面的 [COVERAGE](COVERAGE.md) 行穷尽掉才离站;一条旅程 ✅ 的条件 = 目的达成 **且** 沿途
> 每站整列点亮。每条旅程四行:**目**(用户视角一句话)/**线**(关键站点)/**判**(本线特别要压的
> CODEX 法条或 EDGE 行)/**构**(前置与构造,缺省=无特殊构造)。
>
> 状态列:`·` 未跑 / `▶` 进行中 / `✗` 卡在开缺陷 / `✓` 完(目的达成+沿途全清)。

## A · 初见与落地

| # | 状态 | 旅程 |
|---|---|---|
| J-A1 | · | **全新安装的第一次**。目:装好打开,五分钟内发出第一条消息并得到有用回答,全程零配置。线:onboarding 单页→创建工作区→Composer 飞行→空白 chat→首消息(免费档自动开通)。判:G1;B 系动效(560ms 飞行/reduced 落位);EDGE 空工作区名册/首启创建过渡。构:出厂态(清数据目录) |
| J-A2 | · | **不读文档的第一小时**。目:新用户凭界面自己发现四海洋、@、附件、设置、通知。线:自由探索,每个入口以「第一次看到」的眼光判。判:G1/G2 全程;E3 空态=满态收起形 |
| J-A3 | · | **断网的第一次打开**。目:没网也能打开、能看、能被诚实告知什么不可用。线:拔网冷启动→目录完整→免费档缺席不挂→联网后自愈。判:E4;EDGE 断网启动/开通降级不挂 boot |
| J-A4 | · | **出厂重置轮回**。目:重置后像新机器一样重新开始,不留幽灵状态。线:设置→输名确认→重启→onboarding 重现→再走 J-A1。判:EDGE 出厂重置/keychain 铸钥只对全新安装 |

## B · 对话主线

| # | 状态 | 旅程 |
|---|---|---|
| J-B1 | · | **问一个需要动手的问题**。目:「帮我看看这个文件夹里最大的文件是什么」级任务真办成。线:发问→工具调用(Bash/LS)→工具卡→答案正确(DB/文件系统对证)。判:F1/F4;A5 流式不冻结 |
| J-B2 | · | **长对话马拉松**。目:100+ 回合后模型仍记得开头定下的关键事实,体验不劣化。线:灌长对话→触发压缩→追问早期事实→翻历史滚动。判:EDGE 压缩全族(水位/幂等/demote/附件跨水位);B2 滚动锚不漂 |
| J-B3 | · | **改嘴重来**。目:对回答不满意,重试/换模型重试/编辑重发/分叉,每条支线可回看。线:`:retry` 两分支→版本翻页 ‹2/2›→`:fork`→两分支各自发展。判:EDGE retry 全族/fork 全族(水位重定基/指针 remap) |
| J-B4 | · | **生成中的自由**。目:模型在写时我不被锁住。线:流式中打字→Enter 入队→点开改队列→✕ 删→停止(在飞停、队列留)→空框 ↑ 取回。判:A5;EDGE 生成中再 Send/单槽缓冲;B2 流式不夺视口 |
| J-B5 | · | **@ 出一切**。目:@文档/@实体/@skill 把上下文精确带给模型。线:三类 @→注入验证(线缆)→删除被 @ 源→missing 警告行。判:F4;EDGE 对话挂载的文档被删;fork skill 不进候选 |
| J-B6 | · | **喂它真世界**。目:PDF/截图/CSV 丢进去,模型真看见、真用上。线:拖拽/粘贴/按钮三入口→PDF 原生或抽取→图原生→线缆证像素。判:F4;EDGE 附件 sandbox 提取/不认 mime 降级/单回合媒体额度 |
| J-B7 | · | **人闸的一天**。目:危险的事它先问我,问得我看得懂;我说过总是允许就不再烦我;驻地外写永远拦。线:dangerous 确认→approve_always→越界写(不可豁免)→ask_user→interaction 重同步。判:EDGE 人闸全族;E1 确认框文案 |
| J-B8 | · | **整理我的对话**。目:找到、置顶、改名、归档、按驻地清扫,绝不误伤。线:搜索→置顶→rail 四段→驻地组 ⋯(归档全部/删除全部——**永不出现「目录」字样**)→已归档视图。判:EDGE 驻地分组批量全族;F1 组头计数不漂 |
| J-B9 | · | **听它说**。目:任意回复点喇叭就能听,重听不花钱。线:喇叭→加载→播放三态→重听(llmtap 数调用不变)→换音色。判:F4 零计费以调用数为证;EDGE 朗读缓存全族 |
| J-B10 | · | **侧幕剧场**。目:右岛把 AI 的动作演给我看,我能跳回任何现场。线:工具活动登台→跟随三档→手动关尊重→Cast 点行登台→深跳 re-anchor→回到现场 pill。判:EDGE 侧幕全族;B3 落定洗亮 |

## C · 多模态创作线

| # | 状态 | 旅程 |
|---|---|---|
| J-C1 | · | **生图迭代到满意**。目:画一张海报,改三轮(「背景换黄昏」「字挪上去」),满意的嵌进文档,@文档继续聊时模型看得见图。线:generate_image→多轮改(上下文传递质量=H2)→图卡→嵌文档→@文档→线缆证像素。判:终点验收①⑥同源;F4;ADR 0017 只回 receipt |
| J-C2 | · | **一句话配音**。目:让它说一段话,生成、听、改词重来。线:generate_speech→音频卡三态→改文本重生成(新缓存键)。判:EDGE 多块 PCM 拼接;F4 |
| J-C3 | · | **真出一条视频**。目:生成一条短片,等待期我知道进度、对话不冻、出来能直接看。线:generate_video→danger 闸响(花钱=不可逆)→批准→progress 状态行→产物卡→点击内联播放。判:A4/A5;EDGE 视频轮询超时诚实话;H5.6 成本判据 |
| J-C4 | · | **数据变图表**。目:丢一份 CSV,让它算并画图,图能进文档。线:上传→fn matplotlib→`$media` 声明→receipt→模型看见→嵌文档。判:终点验收③;EDGE function 媒体产物/路径逃逸 |
| J-C5 | · | **一张图的六个家**。目:同一张图在 chat 卡/侧幕/调试台/运行卷宗/approval 门/文档里渲染一致体面。线:铸一张图,六个面逐一看。判:C 系全程;一族卡按 mime 分发 |
| J-C6 | · | **能力的诚实边界**。目:没有路由时功能不出现;模型看不了图时明说。线:拔 key→generate_* 缺席+设置格「怎么获得」→换无 vision 模型发图→文字占位。判:E4;EDGE 能力工具诚实缺席/模型能力缺失降级 |

## D · 构建线(Quadrinity)

| # | 状态 | 旅程 |
|---|---|---|
| J-D1 | · | **第一个函数**。目:说需求,AI 建出来,跑通,改一版,不满意回滚。线:对话建 fn→build 镜像进实体面板→:run→edit→版本 diff→revert→执行历史。判:F1 版本指针;EDGE 版本 cap 50 trim |
| J-D2 | · | **装不上的依赖**。目:拼错包名它自己救;真装不上它诚实说。线:错依赖→envfix 循环(entities 流看尝试行)→改对→装好;真坏→env failed 仍可见→:run 422。判:EDGE envfix 全族;E1 |
| J-D3 | · | **有状态柜台**。目:建一个记着状态的 handler,配置、多轮调用、重启后状态语义清楚。线:create→config 必填闸→call×N(状态保持)→restart(状态清,可见)→edit 重启语义。判:EDGE handler 全族(单飞/孤儿 key/掩码三面) |
| J-D4 | · | **雇一个员工**。目:配一个懂我业务的 agent,挂上文档和工具,派活给它。线:建 agent 挂 skill/知识/fn/sys:generate→mount-health→invoke→输出 schema→:iterate 改。判:EDGE agent 挂载全族/声明输出回解析 |
| J-D5 | · | **实体页深游**。目:每个实体我能看懂它是什么、经历过什么、连着谁。线:详情页各 kind 量身→版本 tab diff→日志 tab→关系图 fly-to→调试台「用这份输入」。判:C/G 系;F1 |
| J-D6 | · | **删除的后果**。目:删被依赖的东西时我被告知谁断了。线:删被 3 个 agent 挂的 fn→dependency_broken 聚合通知→mount-health 变红→关系图对账。判:EDGE 删被依赖实体;E1 |

## E · 自动化线(workflow/trigger/scheduler)

| # | 状态 | 旅程 |
|---|---|---|
| J-E1 | · | **从零建一条自动化**。目:纯对话拥有「每天 8 点抓天气画卡片发通知」,不懂技术也行。线:对话建 fn+agent+wf+cron→capability check→:stage 试跑→activate→真 fire(拨近刻度)→通知带图到。判:终点验收②同源;G1;F 全程 |
| J-E2 | · | **图的语言**。目:分支、审批、循环我都能表达,建错了它会告诉我。线:control 分支+approval+回边→祖先可见性 lint→图编辑器→MaxIterations 栅栏。判:EDGE 菱形 join/栅栏;图编辑器 craft |
| J-E3 | · | **审批的一天**。目:该我批的事在哪都找得到,批了立刻走,不批会超时按我定的办。线:park→铃托盘+scheduler 收件箱+deadline 倒计时→批/拒→超时三行为→人 vs 超时竞态。判:EDGE approval 全族;F2 帧一致 |
| J-E4 | · | **失败与修复**。目:跑挂了我看得懂为什么,修好从断点续。线:节点失败→run 红→attention 点→:triage 诊断对话→修实体→:replay(completed 复用)。判:EDGE replay 全族;E1 错误呈现 |
| J-E5 | · | **调度指挥室**。目:所有自动化的健康一眼可见,任何 run 三击内到细节。线:Overview 五牌→运营主页(矩阵/珠串/连败)→run 旗舰(甘特/台账)→节点检查器。判:F1 牌数==列表长;C 系 |
| J-E6 | · | **重叠的五种脾气**。目:同一 workflow 连续触发时按我选的策略办。线:serial/skip/buffer_one/replace/allow_all 逐一构造双触发。判:EDGE overlap 全族/手动绕过 |
| J-E7 | · | **崩溃之夜**。目:跑到一半断电,醒来它自己续上,一步不重复。线:kill -9 半途 run→重启→Recover 续跑(completed 被抄)→孤儿回合清扫→SSE 410 重放。判:EDGE 崩溃族;F5 |
| J-E8 | · | **错过的刻度**。目:停机/睡眠错过的 cron 有账可查,按我的策略补或不补。线:停机跨刻度→missed 落账(回拨)→catchup_one 补一个→misfire 台账封顶。判:EDGE misfire 全族 |
| J-E9 | · | **外面的世界敲门**。目:webhook 一贴就通,错签名进不来。线:建 webhook→curl 打→HMAC 错 401→分钟桶去重→改 path 旧 404。判:EDGE webhook 全族 |
| J-E10 | · | **看着文件夹干活**。目:文件一落盘流程就跑。线:fsnotify trigger→放文件→秒桶去重→sensor 电平+并发策略兜风暴。判:EDGE fsnotify/sensor 族 |

## F · 知识与技能线(library/skill/MCP)

| # | 状态 | 旅程 |
|---|---|---|
| J-F1 | · | **写一篇真文档**。目:像 Notion 一样顺手:slash、表格、代码块、链接、拖树。线:树建档→slash 全套→[[链接]]→大纲/反链/属性→关三保真(围栏语言标)→拖拽重排。判:C1 选区等高**在此站深清**;EDGE 编辑器族;三保真 |
| J-F2 | · | **skill 的一生**。目:把一套做法固化成 skill,用的时候一喊就来,更新有底线。线:create→activate($ARGUMENTS)→allowed-tools 信任门(未批=逐次确认)→本地改→:update 409→force。判:EDGE skill 全族(炸弹护栏/路径穿越) |
| J-F3 | · | **装一个 MCP**。目:市场里挑一个装上,它的工具马上能用,坏了能救,不要了能删干净。线:市场→装(缺 env 422)→chat 用 mcp__ 工具→degraded 变色→重连→卸载(挂载边清)。判:EDGE MCP 全族 |
| J-F4 | · | **MCP OAuth 全流程**。目:要授权的服务,浏览器点一下就通。线:装 DCR server→浏览器授权→回调→调用→refresh 失效→重授权指引。判:EDGE OAuth 族;G1 |
| J-F5 | · | **它记得我**。目:告诉它的偏好,换个对话还记得;我能管这些记忆。线:write_memory→跨对话读回→pin 常驻→设置面板增删。判:EDGE memory 更新保留策展 |
| J-F6 | · | **搜到一切,搜不到秘密**。目:模糊印象也能找到东西;secret 永远搜不出。线:综搜→垂搜→语义混合(乱码闸)→embedder off 降级→搜 api key 值零命中。判:EDGE search 全族;**密文红线必须过** |

## G · 驻地与真实工作线

| # | 状态 | 旅程 |
|---|---|---|
| J-G1 | · | **在我的仓库里干活**。目:挂上真 git 仓,AI 聚焦这里读改跑,git 操作一键。线:挂驻地(三态字形)→读外放行→写外闸→切分支/建分支/开 worktree→在访达/终端打开。判:EDGE 驻地全族;C 面包屑恒 28pt 不重排 |
| J-G2 | · | **边界的诚实**。目:脏区不让我切分支;目录没了告诉我而不是装死。线:脏区切分支 422→目录移走 exists=false 警示→marker 块落账→Bash 拒绝。判:EDGE 脏区/目录被移走/marker 族 |

## H · 设置与平台线

| # | 状态 | 旅程 |
|---|---|---|
| J-H1 | · | **设置十三面板走查**。目:每个设置我能找到(搜索)、看懂、改了立刻生效或明说要重启。线:13 面板逐控件+设置项搜索+推入三级(13 kind)。判:驻停清扫 SURF settings 全段;A/C/E 系全程 |
| J-H2 | · | **我的钥匙我做主**。目:BYOK 添加即用,坏了看得出,受管档一键修。线:加 key→探测→场景默认→受管卡/配额面→install 自愈(修复钮)→删被引用 key 被指路。判:EDGE apikey/freetier 全族 |
| J-H3 | · | **手感调校**。目:快捷键改成我的习惯,缩放随我眼睛。线:改绑(冲突拒/录后不吞键/冷启动首键活)→⌘+- 到顶即停→字体三轴。判:EDGE 快捷键族/缩放到顶 |
| J-H4 | · | **通知的一生**。目:后台发生的事我不会漏,也不会被淹。线:后台事件→顶带(候场/快照清场/公平调度)→托盘时段组→OS 通知→已读对账→5000 积压。判:EDGE 顶带族;F1 徽标绝不据帧+1 |
| J-H5 | · | **存储治理**。目:占了多少、怎么瘦身,一目了然且不吓人。线:占用盘点→保留线改(踢清理)→压缩 VACUUM→沙箱 GC 两步→孤儿深链墓碑。判:EDGE 保留清理/磁盘回收族 |
| J-H6 | · | **多个工作区**。目:公私分明,切换不串,删得干净。线:建第二个→热切换三拍(旧 id 不 404)→隔离验证(rail/搜索/触点互不可见)→删除级联→守最后一个。判:EDGE workspace 族;D2 隔离链 |

## I · 坏天气线(degraded/对抗)

| # | 状态 | 旅程 |
|---|---|---|
| J-I1 | · | **网关挂了**。目:上游死了我知道是它死了,不是我错了;活了自己好。线:llmtap 上游指向黑洞→聊天失败呈现(E1 三要素)→恢复→自愈。判:E1/E4;EDGE 配额与限流分流 |
| J-I2 | · | **后端崩了**。目:sidecar 死了前端诚实,重启无缝续。线:kill sidecar→前端反应(不灰屏死)→重启→SSE 410→六处 resync 对账。判:EDGE lifecycleResync 族;F5 |
| J-I3 | · | **塞爆它**。目:极端输入不打爆任何一层。线:256KiB tool_result 顶→1MB 文档拒→超长输入→5000 通知→数千行 flowrun 分页。判:EDGE 各封顶族;A 系响应不劣化 |
| J-I4 | · | **恶意的一天**。目:走歪路的输入被有名有姓地拒绝。线:路径穿越(skill files/`$media`)→webhook 错签名→非 loopback Host→prompt injection 样本(工具描述照 H3 兜底)。判:EDGE 安全族;fail-closed 处处 |
| J-I5 | · | **模型跑偏**。目:模型犯轴时产品有底线,我看得懂发生了什么。线:构造重复调用(熔断三道闸)→MaxSteps 顶「继续」→工具风暴熔断→坏参数 ObjectMap 容忍。判:H3;EDGE loop 熔断族 |

**共 47 条主线。** 死角补线(COVERAGE diff 出的无旅程行)在主循环中按需追加于各区之后,命名 `J-<区>x`。

## 排程(与 WRK-087 §7 对齐)

区顺序 = A→B→C→D→E→F→G→H→I(用户旅程序;坏天气收尾防脏环境)。每区末尾跑该区
COVERAGE diff,孤行补线。每日晨间简报报:旅程 ✓ 数、格点亮率、立法数、修复数、代拍数。
