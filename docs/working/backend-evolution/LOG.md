---
id: WRK-026-LOG
type: working
status: active
owner: @weilin
created: 2026-07-29
reviewed: 2026-07-29
review-due: 2026-10-27
audience: [human, ai]
---

# LOG · 已确认发现

> 只追加。每行必须是已确认的事实，并能指向复现、测试、提交或真相源；探索假设不记录在这里。

| 日期 | ID | 发现 / 影响 | 范围 | 证据与守卫 | 落点 |
|---|---|---|---|---|---|
| 2026-07-28 | EVO-001 | 产品边界改为“写留给受管，读交给目录”；BYOK 多模态输入为正式能力 | 聊天、模型能力、生成工具 | WRK-085（2026-07-29 landed）；能力与路由文档 | H11/H12 完成、结论已入 references |
| 2026-07-28 | EVO-002 | OpenAI-compatible 流式工具参数存在增量和累积两种 wire；拼接累积值会使工具调用全量失败 | compat provider / agent loop | `toolargs_test.go`；真实 DashScope 线缆 | compat 归一层 |
| 2026-07-28 | EVO-003 | 音色登记必须走生产网关，且上游异步就绪；mock 无法证明此契约 | managed voice lifecycle | `TestLiveVoice_EnrollSpeakDelete`，`EVALS_VOICE=1` | live acceptance |
| 2026-07-28 | EVO-004 | 真实多模态验收须同时保存上游请求与产物字节，不能采信模型自述 | chat/workflow/MCP/function/handler | `live_media_test.go`，`EVALS_MEDIA=1` | live acceptance |
| 2026-07-29 | EVO-005 | 受管语音真钱验收硬编码旧 qwen3-tts 音色 `Cherry`，使 qwen-audio-3.0 默认路径假红；默认音色必须由 API Serve 决定 | managed TTS acceptance | `TestLiveManaged_ImageAndSpeech`：旧值失败、空 voice 重跑验证 | 当前提交 |
| 2026-07-29 | EVO-006 | 新 workspace 在受管 key 落盘后先做网络能力刷新、后播种默认模型；用户首发消息可命中无默认的 `LLM_RESOLVE_ERROR` | freetier onboarding / 默认聊天 | `TestLiveManaged_DefaultChat` 复现；首发黑盒守卫 + `TestEnsure_SeedsDefaultsBeforeLiveCapabilityProbe` | 当前提交 |
| 2026-07-29 | EVO-007 | 静态多模态夹具把 CRC 损坏的字节称作“合法 PNG”；mock 从不解码而掩盖问题，真实受管视觉路由会被上游拒绝 | testend mock / 媒体回归 | `TestLiveManaged_DefaultChatWithImageAttachment` 先红；`TestMockPNGDecodes` 守 decoder-valid fixture；32×32 真图重跑通过 | 当前提交 |
| 2026-07-29 | EVO-008 | BYOK 视觉输入可在不借用受管 fallback 的情况下走完真实 key 创建、probe、目录能力、默认模型、附件与对话；它证明真实 provider 接受产品请求，不把模型文案误当像素语义证据 | BYOK read / OpenAI-compatible visual input | `EVALS_BYOK=1` + `OPENAI_API_KEY` 的 `TestLiveBYOK_OpenAIImageInput`；harness 默认关闭 gateway，附件字节与 durable turn 双侧断言 | 当前提交 |
| 2026-07-29 | EVO-009 | 混合路径可以让 BYOK 对话模型调度、但由 Anselm 受管路由出图；默认图像场景仍指向 managed key，receipt 标明 `anselm`，一次受限回合只铸一件真实图片 | hybrid / BYOK planner + managed writer | `EVALS_HYBRID=1 EVALS_MANAGED=1` + `OPENAI_API_KEY` 的 `TestLiveHybrid_OpenAIPlansManagedImage`；受管 route、tool receipt、一次调用和产物字节均断言 | 当前提交 |
| 2026-07-29 | EVO-010 | 默认受管 MP4 输入可走完上传、device-proof staging/lease、部署网关与 durable 对话；规范短片未越过网关发布的 3MiB 解码预算，附件字节保持不变 | managed read / video input | `EVALS_MANAGED=1` 的 `TestLiveManaged_DefaultChatWithVideoAttachment`；SHA 校验 fixture、能力投影、回合终态与附件逐字节回读 | 当前提交 |
| 2026-07-29 | EVO-011 | 文档 Markdown 中的 `anselm://media/<attachmentId>` 不会停留为 system prompt 的字面字符串：附图经文档扫描后可进入受管媒体消费路径并完成新对话 | managed read / document media reference | `EVALS_MANAGED=1` 的 `TestLiveManaged_DocumentImageReference`；文档挂载、新对话终态与附件逐字节回读 | 当前提交 |
| 2026-07-29 | EVO-012 | Qwen BYOK 的目录默认 endpoint 与 video 方言可实际接收 MP4：真实 key 的 probe、`qwen3.7-plus` 能力投影、默认模型和附件对话均不借用受管 fallback | BYOK read / Qwen video input | `EVALS_BYOK=1 QWEN_API_KEY=…` 的 `TestLiveBYOK_QwenVideoInput`；harness 默认关闭 gateway，MP4 终态与逐字节回读 | 当前提交 |
| 2026-07-29 | EVO-013 | function 与驻留 handler 的声明式二进制产物都能经共享 `mediaartifact` collector 落为一等附件；handler 连续调用各自拿到不同 receipt，来源与附件字节均保真，且不需 provider key | function / handler artifact producer | `TestFunction_ArtifactProduct`、`TestHandler_ArtifactPerCallProduct`：真实 HTTP、有效 PNG、连续运行/调用、receipt source、附件逐字节回读 | 当前提交 |
| 2026-07-29 | EVO-014 | MCP stdio server 的 image content 会经 `mcp_media` receipt 进入同一附件库，并在下一轮视觉对话请求中以原始 base64 `image_url` 送达；模型文案不是证据，抓包字节与附件回读一致才算通过 | MCP producer / chat media expansion | `TestMCP_ArtifactReachesVisionModel`：真实 Python JSON-RPC server、64×64 PNG、receipt、附件回读、llmmock 线缆 exact-byte 断言、chat 触发台账 | 当前提交 |
| 2026-07-29 | EVO-015 | workflow 可把 function 产物的 MediaRef 从上游 agent 节点的终答交给下游 agent；下游请求包含与附件库逐字节一致的原始 image part，且不需要受管生成或 provider key | workflow / agent-to-agent media | `TestWorkflowMedia_FunctionArtifactToVisionAgent`：真实 function sandbox、flowrun 节点结果、附件回读、第三个模型请求 exact-byte 断言 | 当前提交 |
| 2026-07-29 | EVO-016 | 驻留 handler 的二进制产物也能经 chat 的 lazy `call_handler` 进入视觉消费咽喉；第三次模型请求携带与附件库一致的 PNG，而不是 receipt/占位文本 | handler producer / chat media expansion | `TestHandler_ArtifactReachesVisionModel`：真实 handler sandbox、call_handler 工具、handler_artifact receipt、附件逐字节回读、llmmock 第三请求 exact-byte 断言 | 当前提交 |
| 2026-07-29 | EVO-017 | chat 在 handler 产物调用已发出进度但尚未完成时取消，回合与 handler 台账均落 `cancelled`，临时输出不会晚到上传，附件行数保持不变且不出现伪造 receipt | cancellation / handler media cleanup | `TestChatCancel_HandlerArtifactLeavesNoOrphan`：真实 chat→call_handler、SSE 进度边界、取消、durable 状态、SQLite attachment count、receipt negative assertion | 当前提交 |
| 2026-07-29 | EVO-018 | 对含 handler 产物的 assistant 回合做 regenerate 时，旧 assistant 版本被 supersede，handler 不会再次执行、附件不重复铸造；原始附件行与字节仍可读，符合只对现行版本装配 LLM 历史的 retry 语义 | retry / side-effecting media producer | `TestChatRetry_HandlerArtifactDoesNotReexecute`：真实首次 call_handler、重生成、handler calls=1、附件行数不变、原始内容回读 | 当前提交 |
| 2026-07-29 | EVO-019 | backend 在 handler 媒体调用进行中硬崩溃并重启后，boot sweep 会把 chat 回合对账为 `cancelled`，废弃调用没有晚到附件行 | crash recovery / handler media cleanup | `TestChatCrash_HandlerArtifactLeavesNoOrphan`：真实在途 call_handler、SSE 边界、Kill9→Restart、durable cancelled 状态、SQLite attachment count 不变 | 当前提交 |
| 2026-07-29 | EVO-020 | attachment 的 image preparation 是真实异步、可恢复的用户侧车：有效 PNG 从 queued/processing 到 ready 并暴露 proxy 尺寸；不可解码 image 仍先保留原件、再诚实进入 failed，可 cancel 为 cancelled、retry 后重新失败；非 image 明确为 not_required | attachment / media worker / preparation API | `TestAttachmentPreparation_ManagedImageLifecycle`：真实 HTTP upload→GET 轮询、派生元数据、坏图 failed→cancelled→retry、原件逐字节回读、text not_required | 当前提交 |
| 2026-07-29 | EVO-021 | media cache 超过机器级预算时不会静默丢原件：boot GC 清理派生 CAS、把 ready 行标成 `MEDIA_ARTIFACT_EVICTED` 且可 retry；同一 source/transform identity 可在重启后重新生成 | media cache / boot GC / resource hygiene | `TestAttachmentPreparation_MediaBudgetEvictsAndRegenerates`：真实 `/limits` 设 1MB、>1MB JPEG proxy、Restart、sidecar eviction/error、media CAS 文件清理、原件逐字节回读、retry ready | 当前提交 |
| 2026-07-29 | EVO-022 | `inspect_media` 对 audio/video 当前诚实地只返回本地 metadata capsule：保留 attachment id、kind、mime、size 与 start/end intent，明确写出不含 transcript；下一次模型请求只收到 JSON evidence，原始媒体字节不会泄漏 | attachment tool / temporal media read | `TestInspectMedia_AudioVideoMetadataCapsule`：真实 HTTP upload、lazy `search_tools`、两次 inspect_media、llmmock tool-message JSON 与 raw-byte negative assertion | 当前提交 |
| 2026-07-29 | EVO-023 | image `inspect_media` 走真实嵌套视觉调用：内部请求携带 `data:image/*` native part，主对话拿到的是带 attachment/尺寸/detail/answer 的 bounded JSON evidence，不把 provider 原始回答或图像字节直接回灌 | attachment tool / image inspection / nested LLM | `TestInspectMedia_ImageUsesVisionAndReturnsBoundedEvidence`：真实 HTTP upload、lazy discovery、内部 vision wire、外层 tool-message JSON 解析与字段守卫 | 当前提交 |
| 2026-07-29 | EVO-024 | workflow 中正在 provider 调用的 agent 可经 `POST /flowruns/{id}:cancel` 被打断：run 终止为 `cancelled`，不会留下假 `failed/running` 节点，随后同一 workflow 可接受并完成下一次 run | workflow / scheduler cancellation | `TestFlowrun_CancelInFlightAgent`：真实 agent workflow、异步 manual run、llmmock Stall、list 发现 running、HTTP cancel 202、终态节点负断言、后续 run completed | 当前提交 |
| 2026-07-29 | EVO-025 | API Serve 最新网关变更修正了 managed TTS 的句柄解析、默认音色与 WAV 字节/媒体部署注入边界；这些变化直接改变受管音色验收的真实前提，但尚未宣称 managed E2E 已重跑通过 | API Serve / managed voice / deploy media | API Serve commits `63f402f`, `2e308f3`, `b9c046d`; 在 `Anselm-API-Serve` 执行 `go test ./...` 全绿；managed voice/live read-aloud 下一次真实运行作为 FRT-07 reprobe | gateway audit |
| 2026-07-29 | EVO-026 | image preparation 在上传后硬崩溃可恢复：SIGKILL 留下 running derivative，下一次同 data dir boot 将其 requeue，worker 从不可变原件重新生成 ready proxy，原件字节不变 | attachment / media worker / crash recovery | `TestAttachmentPreparation_CrashRequeuesInterruptedWork`：真实 HTTP 上传、Kill9→Restart 日志 `media: requeued interrupted work`、ready 派生元数据与原件逐字节回读 | 当前提交 |
| 2026-07-29 | EVO-027 | 音色真钱验收不再要求已删除的本机 `DASHSCOPE_*` 凭据或 BYOK Qwen 对话 key：测试改为等待生产 API Serve 的 managed `anselm` 行与默认 dialogue，再由默认 Anselm API 驱动登记工具；真实音色购买尚未重跑 | managed voice acceptance / API boundary | `TestLiveVoice_EnrollSpeakDelete` 静态门控审计通过；无 `EVALS_VOICE=1` 时仅跳过，未宣称上游 enrollment/synthesis/delete 绿 | 当前提交 |
| 2026-07-29 | EVO-028 | 本轮跨实体黑盒回归在最新 backend 上全绿：并行真实 HTTP/SSE 场景、媒体准备/崩溃恢复、workflow/handler/MCP/function 产物链均通过；真钱 EVALS 仍按显式门控未触发 | full testend regression | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 260.291s` | 当前提交 |
| 2026-07-29 | EVO-029 | 最新 API Serve 上默认 Anselm managed voice 全生命周期通过：受管 install/default 就绪后，预置朗读生成参考音频、`enroll_voice` 危险审批与异步登记、库存/上游句柄可见、克隆音色朗读产出不同音频，最后先删上游再删本地行并释放槽位 | managed voice / API Serve TTS / danger approval / inventory | `EVALS_VOICE=1 go test ./scenarios -run '^TestLiveVoice_EnrollSpeakDelete$' -count=1` → PASS 43.50s；未读取本机 provider secret，生产网关 HTTP 真实链路 | 当前提交 |
| 2026-07-29 | EVO-030 | 默认 Anselm managed 多模态读路径全链路通过：普通聊天、图片附件、MP4 附件、文档内 `anselm://media` 图片引用与免费档配额均在生产 API Serve 上完成；附件原件/回合终态/能力投影均成立，未走 BYOK fallback | managed read / image / video / document media / quota | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_(DefaultChat|DefaultChatWithImageAttachment|DefaultChatWithVideoAttachment|DocumentImageReference|Quota)$' -count=1` → PASS 60.627s；无本机 provider key | 当前提交 |
| 2026-07-29 | EVO-031 | 默认 Anselm managed `generate_image` 写路径通过：默认 dialogue 模型恰调用一次工具，receipt 标出 `provider=anselm`，真实图片附件可解码且字节可回读；回合上限为两步，避免模型重画失控 | managed write / image generation | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_GenerateImageArtifact$' -count=1` → PASS 28.11s；无本机 provider key | 当前提交 |
| 2026-07-29 | EVO-032 | 默认 Anselm managed `generate_speech` 写路径通过：默认 dialogue 模型恰调用一次工具，receipt 标出 `provider=anselm`，网关返回真实 RIFF/WAVE 音频附件并可回读；两步上限约束付费重试 | managed write / speech generation / API Serve TTS | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_GenerateSpeechArtifact$' -count=1` → PASS 12.42s；无本机 provider key | 当前提交 |
| 2026-07-29 | EVO-033 | H11 边界审计确认：`live_media_test.go` 与 `live_media_guard_test.go` 仍绑定 `EVALS_MEDIA`、DashScope 录制代理和本机 provider secret，属于历史直连线缆；当前生成实际由 Router 派发到 `anselm`，`EVALS_VOICE` 已走生产 managed。旧文件保留作历史证据，不作为产品入口 | H11 routing boundary / live acceptance governance | `rg` 审计旧测试入口与 `Router.{Image,Speech,Video}Available` / dispatch；working CURRENT/README 与文件注释已明确分层 | 当前提交 |
| 2026-07-29 | EVO-034 | 默认 Anselm managed `generate_image` → `edit_image` 写路径通过：模型先铸一张图，再以 receipt 的 attachmentId 改图；两件均为有效、不同字节的图片，edit receipt 保留 `sourceAttachmentId` 且两次 provider 均为 `anselm`，回合最多四步 | managed write / image edit / MediaRef lineage | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_EditImageArtifact$' -count=1` → PASS 78.28s；无本机 provider key | 当前提交 |
| 2026-07-29 | EVO-035 | 默认 Anselm managed `generate_video` 写路径通过：工具先停在危险审批，批准后经 API Serve 异步提交/轮询/下载，落下一件可解码 MP4；receipt 恰好一条且 provider 为 `anselm`，两步上限阻止重复付费调用 | managed write / video generation / danger approval / async gateway | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_GenerateVideoArtifact$' -count=1` → PASS 166.71s；无本机 provider key | 当前提交 |
| 2026-07-29 | EVO-036 | active `stream-llm` 参考补上 H11 生成边界：旧五家图像/三类语音/两家视频方言条只保留为 archived wire evidence；当前六个生成/克隆工具全部硬收口到 `anselm` managed，BYOK 仅读文本与多模态输入，旧 `EVALS_MEDIA` 不得作为产品入口 | H11 routing boundary / reference governance | `rg` 审计 `Router` availability/dispatch 的 `provider == "anselm"` 硬闸，并以 docs verify 验证新增边界注释 | 当前提交 |
| 2026-07-29 | EVO-037 | Qwen BYOK 多模态金标通过：真实图片、MP4、WAV 三种附件均完成对话，随后真实工具调用续接并返回结果；运行期间受管 gateway 关闭，证明目录能力、Qwen 方言与音频 `input_audio` 线缆均可达 | BYOK-read / image / video / audio / tool continuation | `EVALS=1 EVALS_QWEN_MULTIMODAL=1 EVALS_PROVIDER=qwen EVALS_KEY=<env> EVALS_BASE_URL=<catalog> go test ./golden -run '^TestGolden_C2_QwenMultimodalInputAndToolContinuation$' -count=1 -v` → PASS 206.91s；key 未写日志 | 当前提交 |
| 2026-07-29 | EVO-038 | 默认 Anselm managed 的“用户图片 + capability tools”动画融合尚未成立：首个请求在危险审批交互出现前即以 `LLM_STREAM_ERROR` / 400 终止，未执行 `animate_image`、未铸造动画产物；这与已通过的纯媒体读取和纯工具生成路径是不同边界 | managed-read/write / multimodal tool fusion / animation | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_AnimateImageArtifact$' -count=1 -v` → FAIL 100.21s；回合无 danger interaction，真实网关链路返回 400，测试故意保留为回归哨兵 | 待 API Serve/managed multimodal-tool wire reprobe |
| 2026-07-29 | EVO-039 | 动画 wire reprobe 找到两处真实断点：backend 原先把带首帧的请求误发到文生视频 `/videos/generations`，且 managed resolver 为 lease URL 清掉了 3 MiB 解码信封；修复后确认 API Serve `/videos/animations` 已真实生成 MP4，但超大产物仍会在续接时撞网关 400，证明下游媒体预算必须保留在应用层 | managed animation route / media envelope / continuation | `TestSubmitVideoAnselm_UsesAnimationRouteForFirstFrame`；文本-only managed reprobe 观察到真实动画 tool result 后续 400；无 key/ID 写入日志 | `2539114b` |
| 2026-07-29 | EVO-040 | 默认 Anselm managed 动画融合现已通过：用户图片与 capability tools 同回合、危险审批、专用动画路由、异步 MP4、receipt 来源血缘及终态续接全部成立；约 9.6 MB 的生成视频按 3 MiB 网关预算降级为注记而不再杀死回合 | managed-read/write / multimodal tool fusion / animation / media envelope | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_AnimateImageArtifact(TextOnly)?$' -count=1` → PASS（TextOnly 117.866s；融合 116.047s）；无本机 provider key | `2539114b` |
| 2026-07-29 | EVO-041 | 动画路由与 managed 媒体预算修复后的完整黑盒回归无回归：普通聊天、workflow、MCP/function/handler 产物、取消/重试/崩溃恢复、附件准备与配额资源卫生仍然全绿 | full testend regression | `make -C backend testend` → PASS 309.211s | `2539114b` |
| 2026-07-29 | EVO-042 | 混合调度的下游像素证据补齐：真实 OpenAI BYOK planner 经透明 recorder 收到与 Anselm managed 生成并落库图片完全一致的 base64 image part；同一回合仍恰好一次生成、receipt provider=anselm | hybrid / managed write → BYOK read / downstream wire | `EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_OpenAIPlansManagedImage$' -count=1` → PASS 16.059s；key 未写日志 | `6d6e98dc` |
| 2026-07-29 | EVO-043 | BYOK 文档消费边界补齐：Markdown 中的 `anselm://media` 图片引用不会停留在 system prompt 文本，真实 OpenAI 请求携带与附件库逐字节一致的 native image part；文档渲染与直接 attachment 两条入口一致 | byok-read / document media reference / downstream wire | `EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAIDocumentImageReference$' -count=1` → PASS 6.114s；key 未写日志 | `2c544e45` |
| 2026-07-29 | EVO-044 | 默认 Anselm managed workflow 生成者→观看者状态链通过：上游 agent 真实调用 `generate_image`，节点结果保留 managed receipt，生成图片附件可回读，下游独立 agent 完成；受管网关不向桌面暴露 provider wire，像素线缆仍单列为证据缺口 | managed workflow / generation → MediaRef → downstream agent | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_WorkflowGenerateImageToViewer$' -count=1` → PASS 44.204s；无本机 provider key | `356db631` |
| 2026-07-29 | EVO-045 | 动画、混合 wire、BYOK 文档引用与 managed workflow 哨兵落地后，backend 全包单测/集成测试仍全绿；未发现 resolver、attachment、loop、workflow、provider 或 transport 回归 | full backend package regression | `go test ./...`（backend）→ PASS；含 173 provider catalog 投影与全部内部 app/infra/store/transport 包 | 当前提交 |
| 2026-07-29 | EVO-046 | 同一轮 API Serve 网关仓库也全包通过；最新 managed TTS 句柄解析、WAV 双工载荷、媒体主机部署与视频/聊天/lease/quota 路由均保持绿色，backend live 结论所依赖的网关基线没有隐藏红灯 | Anselm API Serve / managed gateway baseline | `go test ./...`（`Anselm-API-Serve`）→ PASS；未改动网关仓库 | 网关 `63f402f` |
| 2026-07-29 | EVO-047 | BYOK 读侧三入口合跑通过：OpenAI 直接图片、OpenAI 文档 `anselm://media` 图片 exact-byte wire、Qwen MP4 视频均完成真实 key 创建/探针/能力投影/对话；受管 fallback 仍关闭 | byok-read / image / document media / video | `EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_(OpenAIImageInput|OpenAIDocumentImageReference|QwenVideoInput)$' -count=1` → PASS 17.681s；key 未写日志 | 当前提交 |
| 2026-07-29 | EVO-048 | 共享并发面无竞态：loop、chat、workflow、attachment 与 provider 关键包在 race detector 下全绿，动画/lease 修复未引入数据竞争 | concurrency / loop / media / provider | `go test -race ./internal/app/loop ./internal/app/chat ./internal/app/workflow ./internal/app/attachment ./internal/infra/llm` → PASS | 当前提交 |
| 2026-07-29 | EVO-049 | subagent 多模态产地补齐：真实受管 general-purpose 子运行可调用 `generate_image` 恰好一次，媒体 receipt 回到父对话，provider=anselm，图片附件可回读；父层 3 步预算明确覆盖“委派→收果→收尾” | managed subagent / capability tool / MediaRef | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_SubagentGenerateImageArtifact$' -count=1` → PASS 50.043s；无本机 provider key | `be6078a1` |
| 2026-07-29 | EVO-050 | 当前工作树通过全仓门禁：backend、frontend、docs 与 web demo 并行验证均绿色；本轮多模态、网关边界与文档同步没有引入跨层回归 | workspace verification / backend evolution gate | `make verify` → PASS（backend + frontend + docs + demo） | 当前提交 |
| 2026-07-29 | EVO-051 | 受管语音输入补齐真实会话证据：本地 sidecar 通过 workspace 认证与 device-proof 接通部署 API Serve 的 realtime ASR，发送 100ms PCM 帧并收到 `session.finished`；测试只证明握手、转发与生命周期，不把静音帧的文案当转写语义 | managed speech input / realtime ASR / WebSocket | `EVALS_VOICE=1 go test ./scenarios -run '^TestLiveVoice_SpeechInputASR$' -count=1` → PASS 5.31s；无本机 provider key | 当前提交 |
| 2026-07-29 | EVO-052 | 真模型黄金套件默认入口收回产品主路径：workspace 异步开通 managed `anselm` / `anselm-auto` 后，J1–J12 与 C2 全组通过；J10 使用规范 hyphen skill slug，J12b 在受管 1M 窗口上稳定跨越持久 compaction；显式 DeepSeek 仍可作为 BYOK 对照 | golden routing / managed default / compaction fixture | 默认 `EVALS=1 go test -count=1 -timeout 30m ./golden` → PASS 616.060s；`EVALS=1 EVALS_PROVIDER=deepseek go test -run '^TestGolden_J4_WorkflowToParked$'` → PASS 39.24s；key 未写日志 | 当前提交 |
| 2026-07-29 | EVO-053 | 默认 Anselm managed 朗读闭环通过：第一次真实 TTS 生成可播放 WAV，第二次同文本同音色返回同一附件且网关 monthly quota 不增加，换文本得到新附件；未把历史 DashScope recorder 当作当前产品入口 | managed-write / read-aloud cache / quota | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_ReadAloudCache$' -count=1 -timeout 8m` → PASS 11.01s；无本机 provider key；provider-side wire count 仍需 API Serve 侧 recorder/账本证据 | 当前提交 |
| 2026-07-29 | EVO-054 | Qwen-MT 真实行为边界补齐：`qwen-mt-plus` 不接受 `system` role 或 `tools`，且 SSE `delta.content` 重发累计全文；目录已有 `tools:false` 但此前仍把它当普通 agent/chat route。现在 chat-only 路线去掉工具目录、把系统约束折入首个 user，累计文本按 Qwen-MT 模型族去重；agent 在解析阶段返回 `MODEL_NOT_AGENT_CAPABLE` | BYOK chat-only / compat wire / model capability | 真实上游 probe：`Function call not supported`、`Role must be in [user, assistant]`、累计 `Sure`→`Sure!`→全文；`EVALS=1 EVALS_PROVIDER=qwen EVALS_MODEL=qwen-mt-plus go test ./golden -run '^TestGolden_J1_Bootstrap$' -count=1 -timeout 3m -v` → PASS 6.24s；`go test ./internal/infra/llm ./internal/bootstrap ./internal/app/chat ./internal/domain/model` → PASS | 当前工作树 |
| 2026-07-29 | EVO-055 | provider 行为类抽样补齐：DeepSeek 真实 `/models` 当前只返回 `deepseek-v4-flash`/`deepseek-v4-pro`，旧 `deepseek-chat` 不应作为当前验收选择；Google 原生 `/models` 仍列出旧 2.5 名称但对新用户的 `generateContent` 返回 404，当前 `gemini-3-flash-preview` 真实可用。两者都经产品 BYOK 路由完成文本回合；Kimi 凭证在 `moonshot.ai`、`moonshot.cn` 与 Kimi Coding 兼容入口均返回 401，不能误判为后端 wire 通过 | BYOK-read / DeepSeek compat / Google native / Kimi credential boundary | `EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_TextProviderSmoke$' -count=1 -timeout 8m -v` → PASS（DeepSeek 4.70s；Google 6.36s）；官方入口状态 probe：DeepSeek/OpenAI/Qwen/Google 200，Kimi 401；key 未写日志 | 当前工作树 |
| 2026-07-29 | EVO-056 | Google 原生 Gemini 多模态读路径通过：真实探针后的 `gemini-3-flash-preview` 能力含视觉，产品上传的 PNG 经 contents/parts 原生线缆完成 BYOK 对话，附件原件逐字节仍可回读；这补上了 OpenAI-compatible 之外的视觉方言样本 | BYOK-read / Google native / image input | `EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_GoogleImageInput$' -count=1 -timeout 5m -v` → PASS 14.60s；managed gateway 保持关闭，未走受管 fallback，key 未写日志 | 当前工作树 |
| 2026-07-29 | EVO-057 | Qwen-MT 的 chat-only 能力边界在产品 agent 入口真实落地：workspace 可选中 `qwen-mt-plus` 作为 agent 默认，但 invoke 在产生任何步骤或上游调用前返回 `ok=false/status=failed/steps=0` 与可理解的 capability 错误；同一模型仍可走 chat | BYOK-read / agent capability boundary / UX error classification | `EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_QwenChatOnlyAgentRejected$' -count=1 -timeout 5m -v` → PASS 2.87s；Qwen probe 成功，managed gateway 关闭，key 未写日志 | 当前工作树 |
| 2026-07-29 | EVO-058 | Google 原生 Gemini 的 `ListModels` 不提供 function-calling 能力位，不能据此把 `gemini-3-flash-preview` 误标成 chat-only；同时 Gemini 3 工具续调用要求把响应部件上的 `thoughtSignature` 原样回传到下一请求的同一 `functionCall` 部件。产品现已从目录补回 tools 事实，并贯通签名的流事件、工具块属性、历史投影与原生 contents/parts 线缆 | BYOK-read / Google native / tool continuation / model capability | `go test ./internal/infra/llm ./internal/app/loop` → PASS；`EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_GoogleToolContinuation$' -count=1 -timeout 8m -v` → PASS 12.48s；录制真实 Google 请求确认 functionCall、functionResponse 与 thoughtSignature 均穿过，key 未写日志 | 当前工作树 |
| 2026-07-29 | EVO-059 | Google 当前账号的旧 `gemini-2.5-flash` 仍出现在 `ListModels`，但真实 generate 返回 404；产品允许用户保存这个明确选择，首发只发一次、不做错误重试，回合落为 error 且不伪造 assistant 文本，错误消息保留安全的 `model not found (404)` 事实。当前回合级 `errorCode` 仍是通用 `LLM_STREAM_ERROR`，这是后续前端错误分类/模型失效记忆的候选改进，而不是把 404 当可重试故障 | BYOK-read / model eligibility drift / error UX | `EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_GoogleListedModelCanBeAccountUnavailable$' -count=1 -timeout 8m -v` → PASS 4.85s；真实 `/models` 列表、产品选择、单次 generate、durable error turn 均核对；key 未写日志 | 当前工作树 |
| 2026-07-29 | EVO-060 | OpenAI-compatible 的 `input_audio` 编码器此前把 OpenAI 家误判为不支持音频，导致目录即使声明音频也会在 part 层拒绝；现在 OpenAI 方言与能力掩码已覆盖 WAV/MP3 `input_audio`。本账号当前真实 `/models` 已出现 `gpt-audio` 系列，但跟随的 OpenAI 目录快照尚未把这些新 id 归入 `openai` provider，产品级音频探针因此诚实跳过，待目录刷新后重跑真钱线缆 | BYOK-read / OpenAI-compatible / audio input / catalog drift | `go test ./internal/infra/llm` → PASS（wire mask 与 encoder 同步守卫）；`EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAIAudioInput$' -count=1 -timeout 8m -v` → SKIP（当前账号列出但产品能力投影缺少 `gpt-audio`）；未写入 key | 当前工作树 |
| 2026-07-29 | EVO-061 | 一次受控的 models.dev 目录刷新把已由真实 Qwen 线缆验收证明支持 MP4 的 `qwen3.7-plus` 改成 `video` 缺席，并同时漂移多个已覆盖模型的窗口/推理档位；该刷新未落地，说明目录更新必须与近期真实证据对照，不能让静态供应商快照反向撤销产品已证能力 | model catalog governance / Qwen video / regression safety | `make update-model-catalog` → 173 providers / 5593 models；审查发现 `hyper/qwen3.7-plus` `text,image,video → text,image`；恢复原快照，未污染工作树；既有 `EVO-012` Qwen MP4 真实通过作为冲突证据 | 当前工作树 |
| 2026-07-29 | EVO-062 | provider 404 的模型资格漂移现在保留为稳定回合级 `LLM_MODEL_NOT_FOUND`，不再与普通 `LLM_STREAM_ERROR` 混淆；前端失败横幅对该码提供重选模型入口，用户无需猜测「模型仍在目录里为何不能用」 | BYOK-read / model eligibility drift / error UX | `TestRun_StreamError_PreservesModelNotFoundCode`；前端 `LLM_MODEL_NOT_FOUND` banner widget guard；既有 `TestLiveBYOK_GoogleListedModelCanBeAccountUnavailable` 真实 404 单次调用与无 assistant 文本证据 | 当前工作树 |
| 2026-07-29 | EVO-063 | 托管 Anselm workflow 的“生成者→BYOK 观看者”真实闭环通过：`anselm-auto` 先调用 `generate_image`，节点结果保留 `provider=anselm` receipt 与真实 PNG；下游 OpenAI `gpt-4.1-mini` 经 BYOK recorder 收到与附件逐字节一致的原生 image part。过程中发现并修复 `anselm-auto` 静态规格漏报 `tools=true`，否则 agent/workflow 会在 provider 前被资格闸拒绝 | managed workflow / hybrid downstream wire / model capability catalog | `go test ./internal/infra/llm ./internal/bootstrap ./internal/app/agent`；`EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedImageToOpenAIViewer$' -count=1 -timeout 8m -v` → PASS 31.31s；key 未写日志 | 当前提交 |
| 2026-07-29 | EVO-064 | 同一 workflow ownership seam 的 Google 原生变体也通过：managed `generate_image` 的真实 PNG 经节点 `MediaRef` 交给 BYOK `gemini-3-flash-preview`，recorder 在真实 `streamGenerateContent` 请求中捕获与附件逐字节一致的 `inlineData`；因此下游像素证据不依赖 OpenAI-compatible 方言 | hybrid workflow / Google native / downstream media wire | `EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedImageToGoogleViewer$' -count=1 -timeout 8m -v` → PASS 38.93s；key 未写日志 | 当前提交 |
| 2026-07-29 | EVO-065 | workflow 的音频分支也完成真实闭环：managed `generate_speech` 产出可解码 WAV 与 receipt，节点 `MediaRef` 交给 BYOK Qwen `qwen3-omni-flash`，recorder 在真实 `/chat/completions` 请求中捕获与附件逐字节一致的 native `input_audio`；多模态融合不再只覆盖图像路径 | hybrid workflow / managed speech / Qwen native-compatible audio wire | `EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedSpeechToQwenViewer$' -count=1 -timeout 8m -v` → PASS 14.05s；key 未写日志 | 当前提交 |
| 2026-07-29 | EVO-066 | OpenAI 当前 `/models` 已暴露 `gpt-audio` 家族，但 active models.dev OpenAI 投影仍缺这些 id；若只信目录，产品会在 capability 层跳过一条真实音频路径。provider-owned bridge 临时补上官方 128K/16K、text+audio、tools 能力；真实产品验收使用供应商可接受的两秒 WAV，上传原件、回合终态与上游 `input_audio` exact-byte wire 全部通过。零成本测试继续保留更小的 `MockWAV`，避免把供应商时长约束混入通用产物 fixture | BYOK-read / OpenAI gpt-audio / catalog drift / audio input | `go test ./internal/infra/llm ./internal/bootstrap ./internal/app/model` → PASS；`EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAIAudioInput$' -count=1 -timeout 8m -v` → PASS 6.27s；key 未写日志 | 当前工作树 |
| 2026-07-29 | EVO-067 | `gpt-audio` 的音频能力与 agent 能力可在同一产品回合共存：WAV 作为原生 `input_audio` 进入首发，真实 `run_function` 执行后结果回喂第二次请求，最终文本与工具/音频 wire 均保真；验收显式等待 function sandbox ready，并按产品的 `run_function(functionId,args)` 契约驱动，而不是假设 function 名会成为 LLM 工具名 | BYOK-read / OpenAI gpt-audio / audio + tools continuation | `EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAIAudioToolContinuation$' -count=1 -timeout 8m -v` → PASS 8.10s；recorder 见两次 `/chat/completions`、`input_audio` 原始字节与 `tools`；key 未写日志 | 当前工作树 |
| 2026-07-29 | EVO-068 | provider 资格漂移的用户恢复路径也成立：Google 旧模型虽仍在目录中但首发真实 404，回合落为 `LLM_MODEL_NOT_FOUND` 且不重试；用户把同一 workspace 的 dialogue 切到当前可用模型后，原会话可以继续完成，不被失败回合或旧选择污染 | BYOK-read / model eligibility drift / recovery UX | `EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_GoogleListedModelCanBeAccountUnavailable$' -count=1 -timeout 8m -v` → PASS 6.54s；recorder 只见一次失败模型调用 + 一次恢复模型调用，恢复回合含 assistant text，key 未写日志 | 当前工作树 |
| 2026-07-29 | EVO-069 | subagent 失败不再伪装成成功答案，也不会把父回合一起打死：子模型一次 500 后，子回合落 durable `error`，工具结果带“非权威/partial”标记，父模型仍能继续完成；这同时验证了失败 trace 与父历史的分层 | subagent / chat loop / failure recovery | `go test ./scenarios -run '^TestChatR3_SubagentFailureFeedsParent$' -count=1 -timeout 2m -v` → PASS 3.95s；真实 HTTP/持久化消息 + mock provider 线缆确认三请求顺序、错误注记、父续接与子 error 行 | 当前工作树 |
| 2026-07-29 | EVO-070 | 真实 webhook 连续双触发把 workflow overlap 的五种用户可见结果走通：serial 留一条 pending 并在首回合审批后继续；skip 将第二条落为 skipped；buffer_one 留最新 pending 并在首回合后继续；replace 取消旧在途 run、让新 run 接管；allow_all 保留两条 started 且两条 run 同时 parked。测试使用不同 payload，避免把 webhook 同 body/分钟桶的网络重试去重误判成并发策略 | workflow / trigger / scheduler / concurrency / recovery | `go test ./scenarios -run '^TestWorkflow_RealTriggerConcurrencyPolicies$' -count=1 -timeout 4m` → PASS 53.414s；真实 HTTP webhook、firing 台账、flowrun 状态和公开 approval decide 路径全链路；key 未写日志 | 当前工作树 |
| 2026-07-29 | EVO-071 | 旧矩阵里只有 scheduler unit probe 的大图路径补上产品黑盒：12 个 durable 节点、8 路 trigger fan-out、两组四入边 AND-join 和最终合流均经真实 HTTP flowrun 完成；每个 `(node, iteration)` 恰一行，所有 branch 结果以确定顺序抵达终点，未出现提前 join 或静默丢分支 | workflow / durable graph / fan-out / AND-join | `go test ./scenarios -run '^TestWorkflow_LargeParallelFanoutAndJoin$' -count=1 -timeout 2m -v` → PASS 7.95s；节点行、iteration=0、最终 complete 值均断言 | 当前工作树 |
| 2026-07-29 | EVO-072 | 旧体系里最容易把树弄串的双委派并发路径补上：同一 execution group 的两个 general-purpose subagent 真并行运行，两个结果同时回喂父回合；各自 sub-message 以唯一 `parentBlockId` 锚到不同的父 tool_call，答案集合与父续接均保真，子请求不泄漏父历史 | subagent / chat loop / E3 nesting / concurrency | `go test ./scenarios -run '^TestChatR3_ConcurrentSubagentsKeepTheirOwnTrees$' -count=1 -timeout 2m -v` → PASS；`go test -race ./scenarios -run '^TestChatR3_ConcurrentSubagentsKeepTheirOwnTrees$' -count=1 -timeout 3m` → PASS；真实 HTTP、durable messages、mock provider PromptDump 四请求证据 | 当前工作树 |
| 2026-07-29 | EVO-073 | 旧体系里仅有 scheduler unit probe 的深循环补成产品黑盒：workflow 真实跑 25 次 action/control 迭代，`has(previous) ? previous : seed` 的 scopeFor 累加到 25；每个 `(node, iteration)` 只落一行，公开 flowrun 节点分页可完整走完，function execution ledger 的 `flowrunIteration` 与节点行逐轮对应 | workflow / durable graph / deep loop / pagination / execution provenance | `go test ./scenarios -run '^TestWorkflow_DeepLoopPersistsEveryIteration$' -count=1 -timeout 3m` → PASS 6.96s；`go test -race ./scenarios -run '^TestWorkflow_DeepLoopPersistsEveryIteration$' -count=1 -timeout 4m` → PASS 8.09s；52 条 durable 节点行、6 页 REST、25 条 function execution 证据 | 当前工作树 |
| 2026-07-29 | EVO-074 | 旧体系里只有 scheduler unit probe 的 failed replay 补成真实 HTTP 恢复验收：首轮驻留 handler 第一次失败并落 durable failed，`:replay` 只清失败节点、复用已完成 function，让同一 handler 第二次成功并继续 finish；两次 handler call 与 function executions 均保留原 `(flowrun,node,iteration)` 溯源，completed run 的二次 replay 明确拒绝 | workflow / durable replay / handler recovery / execution provenance | `go test ./scenarios -run '^TestWorkflow_FailedReplayReusesCompletedNodes$' -count=1 -timeout 2m -v` → PASS；`go test -race ./scenarios -run '^TestWorkflow_FailedReplayReusesCompletedNodes$' -count=1 -timeout 3m` → PASS；首轮 failed、replay_count=1、终值 2、stable function 恰 1 次、handler 恰 failed+ok 两次 | 当前工作树 |
| 2026-07-29 | EVO-075 | workflow failed replay 的版本边界经真实 HTTP 钉死：function v1 失败后编辑 active 到可用 v2，原 failed run 的 `:replay` 仍按起跑时 pin 重跑 v1 并继续失败；新建 fresh run 才执行 v2 成功。执行台账以 `versionId` 与 `(flowrun,node,iteration)` 证明没有隐式换版本 | workflow / durable replay / function version pin / recovery UX | `go test ./scenarios -run '^TestWorkflow_ReplayKeepsOriginalFunctionPin$' -count=1 -timeout 2m -v` → PASS；`go test -race ./scenarios -run '^TestWorkflow_ReplayKeepsOriginalFunctionPin$' -count=1 -timeout 3m` → PASS；replay 两条 execution 均为旧 version/failed，fresh run 一条新 version/ok | 当前工作树 |
| 2026-07-29 | EVO-076 | workflow 的 SIGKILL 恢复从“最终完成”加深到跨实体审计真相：function 在途时杀进程，boot Recover 重新执行后同一 run 只保留 start/slow 两条唯一 completed 节点行，recovered function execution 恰一条且带 `flowrunId=run、flowrunNodeId=slow、flowrunIteration=0`；没有孤儿或隐式 replay 计数 | workflow / crash recovery / durable node truth / function execution provenance | `go test ./scenarios -run '^TestWorkflow_CrashRecovery$' -count=1 -timeout 2m -v` → PASS 13.88s；`go test -race ./scenarios -run '^TestWorkflow_CrashRecovery$' -count=1 -timeout 3m` → PASS 16.119s；同一 data dir Kill9→Restart 后公开 REST 与 execution ledger 双侧断言 | 当前工作树 |
| 2026-07-29 | EVO-077 | 审批停泊的恢复路径补齐：SIGKILL 时 run 保持 running，boot 后 flowrun inbox 恰保留一条可决策的 parked 行；重启后 yes 决定续跑到 publish，最终 3 个节点各一行，function execution 仍挂回原 `(flowrun,node,iteration)`，没有误取消或重复 inbox | workflow / approval / crash recovery / human-in-the-loop | `go test ./scenarios -run '^TestWorkflow_ParkedApprovalSurvivesRestartAndDecide$' -count=1 -timeout 2m -v` → PASS 6.30s；`go test -race ./scenarios -run '^TestWorkflow_ParkedApprovalSurvivesRestartAndDecide$' -count=1 -timeout 3m` → PASS 7.239s；同一 data dir Kill9→Restart→公开 inbox/decide/flowrun/execution 证据闭合 | 当前工作树 |
| 2026-07-29 | EVO-078 | 外部 webhook 的 firing→flowrun 恢复闭合：HTTP 202 后在 scheduler drain 前/中 SIGKILL，重启后 firing 台账只保留一条 `started`，恰关联一条 `origin=webhook` 的 completed flowrun（`triggerId`/`firingId`/`flowrunId` 全相等），下游 function execution 也只落一条，证明没有丢火或重复跑 | trigger / webhook / scheduler / crash recovery / provenance | `go test ./scenarios -run '^TestTrigger_WebhookFiringSurvivesRestartBeforeDrain$' -count=1 -timeout 2m -v` → PASS 10.81s；`go test -race ./scenarios -run '^TestTrigger_WebhookFiringSurvivesRestartBeforeDrain$' -count=1 -timeout 3m` → PASS 12.232s；真实无 workspace-header webhook、同 data dir Kill9→Restart、公开 firings/flowruns/function executions 互链 | 当前工作树 |
| 2026-07-29 | EVO-079 | webhook 网络重试的幂等边界补回产品黑盒：同一分钟相同 raw body 的两次 202 只落一条 started firing 与一条 completed flowrun；换 body 才落第二条，两个 run 都标 `origin=webhook` 且各自完成，避免把重试误报成 workflow overlap | trigger / webhook / idempotency / scheduler | `go test ./scenarios -run '^TestTrigger_WebhookDuplicateBodyDedupsWithinMinute$' -count=1 -timeout 2m -v` → PASS 13.72s；`go test -race ./scenarios -run '^TestTrigger_WebhookDuplicateBodyDedupsWithinMinute$' -count=1 -timeout 3m` → PASS 15.059s；真实无 workspace-header webhook、相同/不同 body 对照、公开 firings/flowruns 数量与状态断言 | 当前工作树 |
| 2026-07-29 | EVO-080 | fsnotify 旧体系的真实黑盒补齐：监听目录 + `*.txt`/create 过滤后，真实文件创建只生成一条 fsnotify firing，canonical `eventKind=create` 与绝对 path 经 workflow CEL 接线抵达 sink；run origin、firing、function execution 均互链，未把 listener 单元字符串当成产品证据 | trigger / fsnotify / workflow payload / provenance | `go test ./scenarios -run '^TestTrigger_FsnotifyEventReachesWorkflow$' -count=1 -timeout 2m -v` → PASS 8.62s；`go test -race ./scenarios -run '^TestTrigger_FsnotifyEventReachesWorkflow$' -count=1 -timeout 3m` → PASS 9.866s；真实临时目录、文件事件、公开 flowrun/firing/execution 断言 | 当前工作树 |
| 2026-07-29 | EVO-081 | sensor 负→正转移补成真实产品路径：probe 返回 level=1 时 activation 明确记 `condition evaluated false`、保留 ReturnValue 且不产生 firing；编辑目标 function 到 level=42 后下一轮 probe 记 Payload 并触发 `origin=sensor` workflow，随后暂停 source，避免无限重复轮询 | trigger / sensor / activation audit / live edit | `go test ./scenarios -run '^TestTrigger_SensorFalseProbeThenFiresAfterEdit$' -count=1 -timeout 2m -v` → PASS 12.93s；`go test -race ./scenarios -run '^TestTrigger_SensorFalseProbeThenFiresAfterEdit$' -count=1 -timeout 3m` → PASS 14.975s；真实 target function edit、activation false/true、firing/run 状态与 pause 断言 | 当前工作树 |
| 2026-07-29 | EVO-082 | fsnotify 的暂停不是只改 UI 投影：暂停后真实文件创建在硬重启前后都不产生 activation/firing/run，重启仍保持 `paused=true/listening=false`；resume 用原 workflow 引用重挂 watcher，只有新文件触发一条 `origin=fsnotify` run，firing 与 function execution provenance 互链 | trigger / fsnotify / pause / crash recovery / provenance | `go test ./scenarios -run '^TestTrigger_FsnotifyPauseRestartResume$' -count=1 -timeout 8m -v` → PASS 15.29s；`go test -race ./scenarios -run '^TestTrigger_FsnotifyPauseRestartResume$' -count=1 -timeout 8m -v` → PASS 13.19s；真实临时目录、暂停期双负控、SIGKILL→Restart、resume 后新文件与公开审计证据 |
| 2026-07-29 | EVO-083 | sensor 的暂停在硬重启后仍真正停止轮询：暂停期间编辑目标 function 到 level=42，跨越一个完整 5 秒 cadence 仍没有新 activation/firing/run；resume 重新注册当前配置，立即观察新版本并完成 `origin=sensor` workflow，随后再暂停收口 | trigger / sensor / pause / crash recovery / live edit | `go test ./scenarios -run '^TestTrigger_SensorPauseRestartResume$' -count=1 -timeout 8m -v` → PASS 18.82s；`go test -race ./scenarios -run '^TestTrigger_SensorPauseRestartResume$' -count=1 -timeout 8m -v` → PASS 17.64s；真实 target edit、6.5 秒负控、SIGKILL→Restart、resume 后 activation payload/firing/run 证据 |
| 2026-07-29 | EVO-084 | sensor 的 handler target 也走真实产品边界：驻留方法第一次抛出 `sensor probe boom` 时 activation 明确为 `invoke failed`、`firingCount=0`，错误不被静默吞；下一次 cadence 同一 handler 返回 level=42，sensor-origin workflow 完成，handler calls 台账保留 failed 与 ok 两次尝试 | trigger / sensor / handler / failure audit | `go test ./scenarios -run '^TestTrigger_SensorHandlerTargetAndInvokeFailure$' -count=1 -timeout 8m -v` → PASS 13.66s；`go test -race ./scenarios -run '^TestTrigger_SensorHandlerTargetAndInvokeFailure$' -count=1 -timeout 8m -v` → PASS 12.91s；真实 resident handler、公开 activations/flowruns、handler calls failed+ok 对照 |
| 2026-07-29 | EVO-085 | fsnotify 的 pattern 与 event filter 在 durable report 前生效：`*.log` 创建和已有 `.txt` 的 modify 都不产生 activation/firing/run；唯一匹配的 `.txt` create 产生一条 `eventKind=create` activation、一条 started firing 和一条 completed fsnotify run，未被重复放大 | trigger / fsnotify / filter / idempotency | `go test ./scenarios -run '^TestTrigger_FsnotifyFiltersCreateEvents$' -count=1 -timeout 8m -v` → PASS 9.10s；`go test -race ./scenarios -run '^TestTrigger_FsnotifyFiltersCreateEvents$' -count=1 -timeout 8m -v` → PASS 9.18s；真实临时目录、扩展名/事件负控、公开 activation/firing/flowrun 数量与 payload |
| 2026-07-29 | EVO-086 | sensor 的 MCP target 走通真实 stdio server：`targetId/method=echo` 被解析到 mcp server，文本结果 `echo:` 归一进 `payload.text` 并满足 CEL，sensor-origin workflow 完成；公开 MCP calls 台账保留 `status=ok` 且 `triggeredBy=workflow`，没有把 sensor 调用伪装成手工调用 | trigger / sensor / mcp / execution attribution | `go test ./scenarios -run '^TestTrigger_SensorMCPTargetReachesWorkflow$' -count=1 -timeout 8m -v` → PASS 8.56s；`go test -race ./scenarios -run '^TestTrigger_SensorMCPTargetReachesWorkflow$' -count=1 -timeout 8m -v` → PASS 7.93s；真实 Python JSON-RPC MCP server、activation payload、flowrun origin 与 MCP ledger 互证 |
| 2026-07-29 | EVO-087 | active fsnotify trigger 的目录编辑是真正 hot-swap：baseline 旧目录 create 完成后，PATCH 改到新目录会摘掉旧 watcher；旧目录 stale create 不新增 run，新目录 create 恰新增第二条 `origin=fsnotify` run，两个 activation 的绝对 path 与两条 firing 一一对应 | trigger / fsnotify / edit / attach-detach / provenance | `go test ./scenarios -run '^TestTrigger_FsnotifyEditHotSwapsWatch$' -count=1 -timeout 8m -v` → PASS 13.43s；`go test -race ./scenarios -run '^TestTrigger_FsnotifyEditHotSwapsWatch$' -count=1 -timeout 8m -v` → PASS 12.96s；真实双临时目录、active PATCH、旧/新目录对照、公开 activation/firing/flowrun 证据 |
| 2026-07-29 | EVO-088 | sensor 配置错误在 create 边界就可解释地拒绝：坏 condition/坏 output 均返回 422 `TRIGGER_INVALID_CEL` 并在 error details 指出具体 field，悬空 function target 返回 `TRIGGER_SENSOR_TARGET_NOT_FOUND`；三种坏定义都不落入 trigger 列表，不会等到第一次轮询才失败 | trigger / sensor / authoring validation / error UX | `go test ./scenarios -run '^TestTrigger_SensorValidationFailsEarly$' -count=1 -timeout 8m -v` → PASS 5.50s；`go test -race ./scenarios -run '^TestTrigger_SensorValidationFailsEarly$' -count=1 -timeout 8m -v` → PASS 5.32s；真实 HTTP 422、error details field、列表无残留与 eager target lookup 证据 |

| 2026-07-29 | EVO-089 | 多入口 workflow 的 listener 生命周期经真实 HTTP 闭环：图中两个不同 webhook ref 都被挂载；重复 trigger 节点按 entity ref 去重、重复 `:activate` 保持 refcount=1；A/B 两条入口各自产生一条 completed flowrun；`:deactivate` 后两条路径都摘干净并回到 404 | workflow / trigger / attach-detach / multi-entry / idempotency | `go test ./scenarios -run '^TestContractWorkflow_MultiTriggerAttachDetachAndDedup$' -count=1 -timeout 2m -v` → PASS 14.50s；`go test -race ./scenarios -run '^TestContractWorkflow_MultiTriggerAttachDetachAndDedup$' -count=1 -timeout 3m -v` → PASS 13.13s；真实双 webhook、公开 trigger `refCount/listening` 投影、重复激活、flowrun 完成数与停用后双 404 证据 |

| 2026-07-29 | EVO-090 | 真实多入口 `:stage` 暴露并修复两个 one-shot 语义缺陷：第一火此前只摘当前 source、第二入口仍可再跑；两个 source report 并发时 stale snapshot 还可铸造第二条 firing。现在 trigger 共同层先原子 claim staged workflow，再跨所有 entry 撤防并过滤失效快照；第一火恰一条 run，后续入口 404/0 firing | trigger / workflow / stage / multi-entry / concurrency / idempotency | 先行红测 `TestContractWorkflow_StageMultiTriggerDisarmsAllEntries`（旧逻辑 5s 内 refCount B 仍为 1）；先行红测 `TestAttachOnce_ConcurrentReportsConsumeSingleBudget`（旧逻辑 got 2 firings）；修复后 `go test ./internal/app/trigger -run 'TestAttachOnce_(AutoDisarmsAfterFire|SoleListenerStopsAfterFire|MultiTriggerWorkflowDisarmsEveryEntry|ConcurrentReportsConsumeSingleBudget)$' -count=1` → PASS；`go test -race ./internal/app/trigger -run 'TestAttachOnce_(AutoDisarmsAfterFire|SoleListenerStopsAfterFire|MultiTriggerWorkflowDisarmsEveryEntry|ConcurrentReportsConsumeSingleBudget)$' -count=1` → PASS 2.165s；`go test ./scenarios -run '^TestContractWorkflow_(StageOneShotAndKill|MultiTriggerAttachDetachAndDedup|StageMultiTriggerDisarmsAllEntries)$' -count=1 -timeout 4m` → PASS 16.166s；`go test -race ./scenarios -run '^TestContractWorkflow_(StageOneShotAndKill|MultiTriggerAttachDetachAndDedup|StageMultiTriggerDisarmsAllEntries)$' -count=1 -timeout 6m` → PASS 14.931s；真实 HTTP/trigger runtime projection/firing ledger 与受控持久层重叠证据 |
| 2026-07-29 | EVO-091 | active workflow 引用的 webhook 被软删后，workflow 行/历史保留且 capability-check 诚实报告 dangling ref；同名 trigger 重建拿新 id、旧图不会隐式换绑且新入口保持冷；显式 `:edit` 改 ref 会立即生成新版本、重挂 active listener，能力报告恢复干净，替换入口可完成第二次 run，随后 deactivate 全量摘除 | trigger / workflow / delete / rebind / capability / lifecycle | `go test ./scenarios -run '^TestContractWorkflow_DeletedTriggerRebindsActiveWorkflow$' -count=1 -timeout 4m` → PASS 13.858s；真实 DELETE/GET/能力检查、同名重建 fresh id、旧/新 webhook 404→202、trigger `refCount/listening`、两次 completed flowrun 与 deactivate 后 refcount=0；测试对第二次省略 `problems` 字段使用 fresh decode，避免旧 slice 残留假红 | 当前工作树（待提交） |
| 2026-07-29 | EVO-092 | active workflow 软删此前只删主行、不停内存 listener：真实 DELETE 后 trigger 仍 `refCount=1/listening=true`，webhook 继续向幽灵 workflow 扇出。现在 Delete 先摘 active/staged 所有入口、再经 runner 取消全部在途 run，最后软删并保留 activation/firing、flowrun 与 immutable version 历史；同名重建只挂一个新 listener，已排队 firing 由 scheduler shed | workflow / trigger / delete / cancellation / history / lifecycle | 先行红测 `TestContractWorkflow_DeletedActiveWorkflowDetachesTriggerAndKeepsLogs`（旧逻辑 delete 后 refcount=1）；`TestContractWorkflow_DeleteCancelsInFlightRunAndKeepsAudit` 钉 parked approval→cancelled；修复后 `go test ./internal/app/workflow -count=1` → PASS 0.936s，`go test ./internal/app/scheduler -run '^TestFiring_DeletedWorkflowSheds$' -count=1` → PASS 0.986s，5 场景组合 normal → PASS 17.629s、race → PASS 16.163s；HTTP 还确认 deleted workflow 的 `/versions` 仍返不可变历史 | `3de20519` |
| 2026-07-29 | EVO-093 | 删除边界的第二层经真实 HTTP 钉死：workflow 软删后 `/versions` 与单版本历史仍可读，但 PATCH、`:edit/:revert`、`:trigger/:stage/:activate/:deactivate/:kill`、`:capability-check`、`:iterate` 及二次 DELETE 全部稳定返 `WORKFLOW_NOT_FOUND`，不会复活 listener、版本指针或幻影 AI 对话；跨 workspace 对同一 workflow/trigger/id 的读、动作、删除与 flowrun 投影均不越界 | workflow / delete / action surface / workspace isolation / history | `go test ./scenarios -run '^TestContractWorkflow_(DeletedWorkflowRejectsMutationActions|WorkspaceIsolationAcrossActionsAndHistory)$' -count=1 -timeout 4m -v` → PASS 5.848s；真实 HTTP 版本保留、动作矩阵、`:iterate` mention 校验、双 workspace 正控/负控与 owner history 断言 | 当前工作树 |
| 2026-07-29 | EVO-094 | 删除前已接受的第二条 webhook firing 在 serial overlap 下真实停留 pending；删除 workflow 后第一条 parked run 被取消，下一次后台 drain 将第二条终结为 `shed`，不生成幽灵 flowrun；两条 firing 审计仍保留，trigger listener refcount=0 | workflow / trigger / delete / queued firing / shed / audit | `go test ./scenarios -run '^TestContractWorkflow_DeletedWorkflowShedsQueuedFiring$' -count=1 -timeout 4m -v` → PASS 14.744s；真实 webhook→durable pending、公开 firing status 转移、cancelled first run、无 second run、listener projection 断言 | 当前工作树 |
| 2026-07-29 | EVO-095 | active workflow 引用的 trigger 软删后，硬重启只重放 active workflow 记录并诚实跳过 dangling ref：旧 webhook path 仍 404、workflow 保持 active 但 `:activate` 以 `WORKFLOW_NOT_RUNNABLE` 要求修复，同名新 trigger 保持冷；显式 edit ref 后才重挂新 listener，能力检查恢复干净并完成新 run | workflow / trigger / delete / restart / explicit repair / lifecycle | `go test ./scenarios -run '^TestContractWorkflow_DeletedTriggerRebindsAfterRestart$' -count=1 -timeout 5m -v` → PASS 14.666s；真实 DELETE→SIGTERM/Restart、boot `ReattachActive` dangling warning、旧/新 webhook 404→202、active state、capability-check 与两次 completed flowrun 证据 | 当前工作树 |
| 2026-07-29 | EVO-096 | trigger 软删不抹掉已接受的 firing：serial 下第二条 pending 在源实体删除后仍由 scheduler 消化为第二条 run；两条 firing/run 审计仍闭合并保留 deleted triggerId，但 TriggerKind 的 best-effort lookup 诚实失败时 origin 缺席；旧 webhook path 继续 404，未来事件不会复活旧来源 | trigger / workflow / delete / accepted firing / provenance / audit | `go test ./scenarios -run '^TestContractWorkflow_DeletedTriggerKeepsAcceptedFiring$' -count=1 -v` → PASS 13.131s；`go test -race ./scenarios -run '^TestContractWorkflow_DeletedTriggerKeepsAcceptedFiring$' -count=1 -v` → PASS 14.378s；真实 webhook 双 firing、trigger DELETE、旧 path 404、scheduler drain、两条 run/firing 闭环与 provenance 负断言 | 当前工作树 |
| 2026-07-29 | EVO-097 | accepted firing 的删除语义跨硬重启闭合：trigger 删除后 SIGKILL，active workflow 保持 dangling repair state，停泊 run 与 pending firing 均存活；重启不复活 webhook，人工结算第一 run 后 scheduler 恰消费第二 firing 一次，deleted triggerId 与 firing/run 审计保留，TriggerKind 缺席时 origin 仍不伪造 | trigger / workflow / delete / accepted firing / restart / recovery / provenance | `go test ./scenarios -run '^TestContractWorkflow_DeletedTriggerAcceptedFiringSurvivesRestart$' -count=1 -v` → PASS 16.049s；`go test -race ./scenarios -run '^TestContractWorkflow_DeletedTriggerAcceptedFiringSurvivesRestart$' -count=1 -v` → PASS 15.363s；真实 DELETE→SIGKILL→Restart、active repair state、旧 webhook 404、pending inbox 保留、单次 drain 与两条 run/firing 互链 | 当前工作树 |
| 2026-07-29 | EVO-098 | 发现并修复 active workflow 入口热换后的 pending 自旋：旧 trigger firing 已入 inbox、workflow `:edit` 改绑新 trigger 后，旧事件原先每 5 秒重复 `FLOWRUN_INVALID_ENTRY` 且永不终态；现在只对这一类结构性不可重试错误记为中性 `shed`，保留 firing 审计且不铸造幻影 run，暂态 active-version/pin 错误仍留 pending 可重试 | workflow / trigger / edit / accepted firing / shed / scheduler hygiene | 先行红测 `TestContractWorkflow_PendingFiringShedsAfterEntryRebind`：旧逻辑 15s 内仍 pending、日志每 tick 重试；修复后 `go test ./scenarios -run '^TestContractWorkflow_PendingFiringShedsAfterEntryRebind$' -count=1 -v` → PASS 16.155s，`go test -race ./scenarios -run '^TestContractWorkflow_PendingFiringShedsAfterEntryRebind$' -count=1 -v` → PASS 14.912s；scheduler package normal 2.230s/race 11.516s；真实旧/新 listener projection、旧 path 404、first run 原 pin 完成、第二 firing shed 与无 phantom run | 当前工作树 |
| 2026-07-29 | EVO-099 | scheduler stale-firing 修复后的全局 acceptance 无回归：新旧 trigger 重绑、删除/重启/审计边界与既有搜索、媒体、workflow、恢复、多模态路径共同通过 | full testend regression | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 326.576s`；backend verify、docs verify 同轮全绿（docs 仅既有 DTO drift warning） | 当前工作树 |
| 2026-07-29 | EVO-100 | active workflow 在 serial overlap 下先把入口从 old trigger edit 到 new、再 revert 回 v1，并于停泊 run 与 pending firing 都存在时硬重启：监听投影每次只保留当前图，第一 run 保持原 workflow version pin，第二 firing 在恢复 old entry 后恰启动一次且两条 run/firing 审计闭合 | workflow / trigger / edit / revert / pending firing / version pin / restart / listener recovery | `go test ./scenarios -run '^TestContractWorkflow_PendingFiringFollowsActiveRevertAcrossRestart$' -count=1 -timeout 4m -v` → PASS 15.799s；`go test -race ./scenarios -run '^TestContractWorkflow_PendingFiringFollowsActiveRevertAcrossRestart$' -count=1 -timeout 5m -v` → PASS 14.993s；真实 old/new refcount、active version、SIGKILL→Restart、pending 保留、单次 drain、两条 completed run 与 started firing 互链 | 当前工作树 |
| 2026-07-29 | EVO-101 | 发现并修复 `:kill` 的 queued-firing 复活：硬停原先只摘 listener/取消 parked run，serial 下第二条已接受 pending firing 会在下一 tick 于 inactive workflow 上铸造新 run；现在 scheduler 在取消在途 run 前以 durable fence 将该 workflow 全部 pending firing 统一记为 `shed`，保留审计但不再复活 | workflow / trigger / kill / pending firing / cancellation / shed / scheduler fence | 先行红测 `TestContractWorkflow_KillShedsQueuedFiring`：旧逻辑 15s 内 firing 仍 started、产生第二 run；修复后 `go test ./scenarios -run '^TestContractWorkflow_KillShedsQueuedFiring$' -count=1 -timeout 4m -v` → PASS 10.859s；scheduler + trigger store package normal 全绿；真实 `:kill` 后 inactive、first run cancelled、second firing shed、无 post-kill run、webhook 404 | 当前工作树 |
| 2026-07-29 | EVO-102 | approval 停泊后把实体从 v1 改到 v2（新 prompt + 1s timeout），再 SIGKILL→Restart，run 仍按起跑时的 v1 pin 保持 running；flowrun 节点继续呈现 v1、inbox deadline 仍是 parkedAt+30d，人工决策后只留下 yes 终态，未泄漏 v2 或短 timeout | workflow / approval / version pin / inbox / timeout / restart recovery | `TestContractWorkflow_ApprovalPinnedAcrossEditAndRestart`：真实 HTTP create/run park、GET flowrun `pinnedRefs`、approval edit、inbox rendered/deadline、硬重启后 14 拍跨 scheduler tick 负控、human decide；normal PASS 10.09s、race PASS 10.22s；无产品修复（守住长期回归契约） | 当前工作树 |
| 2026-07-29 | EVO-103 | 发现并修复 `:deactivate` 的 lifecycle 早翻：serial 下首个 parked run 结算时，第二条已接受 pending firing 仍在 inbox，旧实现却先把 workflow 标为 inactive、随后又从 inactive 铸造第二 run；现在 `CountOutstanding` 同时计 running run + pending firing，只有两类工作都结算才从 draining 翻 inactive，listener 仍在 deactivate 当刻摘除 | workflow / trigger / deactivate / draining / pending firing / lifecycle reconcile | 先行红测 `TestContractWorkflow_DeactivateDrainsAcceptedFiring`：旧逻辑首 run 结算后 state=`inactive` 但第二 run 仍出现；修复后 normal PASS 15.22s、race PASS 13.14s；scheduler/workflow/bootstrap/trigger store 包 normal+race 全绿；真实 listener 404、first/second accepted firing 各一次 started、两条 run 依次人工决策完成、第二 run 结算后才 inactive | 当前工作树 |
| 2026-07-29 | EVO-104 | 发现并修复 draining 队列中 structural shed 无 run callback 导致永久 draining：pending firing 在 workflow edit 后因旧 entry 不再存在而直接 shed，旧实现没有任何 terminal run 回调可收口；现在 `DrainFirings` 对本批次每个 workflow scope 做一次终态对账，shed/skipped/superseded 等不造 run 的路径也能翻 inactive | workflow / trigger / deactivate / draining / pending outcome / scheduler reconcile | 先行红测 `TestContractWorkflow_DeactivateShedsInvalidPendingAndSettles`：旧逻辑 shed 后 15s 仍为 draining；修复后 normal PASS 16.38s、race PASS 16.81s；相邻 deactivate/kill/revert/approval 场景 normal+race 全绿，scheduler/workflow/bootstrap/trigger store 包 race 全绿；无 phantom run，firing 审计保留 | 当前工作树 |
| 2026-07-29 | EVO-105 | 发现并修复 trigger report 与 `:deactivate` 的 accepted-firing 竞态：report 已抓到 listener snapshot、但 durable AppendFiring 尚未完成时，旧 Detach 可先返回，workflow 随后把 outstanding 数成 0 并翻 inactive；现在每 trigger 的 report read/write gate 在 registry 摘除后 fence 已接纳 report，晚到且尚未跨 snapshot 的报告仍丢弃，manual fire 与 misfire catch-up 也共用同一栅栏 | trigger / workflow / deactivate / concurrent report / lifecycle fence | 先行阻塞红测 `TestDetachWaitsForInFlightReport`：旧实现 AppendFiring 被阻塞时 Detach 立即返回；修复后普通 + race trigger 包全绿，deactivate 三场景 normal 17.50s / race 16.51s；accepted firing 仍 durable、晚到报告不复活 listener | 当前工作树 |
| 2026-07-29 | EVO-106 | FRT-14 在本轮后续迭代再次真实复核：Google 账号仍会列出 `gemini-2.5-flash`，但首发 generate 只调用一次并诚实落为 `LLM_MODEL_NOT_FOUND` error turn；同一会话切到 `gemini-3-flash-preview` 后恢复完成，累计恰两次上游调用，失败回合没有 assistant 文本。模型失效记忆/目录自动降级仍保持为显式产品策略缺口，未擅自改变用户已保存的模型选择 | BYOK-read / model eligibility drift / recovery UX / reprobe | `EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_GoogleListedModelCanBeAccountUnavailable$' -count=1 -timeout 8m -v` → PASS 8.58s；key 未写日志 | 当前工作树 |
| 2026-07-29 | EVO-107 | backend 全量 race 首次暴露 media 单测替身违反真实 SQLite repository 的隔离语义：worker 与测试共享可变 derivative 指针，fake artifact map 也无锁；普通测试虽通过但 race gate 报读写冲突。测试替身现改为 repository 读写副本 + 受锁 artifact map，测试通过 snapshot 观察 durable 状态；生产 media 代码未改，避免把测试别名误判成产品竞态 | media preparation / worker / test-fixture concurrency | 修复前 `go test -race ./...` 仅 `internal/app/media` 失败；修复后 `go test -race ./internal/app/media` → PASS 19.123s，`go test -race ./...` → PASS；普通 media 包亦 PASS | 当前工作树 |
| 2026-07-29 | EVO-108 | media race fixture 收口后的完整产品黑盒回归无回归：当前源下 chat、agent/subagent、附件/文档、多模态、MCP/function/handler、workflow、trigger 生命周期、取消/重试/崩溃恢复与资源卫生场景共同通过 | full testend regression / cross-domain reprobe | `go test -count=1 -parallel 16 -timeout 15m ./scenarios/...` → PASS 327.505s；未注入 provider secret | 当前工作树 |
| 2026-07-29 | EVO-108 | media race fixture 修复后的完整产品黑盒回归无回归：当前源下 chat、附件/文档、多模态、MCP/function/handler、workflow、trigger、取消/重试/崩溃恢复与资源卫生套件全部通过 | full backend testend regression | `make testend`（backend）→ `ok github.com/sunweilin/anselm/testend/scenarios 337.395s` | 当前工作树 |
| 2026-07-29 | EVO-109 | 当前源在 backend race、完整 testend 与 media fixture 修复后通过整仓门禁；前端、文档与 web demo 仍与后端演进保持一致 | workspace verification / cross-layer regression | 根目录 `make verify` → backend ✓、frontend ✓、docs ✓（保留既有 DTO drift warning）、demo ✓、workspace verified | 当前工作树 |
| 2026-07-29 | EVO-110 | 当前 trigger/workflow 生命周期承重面在全量 race black-box 下保持稳定：多入口 listener、pending firing、deactivate/kill、审批停泊与重启、删除/重绑、版本 pin、结构性 shed 与恢复路径均未出现新的数据竞争或状态回归 | workflow / trigger / lifecycle / black-box race | `go test -race ./scenarios -run '^TestContractWorkflow_' -count=1 -parallel 4 -timeout 20m` → PASS 68.472s | 当前工作树 |
| 2026-07-29 | EVO-111 | chat、消息重试/分支/子代理、模型配置与 key 生命周期、文档附件、platform contract 和跨 workspace 隔离在 race black-box 下保持稳定；无新的并发错误或持久化状态漂移 | chat / contract / attachment / platform / black-box race | `go test -race ./scenarios -run '^(TestChat|TestContractChat|TestContractDocsAtt|TestPlatform)' -count=1 -parallel 4 -timeout 25m` → PASS 127.588s | 当前工作树 |
| 2026-07-29 | EVO-112 | 当前已配置 BYOK 的 DeepSeek v4 与 Google Gemini 3 文本路径再次闭合：真实探针成功、模型能力投影可选、产品默认模型保存后首轮均完成；未走受管 gateway，日志不含 key | BYOK-read / provider behavior / text smoke | `EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_TextProviderSmoke$' -count=1 -timeout 8m -v` → PASS 14.29s（DeepSeek 7.39s；Google 6.90s） | 当前工作树 |
| 2026-07-29 | EVO-113 | 多模态真实探针发现 Google 原生图片输入连续遭上游 429：transport 已分类 `ErrRateLimited`，但 loop 旧逻辑把鉴权、请求、模型、额度、限流、上游错误统统压成 `LLM_STREAM_ERROR`，丢失真实恢复语义。现在 durable turn 保留全部稳定 provider error code；Google live lane 仅在断言 `LLM_RATE_LIMITED` 后因外部窗口跳过，其他错误仍失败；OpenAI 图片、OpenAI 文档图片引用、Qwen 视频仍通过 | loop / multimodal / BYOK / error UX / provider rate limit | 修复后 `go test -race ./internal/app/loop ./internal/infra/llm` → PASS；`EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_GoogleImageInput$' -count=1 -timeout 8m -v` → PASS（6.50s，SKIP：结构化 `LLM_RATE_LIMITED`）；同轮组合探针 OpenAI image 7.16s、OpenAI document-image 3.11s、Qwen video 8.53s PASS；无 key 日志 | 当前工作树 |
| 2026-07-29 | EVO-114 | provider error 分类修复提交后的整仓门禁保持闭合，前端契约、文档索引与 web demo 未出现跨层漂移 | workspace verification / post-fix regression | 根目录 `make verify` → backend ✓、frontend ✓、docs ✓（保留既有 DTO drift warning）、demo ✓、workspace verified | `d72aca3e` |
| 2026-07-29 | EVO-115 | 已部署 Anselm API Serve 最近 TTS/媒体部署修复后的 managed 端到端复核闭合：默认 chat、图片附件、文档图片引用、quota；单次受管语音生成；朗读首次扣 quota、相同文本命中缓存不扣、换文本重新扣；实时 ASR；预置音色→危险审批→异步登记→克隆合成→删除且库存回收。所有 managed/voice 探针未注入本地 provider secret；gateway 仓库自身门禁也保持通过 | managed/default / multimodal / quota / TTS / ASR / cloned voice / gateway deployment | `EVALS_MANAGED=1 go test ./scenarios -run '^(TestLiveManaged_DefaultChat|TestLiveManaged_DefaultChatWithImageAttachment|TestLiveManaged_DocumentImageReference|TestLiveManaged_Quota)$' -count=1 -parallel 1 -timeout 20m -v` → PASS 20.324s；`EVALS_MANAGED=1 go test ./scenarios -run '^(TestLiveManaged_GenerateSpeechArtifact|TestLiveManaged_ReadAloudCache)$' -count=1 -parallel 1 -timeout 20m -v` → PASS 21.064s；`EVALS_VOICE=1 go test ./scenarios -run '^(TestLiveVoice_SpeechInputASR|TestLiveVoice_EnrollSpeakDelete)$' -count=1 -parallel 1 -timeout 30m -v` → PASS 47.165s；`Anselm-API-Serve: make verify` → race + integration e2e + lint + docs 全绿；key 未写日志 | gateway HEAD `63f402f` |
| 2026-07-29 | EVO-116 | managed 语音产物跨路由进入 BYOK Qwen 音频观看者的真实 workflow 闭环：上游 receipt 保留 provider=anselm，flowrun 携带真实 WAV MediaRef，Qwen `input_audio` 请求收到与附件逐字节一致的音频；不是文本占位或跨 provider 重画 | hybrid / workflow / managed-write / BYOK-read / audio MediaRef | `set -a; source ../.env; set +a; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedSpeechToQwenViewer$' -count=1 -parallel 1 -timeout 20m -v` → PASS 23.871s；key 未输出 | 当前工作树 |
| 2026-07-29 | EVO-117 | provider 行为探针继续闭合：当前 Kimi/Moonshot 凭证被上游拒绝时，产品 `:test` 入口诚实返回文档约定的 422 `API_KEY_TEST_FAILED`，`details.reason` 保留 `HTTP 401` 而不泄漏 key；同一 BYOK 文本 smoke 中 DeepSeek 完成，Google 当前窗口 429 被 loop 分类为 `LLM_RATE_LIMITED` 后仅跳过该外部受限样本，其他错误仍失败 | BYOK-read / provider auth / error UX / rate limit | `EVALS_BYOK=1 go test ./scenarios -run '^(TestLiveBYOK_KimiCredentialProbe|TestLiveBYOK_TextProviderSmoke)$' -count=1 -parallel 1 -timeout 15m -v` → PASS 10.790s（DeepSeek PASS；Google SKIP after structured rate-limit; Kimi probe PASS）；无 key 日志 | 当前工作树 |
| 2026-07-29 | EVO-118 | 真实并发朗读暴露“最终附件相同但已经重复付费”的竞态：同 workspace 同文本的两个请求原先都越过 miss probe、各自调用 managed TTS，随后才由 cache row 让 follower 复用附件，网关 quota 实际增加 2；read-aloud 共同层现按 workspace/key 串行 miss 并二次探测，修复后 follower 不再触碰 synthesizer，quota 只增加 1，两个响应共享同一附件且一 fresh/一 cached | managed-write / read-aloud cache / concurrency / quota | 修复前 `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_ReadAloudConcurrentDedup$' -count=1 -parallel 1 -timeout 20m -v` → FAIL（`singleCost=1 concurrentDelta=2`）；修复后同命令 → PASS 10.76s；共同层 `go test -race ./internal/app/readaloud` → PASS；backend `go test ./...` → PASS；无 provider secret 输出 | 当前工作树 |
| 2026-07-29 | EVO-119 | managed `generate_speech` 的危险审批拒绝是真正的成本闸：生产默认模型请求一次 danger，用户 deny 后同一回合完成拒绝续接，不留下 `generate_speech` receipt/音频附件；公开 monthly quota 只包含两次对话请求（提议+拒绝续接），没有额外 synthesis reservation | managed-write / danger approval / TTS / quota / error UX | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_GenerateSpeechDeniedNoSpend$' -count=1 -parallel 1 -timeout 15m -v` → PASS 13.86s；quota delta=2、无 TTS receipt/attachment；无 provider secret 输出 | 当前工作树 |
| 2026-07-29 | EVO-120 | 异步视频 writer 的危险审批也在提交 gateway job 之前生效：真实 managed `generate_video` 请求被挂起后 deny，同一回合完成、无视频 receipt/附件，monthly quota 只落两次对话请求，没有第三个视频生成 reservation；语音与视频两条不同生成路由共享相同的成本闸语义 | managed-write / video / danger approval / async resource / quota | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_GenerateVideoDeniedNoSpend$' -count=1 -parallel 1 -timeout 15m -v` → PASS 17.37s；quota delta≤2、无 video receipt/attachment；无 provider secret 输出 | 当前工作树 |
| 2026-07-29 | EVO-121 | read-aloud miss gate 与 managed danger-deny acceptance 落地后，完整黑盒产品面无回归：chat、agent/subagent、workflow/trigger、MCP/function/handler、文档/附件、多模态、媒体准备、取消/重试/崩溃恢复与资源卫生仍全部通过 | full backend testend regression / post-iteration gate | `make testend` → `ok github.com/sunweilin/anselm/testend/scenarios 343.726s`；未注入 provider secret | 当前工作树 |
| 2026-07-29 | EVO-122 | read-aloud 并发 miss gate 与语音/视频 danger-deny 验收之后，backend 全量 race 仍保持无数据竞争；共同层并发缓存、媒体审批边界与现有全域实现未引入新的 race | full backend race / post-fix concurrency gate | `go test -race ./...` → PASS；未见 race 报告，未注入 provider secret | 当前工作树 |
| 2026-07-29 | EVO-123 | 已批准且网关返回 202 后取消视频等待，受管对话落为本地 `cancelled` 终态，不晚写 `generate_video` receipt 或附件；提交点之后的上游任务/费用按网关合同保持在服务侧，客户端取消不被伪装成退款 | managed-write / async video / cancellation / orphan prevention | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_GenerateVideoCancelAfterSubmitLeavesNoOrphan$' -count=1 -parallel 1 -timeout 5m -v` → PASS 16.01s；SSE 见 `submitted to anselm`，取消后无 receipt，延迟 3s 仍无晚到附件；未注入 provider secret | 当前工作树 |
| 2026-07-29 | EVO-124 | 新增 managed 视频取消验收后的全量 backend 黑盒回归保持闭合；旧 chat、agent/subagent、workflow/trigger、媒体/附件、MCP/function/handler、恢复与资源卫生矩阵未受影响 | full backend testend regression / post-cancellation gate | `make testend`（backend）→ `ok github.com/sunweilin/anselm/testend/scenarios 279.266s`；未注入 provider secret | 当前工作树 |
| 2026-07-29 | EVO-125 | 多模态 workflow 视频分支闭合：受管 `generate_video` 在 scheduler 纯信任 workflow agent 中实际提交/轮询并落下 9,063,133-byte MP4，Qwen BYOK `qwen3.7-plus` 下游收到同一附件的原始 `video_url` base64；不是 receipt 文本或模型自述，危险长任务也没有被 workflow 边界误停 | hybrid / workflow / managed-write / BYOK-read / video MediaRef / async writer | `set -a; source ../.env; set +a; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedVideoToQwenViewer$' -count=1 -parallel 1 -timeout 15m -v` → PASS 705.60s；`/api/v1/flowruns` 201 elapsed 700170ms，附件回读 HTTP 200/9,063,133 bytes，recorder `/chat/completions` 命中 `"video_url"` 与完整 base64；key 未输出 | 当前工作树 |
| 2026-07-29 | EVO-126 | 默认 Anselm API 同一回合同时融合 PNG 与 MP4：受管模型能力同时宣称 vision/video，真实 managed chat 完成，回读两件附件均与上传字节逐字相同；不是把两条单模态成功拼成“融合” | managed-read/default / multimodal fusion / image+video | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_DefaultChatWithImageAndVideoAttachments$' -count=1 -parallel 1 -timeout 5m -v` → PASS 45.51s；图片 HTTP 200/98 bytes、视频 HTTP 200/2,969,360 bytes；key 未输出 | 当前工作树 |
| 2026-07-29 | EVO-127 | Qwen BYOK `qwen3.7-plus` 同一回合同时收到 PNG 与 MP4：真实兼容路由完成，recorder 在 `/chat/completions` 请求中同时捕获 `image_url`+图片原始 base64 与 `video_url`+视频原始 base64；两件附件回读仍逐字节一致 | byok-read / multimodal fusion / Qwen wire | `set -a; source ../.env; set +a; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_QwenImageAndVideoFusion$' -count=1 -parallel 1 -timeout 5m -v` → PASS 11.93s；图片 HTTP 200/98 bytes、视频 HTTP 200/2,969,360 bytes；key 未输出 | 当前工作树 |
| 2026-07-29 | EVO-128 | Qwen3 Omni `qwen3-omni-flash` 的“图片+语音留言”边界真实探测：上游对同一回合 image+audio 返回 400，但 recorder 证明后端原本确实发出了两种原始 native part；产品随即把该模型的跨类型约束纳入能力投影，保留首个图片 native wire、把 WAV 变成明确的“单回合只能一种媒体类型”文本注记，回合完成且两件附件仍逐字节可回读。由此区分“后端编码正确”与“模型组合契约不允许”，不再把上游限制伪装成用户看不懂的 LLM_BAD_REQUEST | byok-read / multimodal fusion / Qwen Omni contract / honest degradation | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_QwenImageAndAudioFusion$' -count=1 -parallel 1 -timeout 5m -v` 首次真实探测 → 上游 400，recorder bodyBytes=173584 且同时命中 image/audio 原始 base64；修复后同测试 → PASS 4.91s，turn completed，能力接口返回 `maxDistinctMediaKinds=1`，图片原生且音频未上 native wire、约束注记在请求中、附件回读 98/96,044 bytes 逐字节一致；key 未输出 | 已验证，待收录 commit |
| 2026-07-29 | EVO-129 | 同一 BYOK 会话先用 Qwen `qwen3.7-plus` 接收 PNG+MP4，再切换到 OpenAI `gpt-4.1-mini` 继续：历史按新模型能力重新投影，OpenAI recorder 收到原始图片 `image_url`，视频没有 `video_url` 或原始 base64，只收到明确的“current model has no native video input”注记；回合完成，两个附件仍逐字节可回读。证明模型切换不会把旧模型的 native part 契约硬塞给新模型，也不会篡改持久附件 | byok-read / model switch / history re-projection / multimodal downgrade | `set -a; source ../.env; set +a; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_ModelSwitchReprojectsHistoryMedia$' -count=1 -parallel 1 -timeout 8m -v` → PASS 14.47s；Qwen 与 OpenAI 双 recorder、key 未输出 | 已验证，待收录 commit |
| 2026-07-29 | EVO-130 | 旧 provider 绿格在当前源复核：DeepSeek 与 Google 文本、Google listed-but-unavailable 模型一次 404 后切换恢复、OpenAI `gpt-audio` WAV 及 audio+function continuation、Qwen chat-only agent 边界均保持通过；Google functionCall/functionResponse 续接本次真实请求到达上游但遇当前 429，durable code 是 `LLM_RATE_LIMITED`。原测试未把这个外部限流窗口区别于产品失败，已补为与其它 Google lane 一致：只对结构化 `LLM_RATE_LIMITED` skip，其它错误仍严格失败 | BYOK-read / provider reprobe / error classification / test hygiene | 首轮抽样命令 → 除 Google tool continuation 外均 PASS，Google tool continuation 因 429 使套件失败；修复后 `EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_GoogleToolContinuation$' -count=1 -parallel 1 -timeout 8m -v` → SKIP 7.11s（明确记录 429/`LLM_RATE_LIMITED`）；key 未输出 | 已验证，待收录 commit |
| 2026-07-29 | EVO-131 | Qwen Omni 跨类型防护、同会话模型切换 history re-projection、Google 限流测试闸门之后，当前 backend 全黑盒矩阵仍无回归；旧 chat/agent/subagent、workflow/trigger、MCP/function/handler、媒体准备、取消/重试/崩溃恢复、资源卫生与平台契约全部通过 | full backend testend regression / post-multimodal + provider reprobe gate | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 307.479s`；EVALS 门控未开启、未注入 provider secret | 已验证，待收录 commit |
| 2026-07-29 | EVO-132 | 默认 Anselm API 普通 chat 接收 WAV 附件的真实用户边界：`anselm-auto` capability 明确 `audio=false`（实时 ASR 另路），上传的 96,044-byte WAV 不被当作原生 audio 发送，附件 renderer 诚实降级为文本注记，managed 回合完成且 WAV 回读逐字节一致；没有因用户拖入语音留言而落成 gateway 400 | managed-read/default / audio attachment / honest degradation | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_DefaultChatWithAudioAttachmentDegrades$' -count=1 -parallel 1 -timeout 5m -v` → PASS 9.63s；key 未输出 | 已验证，待收录 commit |
| 2026-07-29 | EVO-133 | 默认 Anselm API 同一回合混合支持的 PNG 与不支持原生 chat audio 的 WAV：图片仍选择受管视觉路由，音频独立降级为文本注记，回合完成，没有被组合输入毒死成 400；两件附件仍可逐字节回读 | managed-read/default / mixed multimodal downgrade / attachment integrity | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_DefaultChatWithImageAndUnsupportedAudio$' -count=1 -parallel 1 -timeout 5m -v` → PASS 13.84s（go test 总计 15.016s）；图片 HTTP 200/98 bytes、WAV HTTP 200/96,044 bytes；key 未输出 | 当前工作树 |
| 2026-07-29 | EVO-134 | OpenAI BYOK `gpt-4.1-mini` 的真实图片+语音留言边界：模型能力明确 `vision=true/audio=false`，同轮仍完成；recorder 看到图片原始 base64 的 `image_url`，没有 `input_audio` 或 WAV base64，只收到明确的 `no native audio input` 注记；两件附件逐字节回读，证明 BYOK renderer 与 managed/default 在“不支持音频但支持图片”上的降级语义一致 | byok-read / OpenAI / mixed multimodal downgrade / recorder wire | `set -a; source ../.env; set +a; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAIImageAndUnsupportedAudio$' -count=1 -parallel 1 -timeout 8m -v` → PASS 7.07s（go test 总计 7.355s）；图片 HTTP 200/98 bytes、WAV HTTP 200/96,044 bytes；key 未输出 | 当前工作树 |
| 2026-07-29 | EVO-135 | OpenAI BYOK `gpt-4.1-mini` 的真实图片+视频留言边界：模型能力明确 `vision=true/video=false`，同轮仍完成；recorder 看到图片原始 base64 的 `image_url`，没有 `video_url` 或 MP4 base64，只收到明确的 `no native video input` 注记；2,969,360-byte MP4 与图片均逐字节回读 | byok-read / OpenAI / mixed multimodal downgrade / recorder wire | `set -a; source ../.env; set +a; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAIImageAndUnsupportedVideo$' -count=1 -parallel 1 -timeout 8m -v` → PASS 8.71s（go test 总计 9.795s）；图片 HTTP 200/98 bytes、MP4 HTTP 200/2,969,360 bytes；key 未输出 | 当前工作树 |
| 2026-07-29 | EVO-136 | 两条 BYOK 同轮媒体降级验收之后，当前 backend 全黑盒矩阵仍保持闭合；chat、agent/subagent、workflow/trigger、MCP/function/handler、媒体准备、取消/重试/崩溃恢复、资源卫生与平台契约没有回归 | full backend testend regression / post-BYOK mixed-media gate | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 295.632s`；EVALS 未开启、未注入 provider secret | 当前工作树 |
| 2026-07-29 | EVO-137 | 作为默认受管路径的上游服务，当前 `Anselm-API-Serve` 网关仓在本轮多模态降级验收后仍全绿：静态、race、集成 e2e、lint 与文档门禁没有回归；网关工作树保持干净 | gateway cross-repo gate / managed dependency | `cd ../Anselm-API-Serve && make verify` → `go vet`、`go build`、`go test -race ./...`、`go test -tags=integration -race -count=1 ./internal/e2e/...`（6.605s）、golangci-lint `0 issues`、docs lint `0 warning`；无 provider secret 输出 | 当前工作树 |
| 2026-07-29 | EVO-138 | OpenAI BYOK `gpt-4.1-mini` 同一回合附两张 PNG：两个 attachment receipt 均保持 98-byte 原样，recorder 在同一 `/chat/completions` 请求中捕获两个精确 `image_url` parts；没有把同类媒体错误折叠成第一件或重复为额外回合 | byok-read / OpenAI / multiple same-kind media / recorder wire | `set -a; source ../.env; set +a; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAIMultipleImages$' -count=1 -parallel 1 -timeout 8m -v` → PASS 7.94s（go test 总计 9.110s）；两件附件 HTTP 200/98 bytes；key 未输出 | 当前工作树 |
| 2026-07-29 | EVO-139 | 多图 live 场景落地后，backend 本身常规门禁仍闭合：格式、vet、全量 Go build 与单测均通过，没有因验收测试扩展造成后端装配回归 | backend verify / post-live-test gate | `make -C backend verify` → `✓ backend verified`；未注入 provider secret | 当前工作树 |
| 2026-07-29 | EVO-140 | 多图验收再加一条成本守卫并真实重跑：OpenAI `gpt-4.1-mini` 同轮两个 PNG 仍只产生一次上游 `/chat/completions`，两个 native `image_url` parts 与附件完整性均保持；避免“重试后才成功”被误计为通过 | byok-read / OpenAI / duplicate-call guard | `set -a; source ../.env; set +a; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAIMultipleImages$' -count=1 -parallel 1 -timeout 8m -v` → PASS 6.75s（go test 总计 7.939s）；`chat/completions=1`、两件附件 HTTP 200/98 bytes；key 未输出 | 当前工作树 |
| 2026-07-29 | EVO-141 | 当前 API Serve + BYOK workflow 的 function 产地闭合：受管 agent 真实执行用户 function，sandbox 写出 PNG 并落 `function_artifact` MediaRef；下游 OpenAI `gpt-4.1-mini` viewer 在同一 flowrun 收到 98-byte exact native image part，附件与 flowrun 均完成；证明 function 不是只返回 receipt 文本 | hybrid / workflow / managed function / BYOK-read / MediaRef producer | `set -a; source ../.env; set +a; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedFunctionToOpenAIViewer$' -count=1 -parallel 1 -timeout 15m -v` → PASS 21.61s（go test 总计 22.762s）；附件 HTTP 200/98 bytes、recorder exact image wire；key 未输出 | 当前工作树 |
| 2026-07-29 | EVO-142 | 当前 API Serve + BYOK workflow 的 resident handler 产地闭合：受管 agent 调用驻留 `plot` 方法，per-call `ANSELM_OUT` 产出 `handler_artifact` MediaRef；下游 OpenAI viewer 在同一 flowrun 收到 98-byte exact native image part，附件与 flowrun 均完成 | hybrid / workflow / managed handler / BYOK-read / per-call artifact | `set -a; source ../.env; set +a; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedHandlerToOpenAIViewer$' -count=1 -parallel 1 -timeout 15m -v` → PASS 20.90s（go test 总计 22.108s）；附件 HTTP 200/98 bytes、recorder exact image wire；key 未输出 | 当前工作树 |
| 2026-07-29 | EVO-143 | 当前 API Serve + BYOK workflow 的 stdio MCP 产地闭合：真实 MCP server `snapshot` 返回 196-byte PNG content block，受管 agent 铸 `mcp_media` receipt，下游 OpenAI viewer 在同一 flowrun 收到 exact native image part；跨进程边界没有退化成占位文本 | hybrid / workflow / managed MCP / BYOK-read / process-boundary MediaRef | `set -a; source ../.env; set +a; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedMCPToOpenAIViewer$' -count=1 -parallel 1 -timeout 15m -v` → PASS 18.87s（go test 总计 19.763s）；附件 HTTP 200/196 bytes、recorder exact image wire；key 未输出 | 当前工作树 |
| 2026-07-29 | EVO-144 | function、resident handler、stdio MCP 三条当前 hybrid producer workflow 落地后，backend 全黑盒矩阵仍闭合；普通 chat、agent/subagent、workflow/trigger、媒体、恢复与资源卫生未出现回归 | full backend testend regression / post-producer-workflow gate | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 339.362s`；EVALS 未开启、未注入 provider secret | 当前工作树 |
| 2026-07-29 | EVO-145 | 本轮 producer workflow 与多模态输入扩展后，根目录跨层总门禁仍闭合：backend、frontend、docs、web demo 全部通过，未出现主仓装配漂移 | workspace verification / post-producer-workflow gate | 根目录 `make verify` → `✓ backend`、`✓ frontend`、`✓ docs`、`✓ demo`、`✓ workspace verified`；未开启 EVALS、未注入 provider secret | 当前工作树 |
| 2026-07-29 | EVO-146 | 默认 Anselm API 同一回合混合 MP4 与不支持原生 chat audio 的 WAV：视频仍走受管视频路由，音频独立降级为文本注记，回合完成且两件附件逐字节回读；证明降级不会因视频分支而退化成整回合 400 | managed-read/default / mixed multimodal downgrade / video + audio attachment | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_DefaultChatWithVideoAndUnsupportedAudio$' -count=1 -parallel 1 -timeout 8m -v` → PASS 45.26s（go test 总计 45.912s）；MP4 HTTP 200/2,969,360 bytes、WAV HTTP 200/96,044 bytes；未输出 provider secret | 当前工作树 |
| 2026-07-29 | EVO-147 | 视频+不支持音频混合场景加入后，backend 全黑盒 acceptance 矩阵仍闭合；既有 chat、agent/subagent、workflow/trigger、MCP/function/handler、媒体准备、取消/重试/崩溃恢复与资源卫生没有回归 | full backend testend regression / post-managed-video-audio gate | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 368.254s`；EVALS 未开启、未注入 provider secret | 当前工作树 |
| 2026-07-29 | EVO-148 | 新增受管视频+音频混合降级验收提交后，主仓跨层总门禁仍闭合；backend、frontend、docs 与 web demo 均通过，网关仓保持独立且干净 | workspace verification / post-managed-video-audio gate | 根目录 `make verify` → `✓ backend`、`✓ frontend`、`✓ docs`、`✓ demo`、`✓ workspace verified`；未开启 EVALS、未注入 provider secret；`Anselm-API-Serve` 工作树 clean | 当前工作树 |
| 2026-07-29 | EVO-149 | depth-1 managed general-purpose subagent 多模态路径 reprobe 保持通过：真实子运行恰调用一次 `generate_image`，receipt 回到父回合且保留 `provider=anselm`，附件内容 HTTP 200/1,124,546 bytes 可回读；本次未出现非法 `subagent_type`，此前告警未复现 | managed subagent / capability tool / MediaRef / nondeterministic-model reprobe | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_SubagentGenerateImageArtifact$' -count=1 -parallel 1 -timeout 10m -v` → PASS 70.16s；无本机 provider key | 当前工作树 |
| 2026-07-29 | EVO-150 | 交叉核对“子代理产物在下一次独立 chat 回合继续作为原生图片”这一探索性假设：当前契约只承诺 loop 内同一 ReAct 回合的临时 `MediaExpander` user 消息，持久历史仅保留 tool receipt 文本；`chat.LoadHistory` 与领域文档一致，故不把跨回合原生媒体续接当成回归或擅自改产品。若未来要支持该体验，需要单独的历史媒体重投影设计与 provider 角色协议 | chat history / subagent media boundary / contract audit | 代码核对：`backend/internal/app/chat/history.go`、`backend/internal/app/loop/loop.go`；契约核对：[chat reference](../../references/backend/domains/chat.md) §3；探索性真实 probe 未纳入验收、不形成通过声称 | 当前工作树 |
| 2026-07-29 | EVO-151 | subagent reprobe 与 iteration log 落盘后，两侧工程门禁仍闭合：backend 全量 Go verify 通过，部署网关静态/race/集成 e2e/lint/docs 全通过，两个工作树均 clean | cross-repo gate / post-reprobe verification | 主仓 `make -C backend verify` → `✓ backend verified`；`../Anselm-API-Serve make verify` → `go vet`、`go build`、`go test -race ./...`、`go test -tags=integration -race -count=1 ./internal/e2e/...` (6.455s)、golangci-lint `0 issues`、docs lint `0 warning`；无 provider secret 输出 | 当前工作树 |
| 2026-07-29 | EVO-152 | 当前 `.env` provider 行为抽样保持闭合：DeepSeek 与 Google 真实文本回合均完成；Kimi/Moonshot 当前凭证仍由上游拒绝，但产品 `:test` 诚实保留 422 `API_KEY_TEST_FAILED` 与 `details.reason=HTTP 401`，没有被 managed fallback 代跑 | BYOK-read / provider smoke / auth error classification | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^(TestLiveBYOK_KimiCredentialProbe|TestLiveBYOK_TextProviderSmoke)$' -count=1 -parallel 1 -timeout 15m -v` → PASS 14.079s（DeepSeek 3.10s、Google 5.37s、Kimi probe 5.06s）；key 未输出 | 当前工作树 |
| 2026-07-29 | EVO-153 | DeepSeek 当前 `deepseek-v4-flash` 的 OpenAI-compatible 工具续接闭合：真实请求先发现并执行 Anselm sandbox function，再以第二次/第三次 `/chat/completions` 续接，持久化 `tool_call`/`tool_result` 与最终 `144` 均成立；recorder 断言按转义历史中的稳定结果标记工作，不绑定某个 provider 角色序列 | BYOK-read / DeepSeek compat / native tool continuation | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_DeepSeekToolContinuation$' -count=1 -parallel 1 -timeout 10m -v` → PASS 11.15s（DeepSeek key probe、function env ready、`chatCalls=3`、tools/tool_calls/result wire 均通过）；key 未输出 | `38d29add` |
| 2026-07-29 | EVO-154 | DeepSeek 续接验收落地后，文档与 backend 常规门禁仍闭合；DTO drift 仅保留既有 opt-in warning，没有把未锚定的同名 Go struct 缺口误报成失败 | docs / backend verification / post-provider-probe gate | `make -C docs verify` → documentation verified（1 个既有 drift warning）；`make -C backend verify` → `✓ backend verified`；未注入 provider secret | 当前提交 |
| 2026-07-29 | EVO-155 | DeepSeek 工具续接验收提交后再次跑完整 backend 黑盒矩阵，普通 chat、agent/subagent、workflow/trigger、MCP/function/handler、媒体/多模态、取消/重试/崩溃恢复与资源卫生均保持闭合；未开启任何真钱 EVALS | full backend testend regression / post-DeepSeek acceptance gate | `make testend`（backend）→ `ok github.com/sunweilin/anselm/testend/scenarios 343.659s`；未注入 provider secret | 当前提交 |
| 2026-07-29 | EVO-156 | OpenAI BYOK 图片回合的真实原地重试闭合：首轮与 regenerate 都在 `/chat/completions` 携带同一 exact image part；持久消息保持一条 user、两条 assistant 版本，`retryOf`/`supersededBy` 双向链和 source attachment 字节均成立 | BYOK-read / OpenAI / retry / multimodal history | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAIImageRetryPreservesNativeHistory$' -count=1 -parallel 1 -timeout 8m -v` → PASS 8.97s（上游 chat requests=2；key 未输出） | `9eb1e6d5` |
| 2026-07-29 | EVO-157 | 图片原地重试验收落地后，docs 与 backend 常规门禁仍通过；既有 DTO drift warning 未扩大为失败，未发现 retry/media 共同层回归 | docs / backend verification / post-retry gate | `make -C docs verify` → documentation verified（1 个既有 drift warning）；`make -C backend verify` → `✓ backend verified`；未注入 provider secret | 当前提交 |
| 2026-07-29 | EVO-158 | OpenAI BYOK 图片编辑重发闭合：编辑文本后旧 user+assistant 版本被正确 supersede，新版本 `retryOf` 双链成立，原始 attachment 快照和 exact image part 在第二次 `/chat/completions` 保留，源字节不变 | BYOK-read / OpenAI / edit-resend / multimodal history | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAIImageEditResendPreservesAttachment$' -count=1 -parallel 1 -timeout 8m -v` → PASS 8.73s（上游 chat requests=2；key 未输出） | `ee7e9693` |
| 2026-07-29 | EVO-159 | edit-resend 验收落地后 docs/backend 门禁仍闭合；retry/media 只新增测试与事实记录，没有触发共同层代码回归 | docs / backend verification / post-edit-resend gate | `make -C docs verify` → documentation verified（1 个既有 drift warning）；`make -C backend verify` → `✓ backend verified`；未注入 provider secret | 当前提交 |
| 2026-07-29 | EVO-160 | Google Gemini 原生工具续接在本次 provider 窗口恢复：真实 `functionCall`→Anselm function→`functionResponse`→最终回答完成，`thoughtSignature` 保持在后续原生 contents 请求；此前 429 仅是外部 rate window，本次不再 skip | BYOK-read / Google native / tool continuation / rate-window reprobe | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_GoogleToolContinuation$' -count=1 -parallel 1 -timeout 8m -v` → PASS 13.72s；key 未输出 | 当前提交 |
| 2026-07-29 | EVO-161 | OpenAI 图片 retry 两分支与 Google provider reprobe 后，主仓跨层总门禁仍闭合：backend、frontend、docs、web demo 全部通过，API Serve 工作树仍 clean | workspace verification / post-retry + provider gate | 根目录 `make verify` → `✓ backend`、`✓ frontend`、`✓ docs`、`✓ demo`、`✓ workspace verified`；未开启 EVALS；`Anselm-API-Serve` 未改动 | 当前提交 |
| 2026-07-29 | EVO-162 | provider behavior 合跑保持闭合：Kimi 当前凭证的上游 HTTP 401 在 `:test` 入口诚实归类为 422 `API_KEY_TEST_FAILED`；DeepSeek/Google 文本与原生工具续接、OpenAI 图片 retry 与 edit-resend 的 durable 版本链和 exact image wire 均通过 | BYOK-read / cross-provider behavior gate / multimodal history | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^(TestLiveBYOK_(KimiCredentialProbe|TextProviderSmoke|DeepSeekToolContinuation|GoogleToolContinuation|OpenAIImageRetryPreservesNativeHistory|OpenAIImageEditResendPreservesAttachment))$' -count=1 -parallel 1 -timeout 20m -v` → PASS（各项通过；Google 单项复核 11.418s，总合跑无失败；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-163 | 受管 `generate_image` → workflow 下游 Gemini 原生 viewer 闭合：flowrun 生成的图片附件被下游真实 Google 请求以原生 `inlineData` 携带，附件字节可回读，workflow 完成且没有退化为 receipt 文本 | hybrid / workflow / managed-write → Google BYOK-read / native multimodal wire | `set -a; source ../.env; set +e; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedImageToGoogleViewer$' -count=1 -parallel 1 -timeout 12m -v` → PASS 57.432s（生成附件 1,085,427 bytes；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-164 | workflow producer→viewer 三路真实复核闭合：function、驻留 handler、stdio MCP 各自产生独立 MediaRef，OpenAI vision viewer 均收到对应 exact image bytes；flowrun、附件回读和 producer 边界均成立，没有跨 producer 串线或重复铸造 | FRT-05 / hybrid / workflow / function + handler + MCP → OpenAI BYOK-read | `set -a; source ../.env; set +e; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManaged(Function|Handler|MCP)ToOpenAIViewer$' -count=1 -parallel 1 -timeout 20m -v` → PASS 54.220s（key 未输出） | 当前提交 |
| 2026-07-29 | EVO-165 | 受管语音 → workflow 下游 Qwen 音频 viewer 闭合：真实 WAV MediaRef 在下游 OpenAI-compatible 请求中以原生 `input_audio` 传递，flowrun 与附件回读完成，没有落回图片或文本占位路径 | FRT-05 / hybrid / workflow / managed-write speech → Qwen BYOK-read audio | `set -a; source ../.env; set +e; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedSpeechToQwenViewer$' -count=1 -parallel 1 -timeout 12m -v` → PASS 16.365s（WAV 80,684 bytes；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-166 | 受管异步视频 → workflow 下游 Qwen video viewer 闭合：视频提交/轮询产出 7.93 MB MP4，flowrun 下游真实消费原生视频输入并完成，附件逐字节可回读，没有退化为 receipt 文本 | FRT-05 / hybrid / workflow / managed-write video → Qwen BYOK-read video | `set -a; source ../.env; set +e; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedVideoToQwenViewer$' -count=1 -parallel 1 -timeout 15m -v` → PASS 129.992s（MP4 7,934,807 bytes；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-167 | 受管 read-aloud 的缓存、配额与并发去重真实闭合：同文本/音色重听不重复花费，换文本才生成新附件；两路同时请求只铸一份音频并共享结果，两个场景均完成且无 token 路径介入 | FRT-08 / managed-write / read-aloud / cache + concurrency | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_ReadAloud(Cache|ConcurrentDedup)$' -count=1 -parallel 1 -timeout 12m -v` → PASS 18.352s（真实 WAV 195,884 bytes；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-168 | 真实 subagent 图片验收首次暴露的是 testend 断言 bug：子 agent 已生成并把 `provider=anselm`、`attachmentId` receipt 放入父 `tool_result`，但模型使用 pretty-printed JSON，旧守卫只认无空格形状而假红；将 image/edit/speech/video/animation receipt 断言统一改为语义字段匹配后原场景通过，附件真实可读 | managed-write / subagent / test governance | 首跑 `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_SubagentGenerateImageArtifact$' -count=1 -parallel 1 -timeout 12m -v` → 断言假红；修复后同命令 → PASS 50.043s，attachment content 1,071,404 bytes；测试修复 `8d559f19`，key 未输出 | `8d559f19` |
| 2026-07-29 | EVO-169 | 危险操作与恢复真实闭合：语音/视频审批拒绝不铸造 receipt 或额外预留；视频已提交后取消时本地回合落正确终态、无迟到附件/孤儿 receipt，上游 job/spend 仍诚实留在 gateway 账本边界 | FRT-09 + FRT-13 / managed-write / approval + cancellation | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_Generate(SpeechDeniedNoSpend|VideoDeniedNoSpend|VideoCancelAfterSubmitLeavesNoOrphan)$' -count=1 -parallel 1 -timeout 20m -v` → PASS 38.659s，key 未输出 | 当前提交 |
| 2026-07-29 | EVO-170 | subagent receipt 守卫修复与本轮 managed/hybrid/provider 真实验收后，完整静态 backend 黑盒矩阵仍无回归：chat、agent/subagent、workflow/trigger、MCP/function/handler、媒体、多模态、取消/重试/崩溃恢复、资源卫生共同通过 | full backend testend regression / post-evolution gate | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 323.182s`；EVALS 未开启、未注入 provider secret | 当前提交 |
| 2026-07-29 | EVO-171 | 本轮 provider/multimodal/workflow/read-aloud/subagent 守卫落地后，主仓跨层总门禁仍闭合；后端、前端、文档和 web demo 全绿，API Serve 工作树未被触碰 | workspace verification / post-evolution gate | 根目录 `make verify` → `✓ backend`、`✓ frontend`、`✓ docs`、`✓ demo`、`✓ workspace verified`；无 EVALS、无 provider secret，工作树 clean | 当前提交 |
| 2026-07-29 | EVO-172 | OpenAI `gpt-audio` provider-owned bridge 复核闭合：真实 WAV 以原生音频输入进入 BYOK 对话，随后真实 `run_function` 工具续接完成，附件与多请求 continuation 均成立，没有回退到文本占位 | FRT-02 / BYOK-read / OpenAI audio / tool continuation | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAIAudioToolContinuation$' -count=1 -parallel 1 -timeout 8m -v` → PASS 12.782s；key 未输出 | 当前提交 |
| 2026-07-29 | EVO-173 | Qwen-MT chat-only 能力边界复核闭合：真实 probe 成功后，agent invoke 在任何 step/provider 调用前明确失败并保持 0 steps；本轮 capability tools 注入没有把 chat-only 模型误露为 agent 可用 | FRT-10 / BYOK-read / Qwen chat-only / capability honesty | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_QwenChatOnlyAgentRejected$' -count=1 -parallel 1 -timeout 8m -v` → PASS 5.263s；key 未输出 | 当前提交 |
| 2026-07-29 | EVO-174 | Qwen Omni 图片+音频融合复核闭合：模型的同轮单一媒体类型限制被能力投影诚实执行，图片保留原生 part、音频降级为明确文本注记；回合完成，两件附件（PNG 98 bytes、WAV 96,044 bytes）仍逐字节可读 | FRT-02 / BYOK-read / Qwen Omni / mixed multimodal degradation | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_QwenImageAndAudioFusion$' -count=1 -parallel 1 -timeout 8m -v` → PASS 7.138s；key 未输出 | 当前提交 |
| 2026-07-29 | EVO-175 | 同一 BYOK 会话的模型切换 history re-projection 复核闭合：Qwen 首轮接收图片+视频后切到 OpenAI，图片仍走 native `image_url`，视频因新模型能力不足转为明确降级注记且不发送 `video_url`/原始 base64；会话完成，两个附件未被改写 | FRT-02 / BYOK-read / model switch / history media projection | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_ModelSwitchReprojectsHistoryMedia$' -count=1 -parallel 1 -timeout 8m -v` → PASS 15.201s；key 未输出 | 当前提交 |
| 2026-07-29 | EVO-176 | 默认受管图片+视频同回合融合复核闭合：两件附件均真实进入对话并逐字节回读（PNG 98 bytes、MP4 2,969,360 bytes），回合完成且多附件组合没有退化为上游 400 | FRT-01 / managed-read/default / same-turn multimodal fusion | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_DefaultChatWithImageAndVideoAttachments$' -count=1 -parallel 1 -timeout 8m -v` → PASS 48.082s；无 provider secret | 当前提交 |
| 2026-07-29 | EVO-177 | OpenAI BYOK 同轮多图复核闭合：两个独立 PNG 均被真实请求按 native image parts 传递，顺序与原始字节保持一致，两个附件均可逐字节回读 | FRT-02 / BYOK-read / OpenAI / multiple images | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAIMultipleImages$' -count=1 -parallel 1 -timeout 8m -v` → PASS 10.642s；key 未输出 | 当前提交 |
| 2026-07-29 | EVO-178 | 生产 managed realtime ASR 复核闭合：workspace 开通后 `/api/v1/speech/asr` WebSocket 握手、会话结束帧与最终文本状态均成立，未读取本地 provider secret | FRT-01 / managed-read/default / realtime speech input | `EVALS_VOICE=1 go test ./scenarios -run '^TestLiveVoice_SpeechInputASR$' -count=1 -parallel 1 -timeout 12m -v` → PASS 6.065s；API Serve 真实链路 | 当前提交 |
| 2026-07-29 | EVO-179 | 生产 managed 音色全生命周期复核闭合：预置参考音频经危险审批登记，异步 voice row/句柄可用，克隆朗读产生真实 WAV，删除后上游/本地音色清理且库存槽位释放 | FRT-07 / managed-write / voice enrollment + synthesis + delete | `EVALS_VOICE=1 go test ./scenarios -run '^TestLiveVoice_EnrollSpeakDelete$' -count=1 -parallel 1 -timeout 20m -v` → PASS 45.218s（原始/克隆 WAV 357,164/337,964 bytes；最终 `/api/v1/voices` 空；API Serve 真实链路，key 未输出） | 当前提交 |
| 2026-07-29 | EVO-180 | 默认受管输入边界矩阵复核闭合：普通 WAV、图片+不支持音频、视频+不支持音频均完成诚实降级，支持的媒体分支未被组合输入毒死，所有附件逐字节保持；同一批次 live quota 仍返回一致的可用计数 | FRT-01 / managed-read/default / honest multimodal degradation + quota | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_(DefaultChatWithAudioAttachmentDegrades|DefaultChatWithImageAndUnsupportedAudio|DefaultChatWithVideoAndUnsupportedAudio|Quota)$' -count=1 -parallel 1 -timeout 20m -v` → PASS 62.554s（WAV 96,044 bytes；PNG 98 bytes；MP4 2,969,360 bytes；未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-181 | Google 目录资格漂移复核闭合：目录可见但账号不可用的模型首发只落一次结构化 `LLM_MODEL_NOT_FOUND`，assistant 不伪造正文；切换到可用模型后同一会话恢复完成，未出现无界重试或 managed fallback 代跑 | FRT-14 / BYOK-read / Google / model qualification drift | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_GoogleListedModelCanBeAccountUnavailable$' -count=1 -parallel 1 -timeout 12m -v` → PASS 9.315s（首发 404；后续切换恢复；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-182 | 混合 ownership 两条高频路径复核闭合：OpenAI BYOK planner 仅调度一次受管 `generate_image`；workflow 的 managed 生成者→OpenAI viewer 收到与附件逐字一致的 native image part，两个 flowrun/对话均完成，没有把 receipt 文本当媒体 | FRT-03 / FRT-04 / hybrid / managed-write → OpenAI BYOK-read | `set -a; source ../.env; set +e; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLive(Hybrid_OpenAIPlansManagedImage|Hybrid_WorkflowManagedImageToOpenAIViewer)$' -count=1 -parallel 1 -timeout 25m -v` → PASS 54.926s（生成附件 1,105,206/1,107,601 bytes；OpenAI recorder exact image wire；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-183 | provider 行为回归全通过：DeepSeek compat 与 Google native functionCall/functionResponse 均完成续接，OpenAI `gpt-audio` 的原生 WAV→`run_function` 续接保真，Qwen-MT 仍只允许 chat 而在 agent invoke 前明确拒绝；DeepSeek/Google 文本 smoke 同样完成 | FRT-10 / FRT-11 / BYOK-read / provider behavior + native tools | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_(TextProviderSmoke|DeepSeekToolContinuation|GoogleToolContinuation|OpenAIAudioToolContinuation|QwenChatOnlyAgentRejected)$' -count=1 -parallel 1 -timeout 25m -v` → PASS 44.139s（5 项均通过；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-184 | managed 直接视频生成复核闭合：真实对话先停在人闸，批准后经 API Serve 异步提交/轮询/下载，恰好一条 `provider=anselm` receipt 绑定可回读 MP4，回合续接完成；没有重复付费调用 | FRT-09 / managed-write / generate_video + danger approval + async resource | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_GenerateVideoArtifact$' -count=1 -parallel 1 -timeout 20m -v` → PASS 155.833s（MP4 3,093,819 bytes；未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-185 | managed 动画专用 writer 文本-only复核闭合：请求走 `/videos/animations` 独立路由并经过危险审批，异步产出 5,965,771-byte MP4；超出普通附件预算时按既定媒体 envelope 降级为可解释注记，仍能完成后续回合，不伪装成普通视频生成 | FRT-09 / managed-write / animate_image text-only + dedicated route + budget cap | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_AnimateImageArtifactTextOnly$' -count=1 -parallel 1 -timeout 20m -v` → PASS 117.382s（未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-186 | managed 图片→动画融合复核闭合：同一对话先接收用户 PNG，再由模型调用 `animate_image`，审批后专用动画路由产出 3,080,918-byte MP4；`sourceAttachmentId` 血缘、receipt 和后续回合完整，输入图片未被重画或丢失 | FRT-09 / managed-read/write / image + animate_image fusion | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_AnimateImageArtifact$' -count=1 -parallel 1 -timeout 20m -v` → PASS 159.269s（PNG 98 bytes、MP4 3,080,918 bytes；未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-187 | Qwen 多模态组合边界复核闭合：`qwen3.7-plus` 同轮 PNG+MP4 仍双 native parts 且附件逐字节回读；Qwen Omni 同轮 PNG+WAV 继续执行单一媒体类型契约，图片保留 native、音频明确降级而非向用户暴露上游 400 | FRT-02 / BYOK-read / Qwen / multimodal fusion + contract degradation | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_Qwen(ImageAndVideoFusion|ImageAndAudioFusion)$' -count=1 -parallel 1 -timeout 15m -v` → PASS 13.642s（PNG 98 bytes、MP4 2,969,360 bytes、WAV 96,044 bytes；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-188 | OpenAI BYOK 读侧入口矩阵复核闭合：直接 PNG、文档 `anselm://media` PNG、`gpt-audio` WAV 均按对应 native part 到达上游，文档渲染没有退化为 prompt URL，三件附件均逐字节可回读 | FRT-02 / FRT-06 / BYOK-read / OpenAI / direct + document + audio input | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAI(ImageInput|DocumentImageReference|AudioInput)$' -count=1 -parallel 1 -timeout 15m -v` → PASS 15.452s（PNG 98 bytes；WAV 96,044 bytes；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-189 | 动画/视频/音频与 provider 读侧连续复核后，根目录跨层总门禁仍闭合：backend、Flutter frontend（4 组）、docs、web demo 全绿；主仓和 API Serve 工作树均无未提交改动 | cross-repo / workspace gate / post-live-evolution | 根目录 `make verify` → `✓ backend`、`✓ frontend`、`✓ docs`、`✓ demo`、`✓ workspace verified`；API Serve `git status --short` clean；未输出 provider secret | 当前提交 |
| 2026-07-29 | EVO-190 | 生产网关仓独立门禁复核闭合：Go vet/build、全量 race 单测、integration e2e、golangci-lint 与 35 份 docs lint 全绿；API Serve 工作树保持 clean，未改变部署仓代码 | API Serve / gateway gate / post-live-evolution | `cd ../Anselm-API-Serve && make verify` → race 全包通过、`internal/e2e` 8.491s、golangci-lint `0 issues`、docs lint `35 file(s) ok, 0 warning(s)` | 当前提交 |
| 2026-07-29 | EVO-191 | managed 图片编辑复核闭合：同一回合先生成原图，再仅调用一次 `edit_image`，新产物与原图字节不同且均为有效图片；edit receipt 保留 `sourceAttachmentId`/provider 血缘，回合续接和两件附件回读完整 | FRT-09 / managed-write / generate_image → edit_image / MediaRef lineage | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_EditImageArtifact$' -count=1 -parallel 1 -timeout 15m -v` → PASS 93.654s（原/编辑图片 1,080,557/1,668,015 bytes；未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-192 | managed 直接语音生成复核闭合：默认对话模型恰调用一次 `generate_speech`，危险/工具回合续接完成，receipt 标记 `provider=anselm` 并绑定真实 RIFF/WAVE 附件，可逐字节回读且无重复合成 | FRT-09 / managed-write / generate_speech + MediaRef | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_GenerateSpeechArtifact$' -count=1 -parallel 1 -timeout 12m -v` → PASS 15.521s（WAV 80,684 bytes；未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-193 | managed 直接图像生成复核闭合：默认模型只铸造一次 `generate_image` receipt，provider 明确为 `anselm`，真实 PNG 附件可回读并继续同一回合；两步上限守住了重复付费边界 | FRT-03 / FRT-09 / managed-write / generate_image | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_GenerateImageArtifact$' -count=1 -parallel 1 -timeout 12m -v` → PASS 35.132s（PNG 1,092,047 bytes；未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-194 | 纯 managed workflow 生成者→受管 viewer 复核闭合：同一 flowrun 中上游 `generate_image` receipt/MediaRef 持久化后，下游 agent 完成消费并回到终态；下游没有把产物退化为 receipt 文本，附件可回读 | FRT-04 / managed workflow / generate_image → MediaRef → managed viewer | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_WorkflowGenerateImageToViewer$' -count=1 -parallel 1 -timeout 15m -v` → PASS 61.323s（PNG 1,101,834 bytes；未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-195 | 本轮 direct managed writers、动画/视频/音频/多模态、provider BYOK/hybrid 与网关复核之后，完整 backend 黑盒 acceptance 仍无回归；chat、agent/subagent、workflow/trigger、MCP/function/handler、媒体准备、取消/重试/崩溃恢复和资源卫生共同通过 | full backend testend regression / post-live-evolution gate | `make testend`（backend）→ `ok github.com/sunweilin/anselm/testend/scenarios 352.631s`；未开启 EVALS、未注入 provider secret | 当前提交 |
| 2026-07-29 | EVO-196 | Kimi 当前凭证复核仍诚实闭合：上游 HTTP 401 在 `:test` 入口归类为产品 422 `API_KEY_TEST_FAILED`，保留结构化 `details.reason`，不触发 managed fallback 或普通对话消费 | FRT-11 / BYOK-read / Kimi / credential error classification | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_KimiCredentialProbe$' -count=1 -parallel 1 -timeout 8m -v` → PASS 5.845s（HTTP 401→422；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-197 | 默认开箱读路径 reprobe 闭合：新 workspace 的 managed key 与 dialogue default 同步就绪后，普通中文聊天、PNG、MP4、文档 `anselm://media` 图片引用均完成；三类附件仍可逐字节回读，没有被近期 managed writer、目录刷新或多模态能力改动污染 | FRT-01 / FRT-06 / managed-read/default / onboarding + chat + image + video + document media | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_(DefaultChat|DefaultChatWithImageAttachment|DefaultChatWithVideoAttachment|DocumentImageReference)$' -count=1 -parallel 1 -timeout 20m -v` → PASS 99.438s（PNG 98 bytes；MP4 2,969,360 bytes；文档图片 98 bytes；无 provider secret） | 当前提交 |
| 2026-07-29 | EVO-198 | OpenAI BYOK 多模态版本链 reprobe 闭合：图片首轮后的 regenerate 与 edit-resend 均保留原图 native part，版本链 old↔new 正确、原图字节不变，分别只产生预期的上游请求，不把重放变成文本-only 或重复上传 | FRT-02 / BYOK-read / OpenAI / multimodal history + retry + edit-resend | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAIImage(RetryPreservesNativeHistory|EditResendPreservesAttachment)$' -count=1 -parallel 1 -timeout 15m -v` → PASS 15.581s（两项均通过；PNG 98 bytes；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-199 | 跨方言 BYOK 读侧 reprobe 闭合：Qwen OpenAI-compatible 视频请求保留完整 2,969,360-byte MP4，Google Gemini 原生图片请求保留 98-byte PNG；两家真实 probe、模型选择、上游回合和附件回读均通过，目录刷新没有改变能力投影 | FRT-02 / BYOK-read / Qwen compat + Google native / video + image | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_(GoogleImageInput|QwenVideoInput)$' -count=1 -parallel 1 -timeout 12m -v` → PASS 25.418s（两项均通过；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-200 | workflow 多模态 producer reprobe 闭合：function、驻留 handler、stdio MCP 三种产地各自铸造独立 PNG MediaRef，OpenAI viewer 均在真实下游请求中收到对应 exact bytes；flowrun 终态、附件回读和 producer 边界均通过，没有跨产地串线或 receipt 文本冒充媒体 | FRT-05 / hybrid / workflow / function + handler + MCP → OpenAI BYOK-read | `set -a; source ../.env; set +e; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManaged(Function|Handler|MCP)ToOpenAIViewer$' -count=1 -parallel 1 -timeout 20m -v` → PASS 76.877s（function/handler/MCP 均通过；PNG 98/196 bytes；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-201 | workflow viewer 音视频边界 reprobe 闭合：managed `generate_speech` 的 80,684-byte WAV 与 managed async video 的 11,947,650-byte MP4 均在下游 Qwen 真实请求中按 native `input_audio`/`video_url` 传递，flowrun 完成且附件逐字节可回读；视频上游耗时 243.574s 仍无重复提交或 receipt 文本退化 | FRT-04 / hybrid / workflow / managed-write speech + video → Qwen BYOK-read | `set -a; source ../.env; set +e; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManaged(SpeechToQwenViewer|VideoToQwenViewer)$' -count=1 -parallel 1 -timeout 25m -v` → PASS 263.538s（两项均通过；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-202 | managed subagent 图片 reprobe 闭合：父对话只保留一条 subagent 调用与一条 `generate_image` receipt，pretty-printed JSON 仍被语义字段守卫正确识别；provider/attachmentId 血缘完整，真实 PNG 1,074,780 bytes 可回读，没有重复生成或假红 | FRT-05 / managed-write / subagent → image MediaRef / receipt governance | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_SubagentGenerateImageArtifact$' -count=1 -parallel 1 -timeout 15m -v` → PASS 62.132s（PNG 1,074,780 bytes；无 provider secret） | 当前提交 |
| 2026-07-29 | EVO-203 | managed 用户图片→动画融合 reprobe 闭合：输入 PNG 经 `sourceAttachmentId` 绑定到恰好一次 `animate_image`，危险审批与专用异步 route 完成，产出 3,613,659-byte MP4；输入未被重画/丢弃，receipt、后续回合和附件回读完整 | FRT-09 / managed-read/write / image + animate_image / source lineage | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_AnimateImageArtifact$' -count=1 -parallel 1 -timeout 20m -v` → PASS 126.461s（MP4 3,613,659 bytes；未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-204 | 本轮 workflow 多模态、managed writer、subagent、BYOK 跨方言与版本链 reprobe 完成后，主仓跨层总门禁仍闭合：backend、Flutter frontend（全部分组）、docs、web demo 全绿；API Serve 工作树仍 clean | cross-repo / workspace gate / post-live-evolution | 根目录 `make verify` → `✓ backend`、`✓ frontend`、`✓ docs`、`✓ demo`、`✓ workspace verified`；未输出 provider secret | 当前提交 |
| 2026-07-29 | EVO-205 | OpenAI `gpt-audio` 音频+工具续接出现一次真实上游静默：首轮等待 150 秒后产品按设计落 `LLM_PROVIDER_ERROR`（无 stream activity）；同场景立即复核在 12.8 秒内完整通过，普通音频入口隔离探针也通过，故当前证据指向 provider 瞬时 stall 而非产品回归；两次复核均保留在日志，不能把偶发上游红灯抹平 | FRT-02 / BYOK-read / OpenAI audio + tools / transient upstream stall classification | 首轮 `EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAIAudioToolContinuation$' -count=1 -parallel 1 -timeout 8m -v` → FAIL 160.41s，结构化 `LLM_PROVIDER_ERROR`；隔离 `TestLiveBYOK_OpenAIAudioInput` → PASS 8.73s；立即同场景复核 → PASS 12.779s（两次 `/chat/completions`、native `input_audio`、tool/result/text 均成立；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-206 | managed-write 高风险合跑的事实分叉已留证：图像生成/编辑、subagent 图片、直接语音、视频拒绝、视频提交后取消均通过（取消路径即使出现一次 `generate_video` execute warning 仍无孤儿）；语音拒绝和动画文本-only 均在 managed key 开通阶段遇 `api.anselm.website` TLS handshake timeout，视频直接生成在约 157 秒异步等待后落 `LLM_PROVIDER_ERROR (520)`，三条均待生产网关恢复后重测，当前不改产品代码；API Serve 当前源本地门禁独立全绿 | FRT-09 / managed-write / danger + async media / gateway availability | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_(GenerateImageArtifact|EditImageArtifact|GenerateSpeechArtifact|GenerateSpeechDeniedNoSpend|GenerateVideoArtifact|GenerateVideoDeniedNoSpend|GenerateVideoCancelAfterSubmitLeavesNoOrphan|AnimateImageArtifact(TextOnly)?|SubagentGenerateImageArtifact)$' -count=1 -parallel 1 -timeout 45m -v` → 7 PASS、3 FAIL（总计 523.212s）；随后 `TestLiveManaged_GenerateSpeechDeniedNoSpend` 单跑再次在 30s managed-key provision 窗口因 TLS timeout FAIL；无 provider secret 输出；`cd ../Anselm-API-Serve && make verify` → PASS；生产 `GET https://api.anselm.website/v1/health` 连续多次 HTTP 000/TLS timeout | 当前提交 |
| 2026-07-29 | EVO-207 | 生产网关健康故障进一步收窄：同一时间窗 `openssl s_client` 可完成 TLS 1.3 握手且证书验证通过，`curl --http1.1` 也成功发出 `GET /v1/health`，但 20 秒内无任何 HTTP 响应字节；因此 EVO-206 的开通失败不能只归因为客户端 TLS，当前更像 Cloudflare→origin/应用实例响应挂起。API Serve 当前源门禁仍全绿，未改部署仓 | gateway availability / HTTP liveness / deployment boundary | `openssl s_client -connect api.anselm.website:443 -servername api.anselm.website -brief` → handshake/cert PASS；`curl --http1.1 --connect-timeout 8 --max-time 20 https://api.anselm.website/v1/health` → request sent, 0 bytes, timeout；本地 `cd ../Anselm-API-Serve && make verify` → PASS | 当前提交 |
| 2026-07-29 | EVO-208 | 生产网关不可达期间，当前源的完整 backend 黑盒回归仍闭合：workflow/function/handler/MCP、agent/subagent、媒体准备、并发、取消/恢复和资源卫生等静态产品路径均通过；没有把外部网关故障误报成代码回归 | backend regression / gateway outage isolation | `cd backend && make testend` → `ok github.com/sunweilin/anselm/testend/scenarios 363.992s`；未开启 EVALS、未注入 provider secret；主仓/API Serve 工作树 clean | 当前提交 |
| 2026-07-29 | EVO-209 | 生产网关恢复后的三条红灯全部复核通过：managed key 开通、普通聊天先恢复，随后语音危险审批拒绝（无 receipt/无额外 quota）、视频异步生成与动画 text-only 均完成；因此 EVO-206 的 TLS timeout/视频 520 明确归类为网关或上游瞬时故障，未发现稳定产品回归，动画专用 route 与媒体预算降级仍保持原契约 | FRT-09 / managed-write / gateway recovery / danger + async media | 健康探针 `GET https://api.anselm.website/healthz` → HTTP 200 `{"status":"ok"}`；`TestLiveManaged_DefaultChat` → PASS 12.313s；`TestLiveManaged_GenerateSpeechDeniedNoSpend` → PASS 17.332s；`TestLiveManaged_GenerateVideoArtifact` → PASS 105.638s（MP4 3,408,330 bytes）；`TestLiveManaged_AnimateImageArtifactTextOnly` → PASS 123.316s（MP4 7,490,295 bytes）；未输出 provider secret | 当前提交 |
| 2026-07-29 | EVO-210 | OpenAI image-only BYOK 输入边界复核闭合：同轮图片+不支持音频、图片+不支持视频均完成；图片在真实 recorder wire 上保持 native exact part，音频/视频分别变成明确降级注记，原附件仍逐字节可回读，没有把合法图片请求打成混合媒体上游 400 | FRT-02 / BYOK-read / OpenAI / honest multimodal degradation | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAIImageAndUnsupported(Audio|Video)$' -count=1 -parallel 1 -timeout 20m -v` → PASS 17.118s（PNG 98 bytes；WAV 96,044 bytes；MP4 2,969,360 bytes；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-211 | managed 同轮图片+视频融合恢复后 reprobe 闭合：单一对话同时消费 PNG 与 MP4，两个 native media input 均完成，原附件逐字节不变；没有因为网关恢复、目录刷新或近期 writer 迭代引入跨模态串线、文本冒充媒体或整轮失败 | FRT-01 / FRT-02 / managed-read / multimodal fusion recovery | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_DefaultChatWithImageAndVideoAttachments$' -count=1 -parallel 1 -timeout 12m -v` → PASS 49.120s（PNG 98 bytes；MP4 2,969,360 bytes；未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-212 | managed read-aloud 恢复后 reprobe 闭合：同文案的第二次读取命中同一 WAV 缓存且不增加 quota，不同文案生成新的真实音频；两个并发相同请求只合成一次并共享 attachment，quota 仅扣一次，守住了重复付费与浏览器双击边界 | FRT-09 / managed-write / read-aloud cache + concurrent dedup / spend safety | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_ReadAloud(Cache|ConcurrentDedup)$' -count=1 -parallel 1 -timeout 15m -v` → PASS 20.103s（缓存音频 195,884 bytes；两项均完成；未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-213 | managed 多模态诚实降级矩阵恢复后闭合：音频-only、图片+不支持音频、视频+不支持音频均完成；支持媒体 native 保留、不支持音频显式注记，三组原附件逐字节可回读，没有混合输入把整轮打失败 | FRT-01 / FRT-02 / managed-read / honest multimodal degradation | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_DefaultChatWith(AudioAttachmentDegrades|ImageAndUnsupportedAudio|VideoAndUnsupportedAudio)$' -count=1 -parallel 1 -timeout 20m -v` → PASS 72.792s（PNG 98 bytes；MP4 2,969,360 bytes；WAV 96,044 bytes；未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-214 | provider 行为恢复后批次闭合：Qwen chat-only agent 拒绝、DeepSeek compat 文本/工具、Google 文本、OpenAI `gpt-audio` 原生 WAV→工具续接均通过；Google native tool 样本命中真实上游 429，产品落结构化 `LLM_RATE_LIMITED` 后按测试契约 skip，未伪造成功也未误报稳定回归 | FRT-10 / FRT-11 / BYOK-read / provider behavior + rate classification | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_(TextProviderSmoke|DeepSeekToolContinuation|GoogleToolContinuation|OpenAIAudioToolContinuation|QwenChatOnlyAgentRejected)$' -count=1 -parallel 1 -timeout 25m -v` → PASS 41.230s（4 项通过；Google tool 429→`LLM_RATE_LIMITED` skip；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-215 | BYOK 历史投影恢复后闭合：OpenAI 多图同轮只发一条上游请求且保留两个 native image parts；Qwen→OpenAI 模型切换后既有 PNG/MP4 history 仍按对应 native media 重投影，原附件逐字节不变，没有退化成 URL/prompt 或重复上传 | FRT-02 / BYOK-read / OpenAI + Qwen / multiple images + model switch history | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_(ModelSwitchReprojectsHistoryMedia|OpenAIMultipleImages)$' -count=1 -parallel 1 -timeout 15m -v` → PASS 24.751s（PNG 98 bytes；MP4 2,969,360 bytes；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-216 | managed producer→Google native viewer workflow 留下一条真实外部红灯：上游 `generate_image` 已恰一次产出 1,089,789-byte PNG 并写入 receipt/MediaRef，但 Google viewer 节点落 `LLM_RATE_LIMITED`（HTTP 429），flowrun 因此 failed；API Serve 没有修改，不能把 provider rate window 误归为媒体血缘或 workflow 代码回归，待 rate window 后复核 | FRT-04 / hybrid / workflow / managed image → Google native viewer / external rate window | `set -a; source ../.env; set +e; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedImageToGoogleViewer$' -count=1 -parallel 1 -timeout 15m -v` → FAIL 49.870s（painter completed；viewer `llm: rate limited (429)`；producer receipt/PNG 仍可回读；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-217 | 生产 voice 资源链恢复后闭合：ASR WebSocket 的 PCM→finish→`session.finished` 完成；参考音频→危险审批→异步 enroll→克隆音色合成→删除全链路完成，删除后 voice 列表无残留，音频附件仍可回读，未改 API Serve | FRT-09 / managed voice / ASR + voice identity lifecycle | `EVALS_VOICE=1 go test ./scenarios -run '^TestLiveVoice_(SpeechInputASR|EnrollSpeakDelete)$' -count=1 -parallel 1 -timeout 15m -v` → PASS（ASR 6.881s；enroll/speak/delete 46.061s；音频 357,164/337,964 bytes；总 52.942s；未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-218 | Google viewer 外部限流连续复核仍红：间隔约两分钟的第二次 managed producer→Google native viewer workflow 中，painter 再次恰一次成功产出 1,089,950-byte PNG，但 viewer 仍落同一 `LLM_RATE_LIMITED` 429；两次失败点、状态与 producer 成果一致，证据足以将其归为 provider rate window，Google viewer 仍待窗口恢复后重测 | FRT-04 / hybrid / workflow / Google viewer / repeated external rate window | 同一命令第二次 `EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedImageToGoogleViewer$' -count=1 -parallel 1 -timeout 15m -v` → FAIL 49.502s（painter completed；viewer 429；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-219 | Qwen BYOK 多模态组合恢复后闭合：图片+视频在兼容 wire 上同时保留 native parts；图片+音频按 Omni 单一媒体契约保留图片并显式降级音频，三份原附件均逐字节可回读，没有把 provider 400 泄漏成产品错误 | FRT-02 / BYOK-read / Qwen / multimodal fusion + honest degradation | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_Qwen(ImageAndVideoFusion|ImageAndAudioFusion)$' -count=1 -parallel 1 -timeout 15m -v` → PASS 19.684s（PNG 98 bytes；MP4 2,969,360 bytes；WAV 96,044 bytes；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-220 | 连续 live 复核与日志提交后，主仓跨层总门禁仍闭合：backend、Flutter frontend 全部测试分组、docs、web demo 与 workspace 检查均通过；未改产品源码，API Serve 工作树保持 clean | cross-repo / workspace gate / post-live-evolution | 根目录 `make verify` → `✓ backend`、`✓ frontend`、`✓ docs`、`✓ demo`、`✓ workspace verified`；未输出 provider secret | 当前提交 |
| 2026-07-29 | EVO-221 | API Serve 网关仓独立门禁恢复后仍闭合：Go vet/build、全量 race 单测、integration e2e、golangci-lint 与 docs lint 均通过；没有因为生产网关此前短暂不可达而留下源代码或部署仓漂移 | API Serve / gateway gate / post-live-evolution | `cd ../Anselm-API-Serve && make verify` → race 全包通过、`internal/e2e` 7.093s、golangci-lint `0 issues`、docs lint `35 file(s) ok, 0 warning(s)`；API Serve 工作树 clean | 当前提交 |
| 2026-07-29 | EVO-222 | Google provider 状态探针再次确认全局 rate window：BYOK key probe 与 capability 目录均正常，但独立 Gemini vision 图片输入真实调用也返回 429；测试按产品契约落 `LLM_RATE_LIMITED` 后 skip，说明 EVO-216/218 的 workflow viewer 红灯来自 Google 上游限流而非 MediaRef/native inlineData 注入 | FRT-02 / BYOK-read / Google / provider-wide rate classification | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_GoogleImageInput$' -count=1 -parallel 1 -timeout 10m -v` → PASS 6.149s（真实调用 429→structured skip；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-223 | workflow 三类 producer→OpenAI viewer 恢复后 reprobe 闭合：function、驻留 handler、stdio MCP 各铸造独立 MediaRef，真实 OpenAI 下游按对应 exact image bytes 消费，flowrun 全 completed；未出现 receipt 文本冒充媒体、跨产地串线或重复调用 | FRT-04 / FRT-05 / hybrid / workflow / function + handler + MCP → OpenAI BYOK-read | `set -a; source ../.env; set +e; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManaged(Function|Handler|MCP)ToOpenAIViewer$' -count=1 -parallel 1 -timeout 20m -v` → PASS 57.758s（PNG 98/196 bytes；三项均完成；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-224 | managed 异步视频成本闸复核留下可区分的瞬时红灯：同批首次运行中，`GenerateVideoDeniedNoSpend` 在 60 秒内未生成 danger interaction 而失败，随后隔离重跑 29.14s 正常出现审批并 deny，回合完成且无视频 receipt/额外 generation reservation；同批 `GenerateVideoCancelAfterSubmitLeavesNoOrphan` 13.12s 通过，批准并提交后本地 cancelled、无晚到视频 receipt/附件。失败点在审批前、隔离复核恢复，暂归受管模型/上游 stall，不改产品代码 | FRT-09 / managed-write / async video / danger + cancellation + spend safety / transient stall classification | 合跑 `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_GenerateVideo(DeniedNoSpend|CancelAfterSubmitLeavesNoOrphan)$' -count=1 -parallel 1 -timeout 12m -v` → 首项未在 60s 产生 interaction、次项 PASS 13.12s、合计 FAIL 139.076s；隔离 `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_GenerateVideoDeniedNoSpend$' -count=1 -parallel 1 -timeout 6m -v` → PASS 29.14s；网关 `/healthz` HTTP 200；未输出 provider secret | 当前提交 |
| 2026-07-29 | EVO-225 | Google native viewer 的第三次窗口复核仍红，但 producer 侧稳定闭合：受管 painter 恰一次完成并落 1,075,685-byte PNG，独立 Google viewer 节点才落 `llm: rate limited (429)`，flowrun failed；Google key probe 与目录刷新均正常，故持续限流仍在 provider 数据面，不是 MediaRef、inlineData 或 workflow join 回归，暂不改产品代码 | FRT-04 / hybrid / managed image → Google native viewer / sustained external rate window | `set -a; source ../.env; set +a; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedImageToGoogleViewer$' -count=1 -parallel 1 -timeout 15m -v` → FAIL 45.48s（painter completed；viewer 429；PNG 1,075,685 bytes；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-226 | gateway 恢复后的受管语音→Qwen workflow reprobe 闭合：`generate_speech` 恰一次产出 80,684-byte WAV，flowrun 下游 Qwen viewer 成功消费，flowrun completed，附件逐字节可回读；没有 receipt 文本退化或重复合成 | FRT-04 / hybrid / workflow / managed-write speech → Qwen BYOK-read audio viewer / recovery reprobe | `set -a; source ../.env; set +a; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedSpeechToQwenViewer$' -count=1 -parallel 1 -timeout 20m -v` → PASS 22.73s（WAV 80,684 bytes；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-227 | gateway 恢复后的受管异步视频→Qwen workflow reprobe 闭合：视频提交、异步等待与下游 Qwen viewer 均完成，同一 flowrun 终态 completed，6,624,564-byte MP4 附件逐字节可回读；没有重复提交、晚到孤儿或 receipt 文本冒充 native video | FRT-04 / hybrid / workflow / managed-write video → Qwen BYOK-read video viewer / async recovery | `set -a; source ../.env; set +a; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedVideoToQwenViewer$' -count=1 -parallel 1 -timeout 25m -v` → PASS 141.44s（MP4 6,624,564 bytes；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-228 | gateway 恢复后的 managed image→OpenAI viewer workflow reprobe 闭合：生成者只铸一次真实 PNG，独立 OpenAI 下游在同一 flowrun 完成消费，1,096,184-byte 附件逐字节可回读；没有 receipt 文本冒充图片或跨节点丢失 | FRT-04 / hybrid / workflow / managed image → OpenAI BYOK-read vision viewer / recovery reprobe | `set -a; source ../.env; set +a; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedImageToOpenAIViewer$' -count=1 -parallel 1 -timeout 15m -v` → PASS 43.63s（PNG 1,096,184 bytes；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-229 | 图片/语音/视频 workflow recovery reprobe 与限流分类落盘后，主仓 backend 与部署网关的静态共同层仍闭合：没有 live 验收扩展、文档状态修正或媒体事实记录造成装配回归 | cross-repo gate / backend + API Serve / post-live-evolution | `make verify`（backend）→ `✓ backend verified`；`cd ../Anselm-API-Serve && make verify` → vet/build、race、integration e2e 7.651s、golangci-lint `0 issues`、docs lint `0 warning`；未输出 provider secret | 当前提交 |
| 2026-07-29 | EVO-230 | gateway 恢复后的 managed 图像编辑源血缘复核闭合：同回合先生成原图、再恰一次 `edit_image`，原/编辑 PNG 分别为 1,093,111 与 1,673,474 bytes，`sourceAttachmentId`/provider receipt 与两件附件回读均成立；没有把编辑重试成重画或 receipt 文本 | FRT-09 / managed-write / generate_image → edit_image / MediaRef lineage / recovery reprobe | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_EditImageArtifact$' -count=1 -parallel 1 -timeout 20m -v` → PASS 109.06s（PNG 1,093,111/1,673,474 bytes；未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-231 | gateway 恢复后的 managed 图片→动画异步写路径闭合：真实输入 PNG 经 `sourceAttachmentId` 绑定到恰一次 `animate_image`，专用动画 route/审批/异步等待完成并产出 6,232,841-byte MP4；输入图未被重画或丢弃，receipt、后续回合和附件回读完整 | FRT-09 / managed-read/write / image + animate_image / source lineage / recovery reprobe | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_AnimateImageArtifact$' -count=1 -parallel 1 -timeout 25m -v` → PASS 139.83s（MP4 6,232,841 bytes；未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-232 | gateway 恢复后的 managed 直接语音写路径闭合：默认模型恰调用一次 `generate_speech`，危险/工具回合续接完成，receipt 标记 `provider=anselm` 并绑定真实 80,684-byte RIFF/WAVE，附件逐字节可回读且无重复合成 | FRT-09 / managed-write / generate_speech + MediaRef / recovery reprobe | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_GenerateSpeechArtifact$' -count=1 -parallel 1 -timeout 12m -v` → PASS 14.66s（WAV 80,684 bytes；未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-233 | managed subagent 图片复核再次暴露模型工具参数质量信号：两次连续真实回合都先传非法 `subagent_type`，服务按 schema 拒绝并记录 warning，随后父回合恢复，子 agent 恰生成一次 `generate_image`，receipt/provider/attachment 血缘与 PNG 回读均正确；当前不是 durable 产品失败，但应持续关注“隐形重试/用户是否看见失败 trace” | FRT-05 / managed-write / subagent / tool-schema recovery / nondeterministic model output | 首次 `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_SubagentGenerateImageArtifact$' -count=1 -parallel 1 -timeout 15m -v` → PASS 49.73s（warning `subagent_type must be one of [Explore Plan general-purpose]`；PNG 1,110,662 bytes）；立即复跑 → PASS 53.34s（同一 warning；PNG 1,144,621 bytes）；两次均恰一条 managed image receipt、无 provider secret | 当前提交 |
| 2026-07-29 | EVO-234 | gateway 恢复后的 managed 直接图像写路径闭合：默认模型恰调用一次 `generate_image`，receipt 标记 `provider=anselm`，真实 1,067,904-byte PNG 可回读并继续同一回合；两步上限守住重复付费边界 | FRT-03 / FRT-09 / managed-write / generate_image + MediaRef / recovery reprobe | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_GenerateImageArtifact$' -count=1 -parallel 1 -timeout 12m -v` → PASS 35.40s（PNG 1,067,904 bytes；未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-235 | gateway 恢复后的混合 ownership 复核闭合：OpenAI BYOK planner 只调度一次 Anselm managed `generate_image`，receipt/provider 与 1,133,949-byte PNG 回读完整；没有把写入责任错误交给 BYOK、重复生成或把产物退化成文本 | FRT-03 / hybrid / OpenAI planner → managed image writer / recovery reprobe | `set -a; source ../.env; set +a; EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_OpenAIPlansManagedImage$' -count=1 -parallel 1 -timeout 15m -v` → PASS 17.10s（PNG 1,133,949 bytes；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-236 | BYOK OpenAI 文档媒体入口 recovery reprobe 闭合：Markdown `anselm://media` 图片引用经文档扫描进入真实下游 native image wire，98-byte 附件逐字节回读；BYOK harness 预期跳过 managed install，未发生 gateway fallback 或 prompt URL 退化 | FRT-06 / byok-read / document media reference / OpenAI recovery reprobe | `set -a; source ../.env; set +a; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAIDocumentImageReference$' -count=1 -parallel 1 -timeout 10m -v` → PASS 8.267s（PNG 98 bytes；free-tier install skipped by BYOK harness；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-237 | 本轮 managed/hybrid/BYOK live reprobe、Google 限流分类、subagent schema warning 观察与文档状态修正之后，完整 backend 黑盒共同回归仍闭合；chat、agent/subagent、workflow/trigger、MCP/function/handler、媒体、取消/重试/崩溃恢复、资源卫生均通过，未开启任何真钱 EVALS | full backend testend regression / post-live-evolution gate | `make testend`（backend）→ `ok github.com/sunweilin/anselm/testend/scenarios 314.237s`；未注入 provider secret、未启用 EVALS | 当前提交 |
| 2026-07-29 | EVO-238 | Google 限流范围进一步收窄：独立 Gemini native 98-byte PNG 输入已恢复通过，但同一时间完整 managed image→Google workflow 仍由 viewer 的约 1.09MB `inlineData` 请求落 `429`，painter 恰一次完成且 MediaRef/附件完整；因此不能再称“全局 key 不可用”，当前更像大媒体 payload/请求形状或该上游 route 的限流，仍不是 encoder/producer 回归 | FRT-02 / FRT-04 / Google native / provider-rate classification / large inlineData | 直输 `EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_GoogleImageInput$' -count=1 -parallel 1 -timeout 10m -v` → PASS 7.53s（PNG 98 bytes）；workflow `EVALS_HYBRID=1 EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveHybrid_WorkflowManagedImageToGoogleViewer$' -count=1 -parallel 1 -timeout 15m -v` → FAIL 37.25s（painter receipt/PNG 1,092,485 bytes；viewer `LLM_RATE_LIMITED` 429；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-239 | Google 限流边界继续分化：独立 vision 小图已通过，但同一 key 的原生 functionCall/functionResponse 小请求仍落 429 并按结构化 `LLM_RATE_LIMITED` skip；这使当前待恢复项更像工具/续接 route 或账号配额窗口，而非全局认证、目录或所有 Gemini 数据面不可用 | FRT-11 / byok-read / Google native tools / rate classification | `set -a; source ../.env; set +a; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_GoogleToolContinuation$' -count=1 -parallel 1 -timeout 10m -v` → SKIP 8.94s（真实上游 429→`LLM_RATE_LIMITED`；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-240 | Google provider 限流没有污染默认产品路径：新 workspace 的 managed key 开通/probe、`anselm-auto` 普通中文 chat 与回合终态均通过，默认 Anselm API 仍可用 | FRT-01 / managed-read/default / gateway health isolation | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_DefaultChat$' -count=1 -parallel 1 -timeout 8m -v` → PASS 8.351s（managed provision/probe/chat 完成；未输出 provider secret） | 当前提交 |
| 2026-07-29 | EVO-241 | Qwen OpenAI-compatible 直读视频 recovery reprobe 闭合：真实 key probe/能力投影通过，2,969,360-byte MP4 在下游 `video_url` wire 中保持，回合完成且附件逐字节回读；与此前 workflow Qwen viewer 结果一致 | FRT-02 / byok-read / Qwen / video input / recovery reprobe | `set -a; source ../.env; set +a; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_QwenVideoInput$' -count=1 -parallel 1 -timeout 12m -v` → PASS 23.50s（MP4 2,969,360 bytes；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-242 | Google 模型资格漂移验收的外部限流假红已收口：首发目录可见但账号不可用模型仍严格只调用一次并落 `LLM_MODEL_NOT_FOUND (404)`、无 assistant 文本；切换有效模型的恢复发送若遇结构化 429 现在明确 skip，不再把 provider window 误报成产品恢复失败；不放宽 stale-model 断言 | FRT-14 / byok-read / Google model qualification / test governance / rate classification | 修复前两次真实重跑均 FAIL：首发 404 正确，恢复回合 `LLM_RATE_LIMITED (429)`；`testend/scenarios/live_managed_test.go` recovery 分支加入仅针对 `LLM_RATE_LIMITED` 的日志+skip；修复后同命令 → SKIP 6.00s（404 断言仍通过，429 明确记录；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-243 | provider 状态抽样继续分化：DeepSeek OpenAI-compatible 文本 smoke 通过；Google 同 key 的文本 smoke 仍真实落 429 并结构化 skip。结合 vision 小图通过、工具/恢复/大媒体失败，当前证据支持 Google 特定 route/配额窗口，而不是产品目录、认证或通用 Gemini 编码回归 | FRT-11 / byok-read / DeepSeek compat + Google native / rate classification | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_TextProviderSmoke$' -count=1 -parallel 1 -timeout 12m -v` → PASS 10.10s（DeepSeek 8.19s PASS；Google 1.90s structured `LLM_RATE_LIMITED` SKIP；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-244 | OpenAI 原生 PDF 读路径闭合：真实 `gpt-4.1-mini` 能力投影声明 `nativeDocs` 后，PDF 经 Anselm 附件上传、对话消费并逐字节回读；recorder 证明上游收到 exact `file`/`file_data` inline PDF，而不是抽取文本或 URL 占位，BYOK 路径未 fallback 到 managed | FRT-02 / byok-read / OpenAI native document | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_OpenAIPDFInput$' -count=1 -parallel 1 -timeout 12m -v` → PASS 8.47s（PDF 540 bytes；native `file_data` wire；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-245 | 受管默认 PDF 抽取路径闭合：`anselm-auto` 明确不宣称 `nativeDocs`，540-byte PDF 经 Anselm 附件上传后在 sandbox 抽取，真实受管对话准确返回唯一 token，原始附件仍逐字节可读；首跑发现并收紧了测试 token 断言，未发现产品失败 | FRT-01 / managed-read/default / PDF sandbox extraction | `set -a; source ../.env; set +a; EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_DefaultChatWithPDFAttachment$' -count=1 -parallel 1 -timeout 12m -v` → 首次断言过严（回答只返回 token 后缀），调整为单一 token 后 PASS 12.88s（PDF 540 bytes；无 provider secret） | 当前提交 |
| 2026-07-29 | EVO-246 | 原生 PDF 验收新增、受管 PDF 抽取断言修正后，完整 backend 黑盒回归仍闭合；chat、agent/subagent、workflow/trigger、MCP/function/handler、媒体、多模态、取消/重试/崩溃恢复、资源卫生与新 live 场景共同通过，未启用 EVALS/provider secret | full backend testend regression / post-PDF evolution gate | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 375.428s`；无 provider secret、EVALS 未开启 | 当前提交 |
| 2026-07-29 | EVO-247 | PDF 受管/BYOK 验收与 backend 黑盒回归之后，主仓跨层总门禁仍闭合：backend、Flutter frontend、docs、web demo 全绿；没有改动 API Serve，未输出 provider secret | cross-repo gate / workspace verification / post-PDF evolution | `make verify` → `✓ backend`、`✓ frontend`、`✓ docs`、`✓ demo`、`✓ workspace verified` | 当前提交 |
| 2026-07-29 | EVO-248 | 部署网关仓独立门禁复核闭合：Go vet/build、全量 race 单测、integration e2e、golangci-lint 与 35 份 docs lint 全绿；API Serve 工作树保持 clean，本轮没有改动部署仓代码 | API Serve / gateway gate / post-PDF evolution | `cd ../Anselm-API-Serve && make verify` → integration e2e 7.177s、golangci-lint `0 issues`、docs lint `35 file(s) ok, 0 warning(s)` | 当前提交 |
| 2026-07-29 | EVO-249 | Google 独立 98-byte Gemini vision probe 在 21:20 再次落 429；key probe、能力目录、Anselm 本地 attachment/route 均正常，测试按结构化 `LLM_RATE_LIMITED` skip。与 EVO-238/239/243 一致，外部限流仍持续，未改产品或 encoder | FRT-02 / FRT-11 / Google native / persistent provider rate window | `set -a; source ../.env; set +e; EVALS_BYOK=1 go test ./scenarios -run '^TestLiveBYOK_GoogleImageInput$' -count=1 -parallel 1 -timeout 10m -v` → SKIP 5.68s（真实 429→`LLM_RATE_LIMITED`；key 未输出） | 当前提交 |
| 2026-07-29 | EVO-250 | 受管 PDF 的工具媒介路径闭合：真实 `anselm-auto` 调用 `read_attachment`，sandbox extractor 将 539-byte PDF 变为 tool result，父回合继续并只返回唯一 token；原始附件逐字节不变，未发生工具错误或额外生成 | FRT-01 / managed-read/default / read_attachment + PDF extraction | `set -a; source ../.env; set +a; EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_ReadAttachmentPDF$' -count=1 -parallel 1 -timeout 12m -v` → PASS 22.54s（tool_call/tool_result/assistant continuation；无 provider secret） | 当前提交 |
| 2026-07-29 | EVO-251 | 新增 managed `read_attachment` PDF live 场景后，完整 backend 黑盒回归仍闭合；chat、agent/subagent、workflow/trigger、MCP/function/handler、附件抽取、多模态、取消/重试/崩溃恢复、资源卫生共同通过，未启用 EVALS/provider secret | full backend testend regression / post-read-attachment evolution gate | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 319.540s`；无 provider secret、EVALS 未开启 | 当前提交 |
| 2026-07-29 | EVO-252 | 受管 `inspect_media` 嵌套视觉路径闭合：真实 `anselm-auto` 对 32×32 PNG 调用工具，内部 vision 请求经部署网关返回 `managed-url` bounded JSON evidence，父回合继续完成，原图 98 bytes 不变；首跑只因测试从 content 取工具名而假红，修正为 `attrs.tool` 后确认产品无故障 | FRT-01 / managed-read/default / inspect_media nested vision | 首次 `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_InspectMediaImage$'`：真实 tool_result/assistant 已成功但断言假红；修正后同命令 → PASS 16.71s（evidence 含 attachmentId/mime/width/height/answer；无 provider secret） | 当前提交 |
| 2026-07-29 | EVO-253 | 新增 managed `inspect_media` live 场景与断言修正后，完整 backend 黑盒回归仍闭合；chat、agent/subagent、workflow/trigger、MCP/function/handler、PDF/附件、多模态、取消/重试/崩溃恢复、资源卫生共同通过，未启用 EVALS/provider secret | full backend testend regression / post-inspect-media evolution gate | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 337.306s`；无 provider secret、EVALS 未开启 | 当前提交 |
| 2026-07-29 | EVO-254 | 受管视频 `inspect_media` 元数据路径闭合：真实 2,969,360-byte MP4 通过工具返回带 `kind=video`/usage 的 bounded local capsule，父回合明确说明未做转录，原始视频逐字节不变；无额外 provider 感知调用 | FRT-01 / managed-read/default / inspect_media temporal metadata | `set -a; source ../.env; set +a; EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_InspectMediaVideo$' -count=1 -parallel 1 -timeout 15m -v` → PASS 18.34s（无 provider secret） | 当前提交 |

| 2026-07-29 | EVO-255 | 新增 managed 视频 `inspect_media` 场景后，完整 backend 黑盒回归仍闭合；chat、agent/subagent、workflow/trigger、MCP/function/handler、PDF/附件工具、多模态、取消/重试/崩溃恢复、资源卫生共同通过，未启用 EVALS/provider secret | full backend testend regression / post-temporal-inspect evolution gate | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 320.217s`；无 provider secret、EVALS 未开启 | 当前提交 |
| 2026-07-29 | EVO-256 | 受管音频 `inspect_media` 元数据路径闭合：真实 96,044-byte WAV 通过工具返回带 `kind=audio`/usage 的 bounded local capsule，父回合明确说明未做转录，原始音频逐字节不变；普通 chat audio 能力仍未被误宣称 | FRT-01 / managed-read/default / inspect_media audio metadata | `set -a; source ../.env; set +e; EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_InspectMediaAudio$' -count=1 -parallel 1 -timeout 12m -v` → PASS 15.72s（无 provider secret） | 当前提交 |

| 2026-07-29 | EVO-257 | 新增 managed 音频 `inspect_media` 场景后，完整 backend 黑盒回归仍闭合；chat、agent/subagent、workflow/trigger、MCP/function/handler、PDF/附件工具、多模态、取消/重试/崩溃恢复、资源卫生共同通过，未启用 EVALS/provider secret | full backend testend regression / post-audio-inspect evolution gate | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 362.409s`；无 provider secret、EVALS 未开启 | 当前提交 |
| 2026-07-29 | EVO-258 | 受管附件发现路径闭合：真实 workspace 上传独立 TXT 与 PNG 后，`anselm-auto` 恰调用一次 `list_attachments`，tool result 返回两件附件的 id/filename/count，父回合继续完成；随后两份源字节均逐字节不变，没有被隐式读取或改写 | FRT-01 / managed-read/default / list_attachments discovery | `set -a; source ../.env; set +e; EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_ListAttachments$' -count=1 -parallel 1 -timeout 12m -v` → PASS 11.540s（TXT 26 bytes、PNG 98 bytes；无 provider secret） | 当前提交 |
| 2026-07-29 | EVO-259 | 新增 managed `list_attachments` 场景后，完整 backend 黑盒回归仍闭合；chat、agent/subagent、workflow/trigger、MCP/function/handler、PDF/附件发现与读取、多模态、取消/重试/崩溃恢复、资源卫生共同通过，未启用 EVALS/provider secret | full backend testend regression / post-list-attachments evolution gate | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 328.342s`；无 provider secret、EVALS 未开启 | 当前提交 |
| 2026-07-29 | EVO-260 | 真实大文本验收先抓到两处共同层缺陷：`search_tools("read attachment search")` 会把 `read_attachment` 排出前五，且 lazy 概览只在前 180 字暴露 `id`，模型无法得知 query/contextChars/maxMatches；修复工具名分词+精确名加权、概览前缀参数说明后，136,890-char 文本的直接 managed query 通过，返回 bounded 唯一 token/totalChars，源字节不变 | FRT-01 / managed-read/default / lazy discovery + attachment query | 首跑 `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_ReadAttachmentLargeTextQuery$'` → FAIL（search 结果漏 `read_attachment`，随后只拿到 8,000-char index）；修复 `search.go` rank、`read.go` overview 后同命令 → PASS 22.133s（query result <5,000 chars；无 provider secret）；单元 `go test ./internal/app/tool/toolset -run 'TestRankLazy|TestSearchTools_Execute_Matches'` → PASS | 当前提交 |
| 2026-07-29 | EVO-261 | lazy 检索加权与附件 query 概览修复后，完整 backend 黑盒共同回归仍闭合；chat、agent/subagent、workflow/trigger、MCP/function/handler、附件发现/抽取/大文本 bounded query、多模态、取消/重试/崩溃恢复、资源卫生共同通过，未启用 EVALS/provider secret | full backend testend regression / post-attachment-query-fix gate | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 338.150s`；无 provider secret、EVALS 未开启 | 当前提交 |
| 2026-07-29 | EVO-262 | 附件发现/大文本 query 修复后，主仓跨层总门禁仍闭合；backend、Flutter frontend、docs、web demo 全绿，工作区装配无回归，API Serve 工作树未被触碰 | cross-repo gate / workspace verification / post-attachment-query-fix | 根目录 `make verify` → `✓ backend`、`✓ frontend`、`✓ docs`、`✓ demo`、`✓ workspace verified`；未输出 provider secret | 当前提交 |
| 2026-07-29 | EVO-263 | 主仓修复后，部署网关仓独立门禁仍闭合：Go vet/build、全量 race 单测、integration e2e、golangci-lint 与 docs lint 全绿；API Serve 工作树保持 clean，本轮没有改动部署仓代码 | API Serve / gateway gate / post-attachment-query-fix | `cd ../Anselm-API-Serve && make verify` → integration e2e 7.837s、golangci-lint `0 issues`、docs lint `35 file(s) ok, 0 warning(s)` | 当前提交 |
| 2026-07-29 | EVO-264 | `inspect_media` 文本/文档 bounded query 路径闭合：真实 91,540-char TXT 由 managed 模型调用工具并指定唯一 query，tool result 返回 `mode=query` 的紧凑证据，父回合继续完成且源字节逐字节不变；前置概览已明确 query/contextChars/maxMatches/page/offset/limitChars，未再出现只暴露必填参数的误导 | FRT-01 / managed-read/default / inspect_media textual query | `set -a; source ../.env; set +e; EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_InspectMediaTextQuery$' -count=1 -parallel 1 -timeout 12m -v` → PASS 29.288s（tool result <5,000 chars；无 provider secret） | 当前提交 |
| 2026-07-29 | EVO-265 | 新增 managed `inspect_media` 文本 query 场景后，完整 backend 黑盒回归仍闭合；chat、agent/subagent、workflow/trigger、MCP/function/handler、附件发现/抽取/大文本 bounded query、多模态、取消/重试/崩溃恢复、资源卫生共同通过，未启用 EVALS/provider secret | full backend testend regression / post-inspect-text-query evolution gate | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 311.771s`；无 provider secret、EVALS 未开启 | 当前提交 |
| 2026-07-29 | EVO-266 | `inspect_media` 文本 query 概览/场景修复后，主仓跨层总门禁仍闭合；backend、Flutter frontend、docs、web demo 全绿，工作区装配无回归，API Serve 工作树未被触碰 | cross-repo gate / workspace verification / post-inspect-text-query | 根目录 `make verify` → `✓ backend`、`✓ frontend`、`✓ docs`、`✓ demo`、`✓ workspace verified`；未输出 provider secret | 当前提交 |
| 2026-07-29 | EVO-267 | 大图 `inspect_media` tiles 路径首跑暴露概览缺参/类型误导：模型把 `tiles/tileRows/tileCols` 当字符串，首次调用被校验拒绝后退回默认 2×2；将图片 tiles 参数及 boolean/integer 类型前置到 180 字概览后，真实 512×512 PNG 按要求返回 2×3 tile map，未触发 vision，父回合完成且源字节不变 | FRT-01 / managed-read/default / inspect_media image tiles | 首跑 `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_InspectMediaImageTiles$'` → FAIL（string→int 校验错误，随后默认 2×2）；修复 `inspect.go` 概览后同命令 → PASS 19.378s（6 tiles；无 provider secret） | 当前提交 |
| 2026-07-29 | EVO-268 | 新增 managed 图片 `inspect_media` tiles 场景与概览修复后，完整 backend 黑盒回归仍闭合；chat、agent/subagent、workflow/trigger、MCP/function/handler、附件发现/抽取/大文本 bounded query、多模态、媒体 tiles、取消/重试/崩溃恢复、资源卫生共同通过，未启用 EVALS/provider secret | full backend testend regression / post-image-tiles evolution gate | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 379.039s`；无 provider secret、EVALS 未开启 | 当前提交 |
| 2026-07-29 | EVO-269 | 图片 tiles 概览/场景修复后的主仓跨层总门禁仍闭合；backend、Flutter frontend、docs、web demo 全绿，工作区装配无回归，API Serve 工作树未被触碰 | cross-repo gate / workspace verification / post-image-tiles | 根目录 `make verify` → `✓ backend`、`✓ frontend`、`✓ docs`、`✓ demo`、`✓ workspace verified`；未输出 provider secret | 当前提交 |
| 2026-07-29 | EVO-270 | 托管 `inspect_media` 视频时间范围真实闭合：模型调用工具时传入 `startMs=1000/endMs=2000`，返回 `kind=video`、`mode=metadata` 的本地 capsule 并保留范围，父回合继续完成；源 MP4 字节保持不变 | FRT-01 / managed-read/default / inspect_media temporal range | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_InspectMediaVideoTimeRange$' -count=1 -parallel 1 -timeout 12m -v` → PASS 26.845s；无 provider secret | 当前提交 |
| 2026-07-29 | EVO-271 | 新增 managed 视频时间范围场景后的完整 backend 黑盒回归仍闭合；chat、agent/subagent、workflow/trigger、MCP/function/handler、附件发现/抽取/大文本 bounded query、多模态、媒体 inspect（含 tiles/query/time range）、取消/重试/崩溃恢复、资源卫生共同通过，未启用 EVALS/provider secret | full backend testend regression / post-video-time-range evolution gate | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 380.497s`；无 provider secret、EVALS 未开启 | 当前提交 |
| 2026-07-29 | EVO-272 | 视频时间范围场景与 backend 回归后的主仓跨层总门禁仍闭合；backend、Flutter frontend、docs、web demo 全绿，工作区装配无回归，API Serve 工作树未被触碰 | cross-repo gate / workspace verification / post-video-time-range | 根目录 `make verify` → `✓ backend`、`✓ frontend`、`✓ docs`、`✓ demo`、`✓ workspace verified`；未输出 provider secret | 当前提交 |
| 2026-07-29 | EVO-273 | 托管 `inspect_media` 音频时间范围真实闭合：模型调用工具时传入 `startMs=1200/endMs=2600`，返回 `kind=audio`、`mode=metadata` 的本地 capsule 并保留范围，父回合继续完成；源 WAV 字节保持不变 | FRT-01 / managed-read/default / inspect_media temporal range | `EVALS_MANAGED=1 go test ./scenarios -run '^TestLiveManaged_InspectMediaAudioTimeRange$' -count=1 -parallel 1 -timeout 12m -v` → PASS 26.177s；无 provider secret | 当前提交 |
| 2026-07-29 | EVO-274 | 新增 managed 音频时间范围场景后的完整 backend 黑盒回归仍闭合；chat、agent/subagent、workflow/trigger、MCP/function/handler、附件发现/抽取/大文本 bounded query、多模态、媒体 inspect（含 tiles/query/audio-video ranges）、取消/重试/崩溃恢复、资源卫生共同通过，未启用 EVALS/provider secret | full backend testend regression / post-audio-time-range evolution gate | `make -C backend testend` → `ok github.com/sunweilin/anselm/testend/scenarios 352.658s`；无 provider secret、EVALS 未开启 | 当前提交 |
| 2026-07-29 | EVO-275 | 音视频时间范围场景与 backend 回归后的主仓跨层总门禁仍闭合；backend、Flutter frontend、docs、web demo 全绿，工作区装配无回归，API Serve 工作树未被触碰 | cross-repo gate / workspace verification / post-audio-time-range | 根目录 `make verify` → `✓ backend`、`✓ frontend`、`✓ docs`、`✓ demo`、`✓ workspace verified`；未输出 provider secret | 当前提交 |

## 追加格式

`日期 | EVO-编号 | 一句事实与用户影响 | 共同层/执行面 | 最小可复现或测试 | commit / reference`

若某项被推翻，在原行之后新增一行说明推翻条件；不要回写历史事实。
