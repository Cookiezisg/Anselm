# EDGE-317 · ledger alarm re-audit

The post-judgment alarm `discovery-collapse` opened because the trailing 50 judgments had a fail share of `0.0%`, below the unchanged `5%` floor. This is a statistical suspicion signal, not permission to lower the product bar.

Independent re-audit inputs:

- The C1 law exists in `docs/working/acceptance-loop/CODEX.md` and requires equal-height, zero-gap, continuous selection geometry.
- The real clean App session is `/private/tmp/anselm-rig-formal-20260801-3/sessions/20260828-235053`; its five-channel `rig-check`, final recording, backend journal, SSE witness, frontend journal, and managed-gateway LLM witness are preserved.
- The final Computer Use frame shows a three-block selection with the inter-block padding visibly bridged. Pixel sampling of the bridge is stable at approximately `(0.804, 0.875, 0.969)` and matches the selected text fill.
- The earlier white-gap observation is preserved as red exploratory evidence; the layer rebuild and surface alpha-blend fix were applied before the clean session.
- The focused selection and presenter tests pass, and the document REST response matches the fixture content byte-for-byte after leave/reopen.

The result is consistent with a real product fix, not a rubber-stamp pattern. Keep all alarm thresholds, algorithms, CODEX laws, anchors, and ledger gates unchanged. Ack only this observed statistical alarm with this note; run `alarms.py check` again and require a clean result before continuing.
