# EDGE-224 · ledger alarm re-audit

- `gap-too-fast` is expected for this focused atomic row: one local TLS end-to-end regression pass
  and four explicit `na` entries were written together; this is not five product observations.
- `discovery-collapse` reflects that no real App/generation/five-channel session was claimed. The
  managed duration invariant is covered through submit → poll → fetch → receipt; visual and
  discoverability evidence remain `na`.
- The test passed under `-race`; anchor calibration remains `10/10` and the sequence gate admitted
  only `EDGE-224`.
- Acknowledge both alarms against this re-audit without changing thresholds or the algorithm.
