# SURF-111 ledger and alarm re-audit

## Evidence decision

The green verdict covers the repaired exact-body read/activation path and the installed trust-gate before/after path. The initial Computer Use placeholder loss is retained as a negative instrumentation fact, not silently counted as exact input success. REST was used only to set up the exact durable fixture and to flip the explicit `approve-tools` state; all visual and activation assertions were re-observed in the real App.

## Ledger integrity

- `anchors.py check`: 10/10 calibration passed.
- Predecessor `SURF-110` was already five-level green before this row.
- No threshold, alarm algorithm, CODEX law, anchor set, or ledger gate was changed.

## Alarm handling

After the five serialized judgments, any `gap-too-fast` or `discovery-collapse` signal must be independently re-read against this session's finalized recording, backend/SSE/frontend/LLM journals, exact REST truth, and focused tests, then acked serially. A clean result means the statistics were caused by ledger serialization and the preserved negative input-bridge fact remains visible, not that discovery checks were bypassed.
