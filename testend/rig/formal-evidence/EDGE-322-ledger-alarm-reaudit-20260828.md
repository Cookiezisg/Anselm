# EDGE-322 · 账本与警报独立复审

本复审对应正式 session `20260828-022508` 与 `EDGE-322 L2` 新增裁决。证据文件、session manifest、封口录屏和五通道日志已重新读取；未修改警报阈值、算法、法条或锚点。

- `gap-too-fast`：本格是在真实操作、收台、证据落盘后写入，但与前一条账本写入处于同一自动化工作段，统计间隔不能代表产品操作时延；不把它解释为产品证据。
- `pass-burst`：本格只有一个真实 session，录屏 `163.741667s`，且包含启动等待、系统弹窗清理、页面操作和收台；速率曲线是账本写入时间分布的信号，不改变本格证据。
- `discovery-collapse`：本格是 L2 数据真相裁决，L3-L5 仍为 `na`，没有把 `na` 当作失败或把功能成功冒充视觉/可发现性通过；该曲线不应阻止本次已完成的独立复审。

复审结论：本格有真实五通道 session 和 G2 法条证据，三项警报均为统计层信号，按既有 `ack` 流程销账；下一格仍必须重新观察，不能沿用本次结果。
