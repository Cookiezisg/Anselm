# EDGE-205 · 账本警报复审

本格五个裁决在同一批次内快速写入，触发 `gap-too-fast`；最近 50 个裁决没有 fail，触发 `discovery-collapse`。这是账本统计信号，不是产品证据缺失被隐藏：本格 L1 的真实回归证据已封存，L2-L5 明确写成 `na`，没有借此伪造五级通过。

复审确认：未修改警报阈值、算法、法典或锚点；下一格仍须遵守相同的证据门。两项警报按本复审记录销账。
