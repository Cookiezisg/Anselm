# EDGE-216 · ledger alarm re-audit

- `gap-too-fast` is the expected short sequence for one atomic row: one focused L1 pass and four
  explicit `na` entries, not five independent product claims.
- `discovery-collapse` reflects the focused-only window. The reference blocker and unreferenced
  delete behavior passed; absent settings/five-channel evidence remains `na`.
- Anchor calibration remains `10/10`, and sequence order admitted only `EDGE-216`. Keep `✓~~~~` and
  acknowledge both alarms against this re-audit.
