# EDGE-300 L4 视觉证据（2026-09-01）

正式 session：`/private/tmp/anselm-rig-formal-20260831-11/sessions/20260901-144816`。

关键画面：`evidence/edge300-fairness-frames/cycle-priority-7.jpg` 与 `edge-strict-normal-followup.jpg`。顶部 capsule 在 approval 与 normal 两种语义间保持同一高度、同一右侧收束和清晰的控制层级；`Approve`/`Reject`/`Dismiss` 没有被长标题挤压，普通 notice 的 `View` 也没有造成跳变。候场只显示有限 cue，`Clear all N` 使用固定槽位，不随 backlog 数量扩展成大量控件。

合成测试名较长导致正文按设计最大宽度显示省略号，但未遮挡操作控件；这不作为产品缺陷。录屏与截图未发现白闪、裁切、RenderFlex overflow、间距跳变或布局重排。法条：C4。
