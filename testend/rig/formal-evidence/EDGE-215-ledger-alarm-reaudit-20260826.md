# EDGE-215 · ledger alarm re-audit

- `gap-too-fast` is the expected short sequence for one immutable-key row: the L1 focused test passed,
  and four higher levels remain explicit `na` rather than independent green judgments.
- `discovery-collapse` is explained by the focused-only window and does not justify promoting absent
  settings UI/five-channel evidence; the test also confirms normal user keys are not over-blocked.
- Anchor calibration remains `10/10`, and the first-unsettled sequence gate admitted only `EDGE-215`.
  Keep `✓~~~~` and acknowledge both alarms against this re-audit.
