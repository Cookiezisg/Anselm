# EDGE-222 账本/警报独立复审

- 日期：2026-08-30
- 对象：`EDGE-222|生成 origin 从凭证派生` 的真实 App L2 裁决
- 新证据水位：`2026-08-29T21:36:38.748721+00:00`

## 复核

- 锚点复核：`anchors.py check` 通过，`10/10`，锚点集未改，四小时校准窗口有效。
- 近 50 条裁决证据完整性：`50` 条，其中 `7` 条 pass、`0` 条 fail、`43` 条明确说明
  不适用；所有非 note 证据文件均存在，`missing_files=0`。
- `gap-too-fast` 重新打开是因为近窗包含前一批集中写入的历史裁决，中位间隔为 `13s`，
  不是本次真实 App 观察的耗时；本次 L2 session 从启动到收台为 `122.823333s`，含真实
  App、SSE、LLM wire、backend 和 frontend journal，并未修改该阈值。
- `discovery-collapse` 的 `0% fail` 不被解释为产品已无缺陷；近窗的 `43` 条是明确的
  适用性结论，`EDGE-222` 的 L3-L5 仍保持 provisional，自动顺序门仍未越过本行。
- 本次真实证据只为 EDGE-222 增加一个 L2 单元；没有借用其它 session、没有把生成 UI 的
  顺滑/craft/discoverability 结论挪到本路由 seam，也没有改变五级标准。
- 随后的 L3-L5 三条 `na` 均使用不适用理由写入顺序门：它们分别归属生成交互、媒体视觉
  与模型/密钥入口，不是缺少现场证据的 waiver；因此自动前线已推进到 `EDGE-223`。

## 处置

完成上述复核后，`gap-too-fast` 与 `discovery-collapse` 可按既有警报协议销账；阈值、
算法、法典、锚点和顺序门均不变。下一步仍须按顺序处理 EDGE-222 尚未收口的等级。
