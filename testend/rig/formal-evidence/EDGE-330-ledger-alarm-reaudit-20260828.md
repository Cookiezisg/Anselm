# EDGE-330 · ledger/alarm re-audit

## Re-audit

- Source session: `/private/tmp/anselm-rig-formal-20260801-3/sessions/20260828-033249`
- Ledger movement: one new judgment only, `EDGE-330|设置项搜索索引漂移|L2=pass`.
- `rig-check` passed while the real App was alive; `rig-down` finalized `screen.mov` at `217.146667s` and left no owned process behind.
- The apparent overlay failure was independently identified as a stale macOS CoreServicesUIAgent dialog from the intentionally closed prior App. It was dismissed before the final `rig-check`; the final check reported no external overlay.
- The source session contains the complete backend, SSE, frontend, LLM and window-recording journals. Backend/frontend contain no unexplained application redline; managed challenge/install/models are `200`; settings search itself has no completion and no fake one was claimed.
- The product evidence covers the real English `zoom` search, cross-panel result grouping, concrete-item jump and search clear, plus the `zzzqqxx` quiet no-match state. The focused index/anchor tests cover both locales and the two-way declaration/mount contract.

## Alarm decision

- `pass-burst`: expected signal from the required sequential five-level ledger protocol; this was one L2 judgment after a completed real session, not a batch of unobserved green claims.
- `discovery-collapse`: not supported by evidence. The path included the empty directory, concrete search results, an item jump that returns to the directory, and a quiet no-match state; the matching settings search/catalog/anchor tests passed.
- No threshold, law, anchor, coverage row definition, or gate is changed. The alarms are acknowledged for this judgment only and must be recomputed on the next batch.
