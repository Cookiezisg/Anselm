# Batch 48 unified gate — 2026-08-25

Scope: EDGE-001 through EDGE-011, 51/50 cells; no new product judgment is introduced by this file.

## Gate results

- Root `make verify`: first run correctly stopped on two real drift findings: frontend formatting in the changed
  transcript test, and `CHAT_TURN_TIMEOUT` registered in `error-codes.md` without a structured constructor. The
  test was formatted; loop now owns `ErrChatTurnTimeout` and `streamErrorCode` uses its stable `.Code`. Second
  root run passed backend, complete Flutter verify, docs, and demo.
- `make -C backend testend`: passed `github.com/sunweilin/anselm/testend/scenarios`, `285.126s`.
- Rig unit suite: `python3 -m unittest discover -s testend/rig -p 'test_*.py' -v`, `51` tests, all passed.
- `python3 -m py_compile testend/rig/*.py` and `bash -n testend/rig/*.sh`: passed.
- `gen_coverage.py --check`: clean, `848 rows / 508 carried judgments / 0 tombstones`.
- Anchor calibration: `10/10`; anchor hash/laws/thresholds/algorithm/gate unchanged.
- `alarms.py check`: clean, `241 live judgments`; `2300` baseline judgments excluded from drift curves. EDGE-010
  and EDGE-011 alarms were independently re-audited and acked, not disabled.
- Changed Go files were checked with `gofmt`; `git diff --check` passed.
- Listener/process audit after backend testend: no Anselm app, testend server, llama-server, ssetap, llmtap, or
  campaign port remained.

## Batch decision

All batch cells, five-level judgments, evidence files, gate outputs and cleanup audits are present. The batch is
eligible for commit. The formal sequence gate will determine the next frontier after commit; no new cell is started
during the commit audit.
