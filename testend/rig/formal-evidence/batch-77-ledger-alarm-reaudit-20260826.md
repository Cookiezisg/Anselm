# Batch 77 · ledger/alarm re-audit

## Scope

- 本批登记 `EDGE-291..300` 共 50 格：每行 L1 为聚焦测试证据通过，L2-L5 因未启动真实 App 与五通道会话而明确记 `na`。
- 账本从 3936 条裁决核对到 `848/797/0`：848 行、797 行已有裁决、0 tombstone；10/10 锚点校准通过。

## 警报复审

- `gap-too-fast`：50 格由同一批机器可复现的聚焦测试和证据文件连续登记，时间间隔 0 秒不代表跳过证据；本批没有把它解释成真实用户观测。
- `discovery-collapse`：本批没有 fail 结论，因为所有聚焦测试通过；这不改变法条、判定算法、锚点或覆盖范围。
- 复审只确认本批的 L1 证据和 `na` 边界，不修改阈值，不覆盖警报，不把 L2-L5 提升为通过。

## 结论

警报可由本独立复审销账；下一批仍须重新运行 `alarms.py check`，任何新警报重新进入复审队列。
