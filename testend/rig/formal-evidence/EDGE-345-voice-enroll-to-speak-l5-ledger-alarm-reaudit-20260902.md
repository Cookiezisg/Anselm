# EDGE-345 L5 账本与警报独立复审

- 本次新增 `EDGE-345|音色登记→指名说话全链` 的 L5=`G1`，证据为
  `testend/rig/formal-evidence/EDGE-345-voice-enroll-to-speak-l5-real-app-20260902.md`。
- 锚点仍为 `10/10`；复核没有修改 CODEX、G1 判定标准、警报阈值、五级标准或顺序策略。
- 复核了从全新 workspace/onboarding 到默认 Chat、附件入口、自然语言目标、完成结果、Settings 分区、hover 删除入口和删除后空态的真实 App 路径。可发现性依据是用户可见 affordance 与下一步说明，不是模型内部工具选择。
- 新增裁决后 `discovery-collapse` 仍按原阈值打开（近 50 条 fail share=`4.0%`）。这是必须复核的质量漂移信号，不是通过证明；本格有独立路径、封存录屏和五通道证据，确认无证据缺失或橡皮章，按原脚本 ack，未调整阈值。
