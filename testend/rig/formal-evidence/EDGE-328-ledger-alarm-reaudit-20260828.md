# EDGE-328 · 账本警报独立复审

## 范围

复审对象是 `EDGE-328 快捷键冷启动` 在 `2026-08-28` 写入的单条 L2 pass，以及随后由统计曲线打开的 `gap-too-fast` 与 `discovery-collapse`。

## 复审结论

- `gap-too-fast`：这是脚本写账动作很快触发的流程警报，不是证据缺失。独立复读了真实 session `/private/tmp/anselm-rig-formal-20260801-3/sessions/20260828-021456`、三张前后截图、51.711667 秒录屏、五通道收台输出和后端/前端/SSE 日志；没有把机械写账速度当成产品观察速度。
- `discovery-collapse`：近尾窗没有 fail 不能证明产品变干净，但本次目标是冷启动快捷键的正向/恢复路径，独立审查确认没有被隐藏的失败路径替代；锚点校准为 `10/10`，没有修改阈值、法典或裁决。
- `EDGE-328 L2` 仍有完整 `G2` 法条与真实五通道证据，不撤销；L3-L5 仍为 `na`，没有扩张裁决范围。

复审后按原规则 ack 两条警报。后续继续观察新鲜证据，不用 ack 绕过 gate。
