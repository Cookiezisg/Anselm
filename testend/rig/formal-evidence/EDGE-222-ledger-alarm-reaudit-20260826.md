# EDGE-222 · ledger alarm re-audit

- `gap-too-fast` is expected for this atomic focused row: one local regression pass and four
  explicit `na` entries were written together; that is not five independent product observations.
- `discovery-collapse` is a focused-only window signal. The regional-origin regression is green,
  while real App generation, independent SSE/LLM wire observation, visual craft review and
  discoverability remain honestly unexecuted and therefore `na`.
- Anchor calibration was rechecked at `10/10`; the sequence gate admitted only `EDGE-222`.
- After this independent re-audit, acknowledge both alarms and require the next cell to repeat the
  same evidence discipline. Do not change thresholds, the alarm algorithm, or the ledger verdict.
