# EDGE-030 · L3 裁决后的统计警报复审

本次新增裁决是 `EDGE-030|生成中再 Send|L3`，不是批量补写：证据文件
`EDGE-030-real-app-20260830-l3-queue.md` 记录了真实 App 窗口、全量解码后的 CFR 帧、
`measure latency` 输出 `16.7ms`、五通道互证和 A5 法条适用理由。前一格 L2 也有独立
真实 session 与 F1 证据；L4/L5 没有被顺带放行。

`gap-too-fast` 的触发原因是两个真实产品等级在相邻审计动作中连续落账，不能解释成“没有
看证据”；本次复审重新核对了 session 文件、原始录像时间轴、队列前后帧和后端/SSE/LLM
日志。`discovery-collapse` 的 0% fail 仍是保留的统计警报含义：它提示裁判可能过于乐观，
不是产品变得完美；本次仅按独立证据逐项复核，未修改阈值、法典、锚点或顺序门。

复审结论：两条警报均可销账，后续新裁决仍由原阈值重新监控；若下一次没有足够证据，gate
必须再次拒绝。
