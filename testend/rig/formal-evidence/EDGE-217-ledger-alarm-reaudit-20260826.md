# EDGE-217 · ledger alarm re-audit

- `gap-too-fast` is the expected sequence for one atomic row: one focused L1 pass and four explicit
  `na` entries, not five independent product claims.
- `discovery-collapse` reflects the focused-only window. The rotation/probe split-brain regression
  passed; absent settings/five-channel evidence remains `na`.
- Anchor calibration remains `10/10`, and sequence order admitted only `EDGE-217`. Keep `✓~~~~` and
  acknowledge both alarms against this re-audit.
