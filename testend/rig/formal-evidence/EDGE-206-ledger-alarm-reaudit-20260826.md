# EDGE-206 · 账本警报复审

本格五个裁决在同一批次内快速写入，触发 `gap-too-fast`；最近 50 个裁决没有 fail，触发 `discovery-collapse`。复审核对了 EDGE-206 的真实 L1 回归证据和 L2-L5 的明确 `na` 分层，没有将本地输入边界测试冒充完整产品 session。

未修改警报阈值、算法、法典或锚点；后续格继续受同一 gate 约束。两项警报按本记录销账。
