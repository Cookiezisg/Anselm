# SURF-071 · ledger/alarm re-audit

## Re-audit

- Time: `2026-08-19 23:40 +08`
- Clean source session: `/private/tmp/anselm-rig-formal-20260801-3/sessions/20260819-232740`
- Evidence reread: clean `screen.mov`, `recording-lifecycle.json`, `manifest.json`, `backend.log`, `sse.jsonl`, `frontend.log`, `llm.jsonl`, the 47-test Flutter output, and successful live `rig-check`.

## Independent result

- The five cells are based on a real product path, not on the static 102-entry marketplace count. The market was searched, a real plan was opened, an external credential was deliberately not submitted, and a local missing command was used to force a truthful failed state.
- The failed state was followed through list, detail, Tools, Call history, stderr, reconnect affordance and soft-delete confirmation. The import path was exercised with valid JSON and repeated to verify `Imported 1 · skipped 0` followed by `Imported 0 · skipped 1`.
- The first malformed import attempt was retained as a rig note: Chinese IME punctuation was not the intended ASCII JSON. After switching the input method, the exact valid JSON imported successfully. No product code was changed and no false failure was filed.
- SSE and backend evidence agree with UI transitions: each install produced an `mcp.installed` notification and each deletion produced `mcp.removed`; entity status frames recorded connecting→failed with the same sandbox error shown in the card.
- No law, threshold, coverage row, anchor or gate definition was weakened. The clean session had backend/frontend/LLM journals and all five physical channel attribution checks.

## Decision

The five judgments are admissible. Alarm state must be recomputed after this batch; any opened alarm requires a separate resolution note and acknowledgement, not a blanket waiver.
