# EDGE-241 · 账本与警报独立复审

- 复审对象：本格 L2-L5 的四项 `na` 裁决，以及真实错误 seed 启动探针
  `/private/tmp/anselm-rig-formal-20260831-11/sessions/20260831-170033`。
- L1 仍引用既有 `F5` 加密回归；本次没有新增 pass，也没有把后端启动错误、缺失 UI 或普通启动失败页冒充产品证据。
- 四项 `na` 均有独立适用性理由：`ANSELM_MASTER_KEY` 不是产品入口，错误 seed 在 HTTP 服务前终止，且该内部变更不产生独立交互、视觉对象或可发现入口。
- `judge.py` 已按 `na` 要求写入四条裁决；没有改 CODEX、阈值、锚点、正式序列或五级标准。
- 本次 `gap-too-fast` 与 `discovery-collapse` 只反映连续写账节奏，不反映产品质量。复审确认裁决内容与原始证据一致后，逐项 ack；未修改警报算法。
