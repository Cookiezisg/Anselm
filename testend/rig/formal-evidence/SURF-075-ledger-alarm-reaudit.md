# SURF-075 · 账本与警报复审

- Scope: the independent five-level judgment for `settings/panel-storage` and the real factory-reset path.
- Evidence: `/private/tmp/anselm-rig-formal-20260801-3/sessions/20260820-121523/evidence/SURF-075-settings-panel-storage-five-level.md`
- The formal session was owned by one conductor. The App-owned sidecar, bearer token, port, SSE tap, LLM tap, frontend journal, backend projection, and window recorder were all re-attributed after the product relaunch; the old crop was never reused after the one-pixel geometry change.
- The earlier external-backend deletion failure is retained as red diagnostic evidence. It is not used to support any green judgment.
- The final session has readable frame evidence, three independent SSE stream connections without a gap record, managed gateway bootstrap `200` responses, and clean frontend/backend application scans. No model completion is claimed for this deterministic settings path.
- `gap-too-fast` and `discovery-collapse` are expected to be re-audited after the five serial ledger writes. Any opened alarm must be acknowledged against this evidence without changing thresholds, algorithms, CODEX laws, anchors, or the ledger gate.
- Final judgment must remain five serial writes: `G1 / F1 / B2 / C4 / G1`.
