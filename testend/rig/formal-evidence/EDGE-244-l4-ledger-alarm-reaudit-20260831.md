# EDGE-244 L4 ledger/alarm re-audit

- Re-audit target: `EDGE|bearer token 缺失` L4=`C4`, using the sealed real-App
  session `/private/tmp/anselm-rig-formal-20260831-12/sessions/20260831-174529`.
- The reviewer re-read the stable frame, Computer Use accessibility state,
  repository radius token, connected-component measurement, contrast
  measurement, strict post-transition diff and all five-channel session
  journals. The L4 conclusion is tied to visual evidence and not copied from
  the L2/L3 verdicts.
- `gap-too-fast` is acknowledged only as ledger write-rate telemetry; the
  `25s` threshold remains unchanged. `discovery-collapse` is acknowledged as
  the trailing-window fail-share signal; the `5%` floor remains unchanged.
- Anchors remain `10/10`; `gen_coverage.py --check` remains `848 rows, 848
  carried judgments, 0 tombstones`. No alarm algorithm, CODEX law, sequence
  policy, five-level standard or product verdict was weakened.
