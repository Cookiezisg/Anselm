# EDGE-175 · MCP 失败附 stderr 尾 · L4 ledger/alarm re-audit

## Scope

- The prior L4 judgment was a genuine product failure, not a measurement or transport
  anomaly: `/private/tmp/edge175-final-20260830/final.png` showed the same MCP transport
  failure as raw primary copy, a second chassis error, assistant prose, and an Activity
  failure row.
- The failure evidence is non-empty and cites CODEX law `E1` (error three-part honesty).
- No pass judgment was written while `discovery-collapse` was open.

## Re-audit

- The failure is preserved as a red ledger fact; it is not deleted, rewritten, or counted as
  a successful discovery.
- This re-audit changes no alarm threshold, algorithm, CODEX law, anchor answer, or ledger
  sequence. It only records that the product defect is being fixed before the same cell can
  be reconsidered.
- The next L4 judgment must use a fresh real-App session with a finalized screen recording,
  backend journal, three SSE journals, frontend console journal, managed LLM wire journal,
  and a new visual inspection of the default collapsed/expanded error face.

## Decision

The alarm is safe to acknowledge for this red finding because the red evidence is explicit,
the corresponding law exists, and the subsequent pass is gated on new evidence. The alarm
must reopen if the fix is judged without a fresh session or if the duplicate technical error
remains in the default user-facing frame.
