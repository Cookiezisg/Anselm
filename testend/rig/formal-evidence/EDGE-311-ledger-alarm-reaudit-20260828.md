# EDGE-311 账本与警报复审

- target: `EDGE-311 / 归队重钉贴底 / L2`
- session: `/private/tmp/anselm-rig-formal-20260801-3/sessions/20260828-091845`
- primary evidence: `EDGE-311-back-to-live-reanchor-real-app-20260828.md`

本格在干净录屏 session 收台、五通道材料齐全、`rig-check` 无外部遮挡、锚点 `10/10` 后才由 `judge.py` 写入 `L2 pass (F5)`。没有修改 CODEX 法条、警报阈值、覆盖顺序或锚点答案。

本次 `discovery-collapse` 是尾部 50 条裁决中 fail 占比为 `0.0%` 的统计提醒，不是本格证据缺失。已重新核对 EDGE-311 的深跳帧、快速归队帧、AX 历史态消失、真实 head 重拉、backend/SSE/frontend/LLM tap journal 和完整 rig lifecycle；L3-L5 仍保持 `na`，没有把未测维度算作通过。按既有机制，本次独立复审后销账，阈值和算法保持不变。
