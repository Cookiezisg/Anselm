# EDGE-217 · ledger/alarm independent re-audit

## Scope

The four EDGE-217 judgments were written only after the fixed real-App frame and the
durable row state were independently reviewed. The re-audit acknowledges the expected
statistical alarms from four level writes without changing the thresholds or turning the
result into a claim that all key-management paths are defect-free.

## Evidence audit

- Formal session:
  `/private/tmp/anselm-rig-edge217-20260831-r2/sessions/20260831-112438`.
- The session contains a finalized window recording, fresh frontend/backend/SSE/LLM
  journals, the repaired failure frame, and a passed `rig-check`/`rig-down` lifecycle.
- The durable row proves the rotated mask is different from the initial mask and that
  `testStatus=error` was recorded after the rotation. The UI frame adds the product-level
  distinction that the rotation itself succeeded.
- The URL punctuation limitation is explicitly recorded in the primary evidence; no
  closed-port or external BYOK failure is inferred from it.

## Resolution

`gap-too-fast` and `discovery-collapse` are acknowledged as required review signals. The
resolution is this independently readable evidence and the next frontier, not a threshold
change. No CODEX law, anchor, five-level standard, sequence rule, or forced queue entry was
changed.
