# EDGE-223 · ledger alarm re-audit

- `gap-too-fast` is expected for this focused atomic row: one local HTTP regression pass and four
  explicit `na` entries were written together; this is not five product claims.
- `discovery-collapse` reflects that the row deliberately did not claim a real video generation
  session. The cancellation/error contract is covered; App, SSE, LLM wire, visual and
  discoverability evidence remain absent and therefore `na`.
- The independent video timeout test passed under `-race`; anchor calibration remains `10/10` and
  the sequence gate admitted only `EDGE-223`.
- Acknowledge both alarms against this re-audit without changing thresholds or the algorithm.
