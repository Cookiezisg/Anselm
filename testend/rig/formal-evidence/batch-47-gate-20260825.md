# Batch 47 unified gate — 2026-08-25

Scope: SURF-105 through SURF-114, 50/50 cells; no new product judgment is introduced by this file.

## Gate results

- `make verify`: passed backend, frontend, docs and demo. The first run correctly failed on frontend analyzer `unnecessary_cast` at `frontend/lib/features/chat/ui/tool_card_trigger.dart:26`; the cast was removed, the focused trigger/poll tests passed, and the complete root gate passed on the second run.
- Focused regression: `stage_director_provider_test.dart` real ordering case (`tool_result open → run_terminal → tool_result close`) passed; trigger card and poll wiring tests passed.
- `make -C backend testend`: passed `github.com/sunweilin/anselm/testend/scenarios`, `312.506s`.
- Rig unit suite: `python3 -m unittest discover -s testend/rig -p 'test_*.py' -v`, `50` tests, all passed.
- `gen_coverage.py --check`: clean, `848 rows / 497 carried judgments / 0 tombstones`.
- Anchor calibration: `10/10`; anchor hash/laws/thresholds/algorithm/gate unchanged.
- `alarms.py check`: clean, `185 live judgments`; `2300` baseline judgments excluded from drift curves. SURF-114 `gap-too-fast` and `discovery-collapse` were independently re-audited and acked, not disabled.
- Changed Go files: `gofmt` completed for `backend/internal/app/loop/redact.go` and `redact_test.go`; `git diff --check` passed.
- `python3 -m py_compile testend/rig/*.py` and `bash -n testend/rig/*.sh`: passed.
- Listener/process audit after backend testend: no Anselm app, testend server, llama-server, ssetap, llmtap, or campaign port remained.

## Gate-only repair

The first root gate exposed an unnecessary cast in the newly exercised trigger configuration projection. It was removed without changing the public contract or behavior; the focused tests and complete root gate then passed. No analyzer warning was suppressed.

## Batch decision

All 50 cells, five-level judgments, evidence files, gate outputs and cleanup audits are present. The batch is eligible for commit. The next formal frontier remains determined by the sequence gate after this commit; no new cell was started during the gate.
