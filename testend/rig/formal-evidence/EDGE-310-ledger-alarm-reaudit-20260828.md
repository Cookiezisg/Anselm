# EDGE-310 账本与警报复审

- target: `EDGE-310 / 深跳 ?around= 整窗替换 / L2`
- session: `/private/tmp/anselm-rig-formal-20260801-3/sessions/20260828-062450`
- primary evidence: `EDGE-310-transcript-deep-jump-real-app-20260828.md`

本格在统一 session 收台、五通道材料齐全、录屏可读、锚点 `10/10` 且 judge 尚未锁定前完成复核；`judge.py` 已写入 `L2 pass (F1)`。没有修改 CODEX 法条、警报阈值、覆盖顺序或锚点答案。

本次 `discovery-collapse` 是尾部 50 条裁决中 fail 占比为 `0.0%` 的统计提醒，不是本格证据缺失。已重新核对 EDGE-310 的真实 Computer Use 关键帧、AX 目标唯一性、深跳与回现场前后状态、REST `?around=` 返回、backend/SSE/frontend/LLM tap journal 以及 `rig-check`/`rig-down` 收台结果；L3-L5 仍保持 `na`，没有把未测维度算作通过。按既有机制，本次独立复审完成后销账，阈值和算法保持不变。
