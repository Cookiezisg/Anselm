# Batch 80 · ledger/alarm re-audit

## Scope

- 本批登记 `EDGE-321..324`、`EDGE-327..332`，共 50 格：每格 L1 由对应聚焦测试与源码契约证据支持；L2-L5 因本批没有真实 App / 五通道台架会话，明确记 `na`。
- `EDGE-325` 与 `EDGE-326` 已在先前批次完整落账，本批不重复写入。
- 账本核对：总裁决 `4136`（2300 baseline + 1836 live），`COVERAGE=848/827/0`，锚点仍为 `10/10`。

## 警报复审

- `gap-too-fast`：50 格的 250 次动作在脚本中连续登记，间隔中位数为 0 秒；这是批量账本动作的统计信号，不被解释为真实观看速度。每个 L1 均有独立 evidence 文件，且聚焦测试先于登记完成。
- `discovery-collapse`：本批没有 fail，但证据只覆盖实现级回归，真实 App 级别仍通过 L2-L5 保持 `na`；不把“测试无 fail”解释成产品已无缺陷。
- 不修改报警阈值、算法、法典、锚点或判定标准；复审仅销本批产生的统计警报。

## Conclusion

两项警报按独立复审结果销账；后续新裁决继续受到原有报警机制约束。
