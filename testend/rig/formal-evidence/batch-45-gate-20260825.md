# Batch 45 unified gate — 2026-08-25

Scope: SURF-085 through SURF-094, 50/50 cells; no new product judgment is introduced by this file.

## Gate results

- `make verify`: passed backend, frontend, docs and demo; the frontend four-group suite completed after the two gate-only drifts were repaired.
- `make -C backend testend`: passed, `github.com/sunweilin/anselm/testend/scenarios`, `300.030s`.
- Rig unit suite: `python3 -m unittest discover -s testend/rig -p 'test_*.py' -v`, `50` tests, all passed.
- `gen_coverage.py --check`: clean, `848 rows / 477 carried judgments / 0 tombstones`.
- Anchor calibration: `10/10`; no law, threshold, algorithm or anchor changed.
- `alarms.py check`: clean, `85 live judgments`; `2300` baseline judgments excluded from drift curves.
- Changed Go files: `gofmt -l` empty; `git diff --check` passed.
- Listener/process audit: no Anselm, ssetap, llmtap, testend server or campaign ports remained after rig teardown.

## Gate-only repairs

The first root gate run exposed two deterministic repository drifts, both fixed before the green rerun:

1. The newly added `frontend/lib/core/platform/native_accessibility.dart` was absent from the convergence coverage denominator. `UPDATE_COVERAGE=1` reconciled the set and added it as `pending` without changing existing statuses.
2. `frontend/test/app/workspace_gate_test.dart` explicitly booted `zh-CN` but asserted the retired English `Create a workspace`. The assertion now reads the same generated locale value used by the product. The focused workspace/coverage tests and the complete group 3 then passed.

These were gate/test truth corrections, not suppressed failures. The real-App SURF-094 evidence remains unchanged and retains the Computer Use Flutter AX bridge boundary in its frontend journal.
