# EDGE-225 · ledger alarm re-audit

- `gap-too-fast` is expected for this focused atomic row: one local capability/error regression pass
  and four explicit `na` entries were written together; this is not five product observations.
- `discovery-collapse` reflects that no real App/tool-turn/five-channel session was claimed. The
  absence gate and stale-call typed error are covered; UI, SSE, wire, visual and discoverability
  evidence remain `na`.
- The focused tests passed under `-race`; anchor calibration remains `10/10` and the sequence gate
  admitted only `EDGE-225`.
- Acknowledge both alarms against this re-audit without changing thresholds or the algorithm.
